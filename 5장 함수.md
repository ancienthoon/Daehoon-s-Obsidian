---
tags: [2학기전공과목]
---

# 5장. 함수 (Function)

> [!info] 강의 정보
> 
> - **과목**: 파이썬익스프레스
> - **단원**: 5장 — 함수
> - **연관 노트**: [[6장 리스트]] · [[7장_파이썬_자료구조II]]

---

## 🎯 학습 목표

- [ ] 함수의 개념을 학습한다
- [ ] 함수를 작성하는 방법을 학습한다
- [ ] 함수를 호출하여 사용하는 방법을 학습한다

---

## 📌 코드를 묶는 3가지 방법

|방법|설명|
|---|---|
|**함수** (function)|반복적으로 사용하는 코드를 묶은 것 — 프로그램의 빌딩 블록|
|**객체** (object)|서로 관련 있는 변수와 함수를 묶는 방법|
|**모듈** (module)|함수나 객체들을 소스 파일 안에 모은 것|

---

## 📌 참고 — 자료구조 미리보기 (6장·7장)

```python
a = [1, 'a', b]              # 리스트
b = (1, 'a', b)              # 튜플
capitals = {"Korea": "Seoul", "UK": "London"}  # 딕셔너리
print(capitals["Korea"])     # 키를 인덱스로 사용
```

---

## 1️⃣ 함수란?

> **특정 작업을 수행하는 명령어들의 모음에 이름을 붙인 것**

- 작업에 필요한 **데이터를 전달받고** (입력)
- 작업이 완료된 후 **결과를 호출자에게 반환** (출력)

```
입력 → [ 함수 abs() ] → 출력
       함수는 입력을 받아 처리한 후 출력하는 상자
```

---

## 2️⃣ 함수 작성 & 호출

### 기본 형식

```python
def 함수이름(매개변수1, 매개변수2, ...):
    명령문1
    명령문2
    return 결과값      # return문은 함수를 종료시키고 결과를 반환
```

### 예시 — 원의 넓이 계산

```python
def get_area(radius):        # 함수 헤더
    area = 3.14 * radius**2  # 함수 몸체
    return area

result = get_area(3)         # 함수 호출
print("반지름이 3인 원의 면적=", result)
# 출력: 반지름이 3인 원의 면적= 28.26
```

---

## 3️⃣ 함수의 순서 (중요!)

> [!warning] 파이썬은 인터프리터 언어 **위에서 아래로 한 줄씩 번역** → 함수 정의가 호출보다 먼저 나와야 함

```python
# ✅ 오류 없음 — 정의가 먼저
def test():
    result = get_area(3)       # 호출 시점에 get_area 아직 몰라도 OK
    print("면적=", result)     # test() 호출 전에 get_area 정의됨

def get_area(radius):
    return 3.14 * radius**2

test()   # ← 이 시점에 이미 get_area 정의됨

# ❌ 오류 발생 — 호출이 먼저
def test():
    result = get_area(3)       # 아직 get_area 모름!

test()   # ← 이 시점에 get_area 미정의 → 오류

def get_area(radius):
    return 3.14 * radius**2
```

> [!tip] pass 키워드 함수 헤더만 결정하고 몸체를 나중에 작성할 때 사용
> 
> ```python
> def sub():
>     pass   # 나중에 구현 예정 — 오류 없이 실행 가능
> ```

---

## 4️⃣ 매개변수 & 인수

```
함수 정의: def get_sum( start ,  end  ):
                        ↑         ↑
                    매개변수(parameter)

함수 호출: value = get_sum(  1  ,  10  )
                             ↑      ↑
                          인수(argument)
```

> [!note] 매개변수는 지역변수 함수 안에서 매개변수를 변경해도 **원래 인수는 변경되지 않음** (불변 객체의 경우)
> 
> ```python
> def set_radius(radius):
>     radius = 100   # 지역변수만 바뀜
> 
> r = 20
> set_radius(r)
> print(r)   # 20 — r은 그대로
> ```

---

## 5️⃣ 인수의 종류

### ① 디폴트 인수 (Default Argument)

```python
def greet(name, msg="별일없죠?"):   # msg에 기본값 설정
    print("안녕", name + ', ' + msg)

greet("영희")           # 안녕 영희, 별일없죠?
greet("철수", "잘지내?") # 안녕 철수, 잘지내?
```

---

### ② 키워드 인수 (Keyword Argument)

> 인수의 **이름을 지정**해서 전달 → 순서 변경 가능

```python
def sub(x, y, z):
    print("x=", x, "y=", y, "z=", z)

sub(10, 20, 30)          # 위치 인수 → x=10 y=20 z=30
sub(y=20, x=10, z=30)    # 키워드 인수 → x=10 y=20 z=30
sub(10, y=20, z=30)      # 혼합 → x=10 y=20 z=30

sub(z=10, 20, 30)        # ❌ SyntaxError — 위치 인수가 키워드 인수 앞에 있어야 함
```

> [!warning] 규칙 **위치 인수(positional)** 는 항상 **키워드 인수(keyword)** 앞에 있어야 함

---

### ③ 가변 인수 (Variable Argument)

#### `*args` — 위치 가변 인수 (튜플로 받음)

```python
def varfunc(*args):
    print(args)
    print(type(args))   # <class 'tuple'>

varfunc(10)           # (10,)
varfunc(10, 20, 30)   # (10, 20, 30)
```

```python
# 활용 예: 가변 인수 합산
def add(*numbers):
    sum = 0
    for n in numbers:
        sum += n
    return sum

print(add(10, 20))        # 30
print(add(10, 20, 30))    # 60
```

```python
# 활용 예: 선택 + 가변 인수 조합
def add_cal(choice, *args):
    if choice == 'add':
        result = sum(args)
    elif choice == 'mul':
        result = 1
        for i in args: result *= i
    return result

print(add_cal('add', 1, 2, 3, 4, 5))   # 15
print(add_cal('mul', 2, 3, 100))        # 600
```

---

#### `**kwargs` — 키워드 가변 인수 (딕셔너리로 받음)

```python
def myfunc(**kwargs):
    result = ""
    for arg in kwargs.values():   # value 값만 꺼냄
        result += arg
    return result

print(myfunc(a="Hi!", b="Mr.", c="Kim"))   # Hi!Mr.Kim
```

```python
def print_kwargs(**kwargs):
    print(kwargs)

print_kwargs(name='foo', age=3)
# {'name': 'foo', 'age': 3}   ← 딕셔너리
```

---

### 가변 인수 정리표

|호출 방식|함수 매개변수|받는 자료형|
|---|---|---|
|`func(1, 2, 3)`|`*args`|**튜플** `(1, 2, 3)`|
|`func(a=1, b=2)`|`**kwargs`|**딕셔너리** `{'a':1, 'b':2}`|

```python
# 정리 예시
def my_func_1(*args):       # 튜플로 받음
    for arg in args:
        print(arg)
my_func_1(1, 2, 3)          # 1 / 2 / 3

def my_func_2(**kwargs):    # 딕셔너리로 받음
    for key, value in kwargs.items():
        print(f"{key}: {value}")
my_func_2(name="Alice", age=30)   # name: Alice / age: 30
```

---

## 6️⃣ * 연산자로 언패킹하기

> **반복 가능한 객체(iterable)** 를 풀어서 개별 인수로 전달

```python
alist = [1, 2, 3]

print(alist)    # [1, 2, 3]  ← 리스트 통째로
print(*alist)   # 1 2 3      ← 언패킹 → print(1, 2, 3)과 동일

def sum(a, b, c):
    print(a + b + c)

sum(*alist)    # ✅ sum(1, 2, 3) 과 동일 — 인자 3개 전달 → 6
sum(alist)     # ❌ 오류 — 인자 1개(리스트)만 전달
```

|연산자|대상|동작|
|---|---|---|
|`*`|리스트, 튜플, 문자열 등|요소를 개별 인수로 언패킹|
|`**`|딕셔너리|키=값 형태로 언패킹|

---

## 7️⃣ 값 반환하기

### 항상 모든 경우에 반환하는 것이 좋다

```python
# ❌ 권장 안함 — radius가 음수면 아무것도 반환 안 됨
def get_area(radius):
    if radius > 0:
        return 3.14 * radius**2

# ✅ 권장 — 모든 경우 반환
def get_area(radius):
    if radius > 0:
        return 3.14 * radius**2
    else:
        return 0
```

### 여러 개의 값 반환 (튜플로 반환)

```python
def sub():
    return 1, 2, 3      # 튜플 (1, 2, 3) 으로 반환

a, b, c = sub()         # 언패킹으로 각각 받기
print(a, b, c)          # 1 2 3

# 2개짜리 예시
def get_input():
    return 2, 3

x, y = get_input()      # x=2, y=3
```

---

## 8️⃣ 변수의 범위 (Scope)

### 지역 변수 (Local Variable)

```python
def myfunc():
    x = 100       # 함수 안에서만 유효한 지역변수
    print(x)

myfunc()          # 100
# print(x)        # ❌ 함수 밖에서는 접근 불가
```

### 전역 변수 (Global Variable)

```python
gx = 100          # 전역변수 — 어디서나 접근 가능

def myfunc():
    print(gx)     # 전역변수 읽기 가능

myfunc()          # 100
print(gx)         # 100
```

### 함수 안에서 전역 변수 변경

```python
gx = 100

# ❌ 전역변수가 변경되지 않음
def myfunc():
    gx = 200      # 새로운 지역변수 gx 생성! 전역 gx와 다름
    print(gx)

myfunc()          # 200 (지역변수)
print(gx)         # 100 (전역변수 그대로)

# ✅ global 키워드로 명시하면 변경 가능
def myfunc():
    global gx     # "이 gx는 전역변수입니다" 명시
    gx = 200      # 이제 진짜 전역변수 변경

myfunc()
print(gx)         # 200
```

> [!tip] 변수 구분 규칙 함수 안에서 **값을 저장(=)하면** → 지역변수 생성  
> 함수 안에서 **값을 저장하지 않으면** → 전역변수 참조

---

## 9️⃣ 순환호출 (Recursion)

> **함수가 자기 자신을 호출**하여 문제를 해결하는 프로그래밍 기법

### 팩토리얼 예제

```
팩토리얼 정의:
  fact(n) = 1              if n = 1
  fact(n) = n * fact(n-1)  if n ≠ 1
```

```python
def factorial(n):
    if n == 1:
        return 1
    else:
        return n * factorial(n-1)   # 자기 자신 호출

n = int(input("정수를 입력하시오: "))
print(n, "!=", factorial(n))

# 입력: 10
# 출력: 10 != 3628800
```

```
호출 과정:
factorial(3)
  → 3 * factorial(2)
        → 2 * factorial(1)
                → 1  (반환)
        → 2 * 1 = 2  (반환)
  → 3 * 2 = 6  (반환)
```

---

## 🔟 인수 전달 방식 — Call by Assignment

> [!important] 파이썬의 인수 전달 방식 C++의 call by value / call by reference 대신  
> 파이썬은 **객체의 종류**에 따라 다르게 동작!

|객체 종류|해당 타입|함수 내 변경 시|
|---|---|---|
|**불변(Immutable)**|`int`, `float`, `str`, `tuple`|원본 **변경 안 됨** (call by value처럼)|
|**가변(Mutable)**|`list`, `set`, `dict`|원본 **변경됨** (call by reference처럼)|

```python
# 불변 객체 — 원본 유지
def func1(x):
    x = 42        # 새 객체 생성 (원본 y와 별개)
    print("x=", x, "id=", id(x))

y = 10
func1(y)
print("y=", y)    # y = 10 그대로

# 출력:
# x= 42  id= 1640249760  ← 다른 주소
# y= 10  id= 1640249248  ← 원본 주소 유지
```

```python
# 가변 객체 — 원본 변경됨
def func2(lst):
    lst[0] = 99   # 리스트는 가변 → 참조로 전달

values = [0, 1, 1, 2, 3, 5, 8]
func2(values)
print(values)     # [99, 1, 1, 2, 3, 5, 8] ← 원본이 바뀜!
```

---

## 💡 핵심 요약

> [!summary] 5장 핵심 정리
> 
> **① 함수 기본**
> 
> - `def 이름(매개변수):` 로 정의, `return` 으로 반환
> - 파이썬은 인터프리터 → **정의가 호출보다 먼저** 나와야 함
> - 몸체 미구현 시 `pass` 사용
> 
> **② 인수 종류**
> 
> - **디폴트 인수**: `def f(x, y=10)` — 기본값 설정
> - **키워드 인수**: `f(y=20, x=10)` — 이름으로 전달, 순서 무관
> - **가변 인수**: `*args` → 튜플, `**kwargs` → 딕셔너리
> 
> **③ 반환**
> 
> - 항상 모든 경우에 값을 반환하는 것이 좋음
> - 여러 값 반환 시 **튜플**로 반환 → 언패킹으로 받기
> 
> **④ 변수 범위**
> 
> - 함수 안에서 `=` 저장 → **지역변수** 생성
> - 전역변수 변경하려면 `global` 키워드 필요
> 
> **⑤ 인수 전달**
> 
> - 불변 객체(`int`, `str` 등) → 원본 변경 안 됨
> - 가변 객체(`list` 등) → **원본 변경됨** 주의!

---

#파이썬 #함수 #5장 #파이썬익스프레스 #재귀 #가변인수 #스코프