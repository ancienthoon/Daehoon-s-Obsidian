# 스택 (Stack)

---

## 1. 스택이란?

> **스택(Stack)**: 쌓아놓은 더미처럼, 데이터를 한쪽 끝에서만 넣고 빼는 자료구조

실생활 예시: 접시 더미, 책 더미, 택배 박스 쌓기

---

## 2. 스택의 특징

### 후입선출 (LIFO: Last-In First-Out)

> **가장 최근에 들어온 데이터가 가장 먼저 나간다**

```
초기: [A][B][C] 쌓인 상태
push(C) → [A][B][C]  ← top
push(D) → [A][B][C][D]  ← top
pop()   → [A][B][C]  ← top  (D가 먼저 나감)
```

---

## 3. 스택의 구조

```
      ↑ (push / pop)
  ┌───────┐
  │   C   │  ← top (스택 상단)
  ├───────┤
  │   B   │
  ├───────┤
  │   A   │  ← bottom (스택 하단)
  └───────┘
  
요소(element): 스택에 저장되는 각각의 데이터
```

---

## 4. 스택의 연산

### 핵심 연산

|연산|설명|
|---|---|
|`push(x)`|스택에 데이터 x를 **추가**|
|`pop()`|스택에서 top 데이터를 **삭제하면서 반환**|
|`peek(s)`|top 데이터를 **삭제하지 않고** 확인만 함|
|`is_empty(s)`|스택이 **공백 상태**인지 검사|
|`is_full(s)`|스택이 **포화 상태**인지 검사|
|`create()`|스택을 **생성**|

> [!note] peek vs pop
> 
> - `pop()`: 요소를 스택에서 **완전히 삭제하면서** 가져옴
> - `peek()`: 요소를 스택에서 **삭제하지 않고** 보기만 함

### push / pop 동작 예시

```
초기상태 → push(A) → push(B) → push(C) → pop()
  []         [A]       [A,B]    [A,B,C]   [A,B]
                                  ↑top      ↑top
```

---

## 5. 배열을 이용한 스택 구현

### 구현 원리

- 1차원 배열 `stack[]` 사용
- `top` 변수: 가장 최근에 입력된 자료의 **인덱스**를 가리킴
- 가장 먼저 들어온 요소 → `stack[0]`
- 가장 최근에 들어온 요소 → `stack[top]`
- **공백 상태이면 `top = -1`**

```
stack[MAX_STACK_SIZE]
┌────┬────┬────┬────┬────┐
│    │    │    │ 데 │ 데 │  ← top (현재 여기까지 차 있음)
└────┴────┴────┴────┴────┘
  4    3    2    1    0
```

### C++ 직접 구현 예시

```cpp
#include <iostream>
using namespace std;

const int MAX_STACK_SIZE = 100;

int stack[MAX_STACK_SIZE];
int top = -1;

// 공백 검사
bool is_empty() {
    return top == -1;
}

// 포화 검사
bool is_full() {
    return top == MAX_STACK_SIZE - 1;
}

// 삽입
void push(int val) {
    if (is_full()) {
        cerr << "스택 포화 오류" << endl;
        return;
    }
    stack[++top] = val;
}

// 삭제 및 반환
int pop() {
    if (is_empty()) {
        cerr << "스택 공백 오류" << endl;
        return -1;
    }
    return stack[top--];
}

// 확인만
int peek() {
    if (is_empty()) {
        cerr << "스택 공백 오류" << endl;
        return -1;
    }
    return stack[top];
}
```

---

## 6. STL의 스택 (`std::stack`)

> C++ STL에서는 스택을 **템플릿 클래스 `stack`** 으로 제공

### STL stack 멤버 함수 정리

|멤버 함수|기능|
|---|---|
|`s.size()`|저장된 원소 **개수** 반환 (물리적 용량 아님)|
|`s.empty()`|사이즈가 0인지 확인 (`true` / `false`)|
|`s.top()`|가장 나중에 들어간 원소 **반환** (삭제 X)|
|`s.push(val)`|스택에 val **삽입**|
|`s.pop()`|가장 나중에 들어간 원소 **삭제** (반환 X)|

### STL stack 사용 예시

```cpp
#include <iostream>
#include <stack>
using namespace std;

int main() {
    stack<int> st; // int 자료형을 보관하는 스택

    st.push(4); // 스택: [4]
    st.push(7); // 스택: [4, 7]
    st.push(8); // 스택: [4, 7, 8]

    cout << "stack size: " << st.size() << endl; // 3

    // 비었는지 확인하며 전체 출력
    while (!st.empty()) {
        cout << st.top() << " "; // 가장 최근 자료 확인
        st.pop();                // 가장 최근 자료 삭제
    }
    // 출력: 8 7 4  (LIFO 순서)

    return 0;
}
```

> [!tip] STL 사용 권장  
> 직접 배열로 구현하는 것보다 **STL `stack`을 사용하는 것이 안전하고 편리**하다.  
> 경쟁 프로그래밍(백준 등)에서도 STL 스택 사용이 기본.

---

## 7. 스택의 용도와 단점

### 용도

- **지역변수 저장** (함수 호출 시 스택 프레임)
- **임시 데이터 백업**
- **함수 호출의 순서 제어** (콜 스택)
- **수식 계산** (후위 표기식 계산)
- **괄호 검사**
- **미로 탐색** (DFS)

### 시스템 스택을 이용한 함수 호출 예시

```cpp
void func2() {
    return;
}

void func1() {
    func2();
}

int main() {
    func1();
    return 0;
}
```

```
호출 순서에 따른 콜 스택 변화:
① main() 실행      → [main()]
② func1() 호출     → [main(), func1()]
③ func2() 호출     → [main(), func1(), func2()]
④ func2() 반환     → [main(), func1()]
⑤ func1() 반환     → [main()]
```

### 단점

- LIFO 구조로 **우선순위 관련 문제**가 생길 수 있음
- 새 입력이 계속 들어오면 **바닥 쪽 데이터가 오랫동안 잔류**할 수 있음

---

## 8. 스택의 응용 ①: 괄호 검사

### 괄호의 종류

- 대괄호: `[` `]`
- 중괄호: `{` `}`
- 소괄호: `(` `)`

### 올바른 괄호의 조건

1. 왼쪽 괄호의 개수 = 오른쪽 괄호의 개수
2. 같은 괄호에서 왼쪽 괄호는 오른쪽 괄호보다 **먼저** 나와야 함
3. 괄호 사이에는 **포함 관계만** 존재 (교차 불가)

### 잘못된 괄호 예시

```
(a(b)       → 왼쪽 괄호 2개, 오른쪽 1개 → 불일치
a(b)c)      → 오른쪽 괄호가 더 많음
a{b(c[d]e}f) → {와 )가 교차됨
```

### 스택을 이용한 괄호 검사 알고리즘

```
{ A [ ( i+1 ) ] =0; }
→ {를 push → [를 push → (를 push
→ )를 만나면 → top에서 (를 pop하여 비교 → 일치!
→ ]를 만나면 → top에서 [를 pop하여 비교 → 일치!
→ }를 만나면 → top에서 {를 pop하여 비교 → 일치!
→ 스택이 비었으면 → 성공!
```

```cpp
#include <iostream>
#include <stack>
#include <string>
using namespace std;

bool checkBracket(const string& str) {
    stack<char> st;

    for (char ch : str) {
        // 여는 괄호는 push
        if (ch == '(' || ch == '[' || ch == '{') {
            st.push(ch);
        }
        // 닫는 괄호 처리
        else if (ch == ')' || ch == ']' || ch == '}') {
            if (st.empty()) return false; // 대응되는 여는 괄호 없음

            char top = st.top();
            st.pop();

            if (ch == ')' && top != '(') return false;
            if (ch == ']' && top != '[') return false;
            if (ch == '}' && top != '{') return false;
        }
    }
    return st.empty(); // 스택이 비어있어야 완전 성공
}

int main() {
    cout << checkBracket("{A[(i+1)]=0;}") << endl; // 1 (성공)
    cout << checkBracket("if((i==0)&&(j==0))")    << endl; // 0 (오류)
    return 0;
}
```

---

## 9. 스택의 응용 ②: 수식 계산

### 수식 표기법 3가지

|표기법|설명|예시|
|---|---|---|
|**중위(infix)**|연산자가 피연산자 **사이**|`2+3*4`|
|**전위(prefix)**|연산자가 피연산자 **앞**|`+2*34`|
|**후위(postfix)**|연산자가 피연산자 **뒤**|`234*+`|

### 중위 vs 후위 변환 예시

|중위 표기법|후위 표기법|
|---|---|
|`a+b`|`ab+`|
|`(a+b)*c`|`ab+c*`|
|`a+b*c`|`abc*+`|
|`2+3*4`|`234*+`|
|`a*b+5`|`ab*5+`|
|`(1+2)*7`|`12+7*`|

### 컴퓨터에서의 수식 계산 순서

```
중위 표기식 → 후위 표기식 → 계산
  2+3*4    →   234*+    →  14
```

모든 과정에서 **스택을 사용**한다.

---

## 10. 스택의 응용 ③: 후위 표기식 계산

### 알고리즘

1. 수식을 **왼쪽 → 오른쪽** 으로 스캔
2. **피연산자**이면 → 스택에 **push**
3. **연산자**이면 → 스택에서 피연산자 **2개를 pop** → 연산 수행 → 결과를 **push**
4. 모든 처리 후 스택에 남은 값이 **최종 결과**

### 예시: `8 2 / 3 - 3 2 * +` 계산

```
스캔  | 동작          | 스택 상태
------+---------------+----------
8     | push(8)       | [8]
2     | push(2)       | [8, 2]
/     | pop(2),pop(8) | [4]       ← 8/2=4
3     | push(3)       | [4, 3]
-     | pop(3),pop(4) | [1]       ← 4-3=1
3     | push(3)       | [1, 3]
2     | push(2)       | [1, 3, 2]
*     | pop(2),pop(3) | [1, 6]    ← 3*2=6
+     | pop(6),pop(1) | [7]       ← 1+6=7
종료  |               | 최종결과 = 7
```

### C++ 구현

```cpp
#include <iostream>
#include <stack>
#include <sstream>
#include <string>
using namespace std;

int evalPostfix(const string& expr) {
    stack<int> st;
    istringstream iss(expr);
    string token;

    while (iss >> token) {
        if (token == "+" || token == "-" || token == "*" || token == "/") {
            int b = st.top(); st.pop();
            int a = st.top(); st.pop();

            if      (token == "+") st.push(a + b);
            else if (token == "-") st.push(a - b);
            else if (token == "*") st.push(a * b);
            else if (token == "/") st.push(a / b);
        } else {
            st.push(stoi(token)); // 피연산자 push
        }
    }
    return st.top();
}

int main() {
    string expr = "8 2 / 3 - 3 2 * +";
    cout << evalPostfix(expr) << endl; // 7
    return 0;
}
```

---

## 11. 스택의 응용 ④: 미로 탐색 (DFS)

### 아이디어

- 현재 위치에서 **갈 수 있는 방향**을 모두 스택에 저장
- **막다른 길**을 만나면 스택에서 이전 위치를 꺼내어 다른 방향 탐색
- 이 방식 = **깊이 우선 탐색(DFS)**

### 미로 탐색 흐름 예시

```
시작: (1,1)
스택: [(1,1)]

→ (1,2) 이동 가능 → 스택: [(2,1), (1,2)]
→ (1,3) 이동 가능 → 스택: [(2,1), (1,3)]
→ (1,4) 이동 가능 → 스택: [(2,1), (2,3), (1,4)]
...막다른 길 → pop하여 (2,3)으로 되돌아감
```

### C++ 구현 (개념 코드)

```cpp
#include <iostream>
#include <stack>
#include <utility> // pair
using namespace std;

const int MAZE_SIZE = 6;
// 0 = 길, 1 = 벽
int maze[MAZE_SIZE][MAZE_SIZE] = {
    {1,1,1,1,1,1},
    {0,0,0,1,1,1},
    {1,1,0,1,1,1},
    {1,1,0,0,0,1},
    {1,1,1,1,0,1},
    {1,1,1,1,0,0}, // (5,5) = 출구
};

void solveMaze(int startR, int startC) {
    stack<pair<int,int>> st;
    bool visited[MAZE_SIZE][MAZE_SIZE] = {};

    st.push({startR, startC});

    int dr[] = {-1, 1, 0, 0}; // 상하좌우
    int dc[] = {0, 0, -1, 1};

    while (!st.empty()) {
        auto [r, c] = st.top();
        st.pop();

        if (visited[r][c]) continue;
        visited[r][c] = true;

        cout << "(" << r << "," << c << ") ";

        if (r == MAZE_SIZE - 1 && c == MAZE_SIZE - 1) {
            cout << "\n출구 도달!" << endl;
            return;
        }

        for (int i = 0; i < 4; i++) {
            int nr = r + dr[i];
            int nc = c + dc[i];
            if (nr >= 0 && nr < MAZE_SIZE &&
                nc >= 0 && nc < MAZE_SIZE &&
                maze[nr][nc] == 0 && !visited[nr][nc]) {
                st.push({nr, nc});
            }
        }
    }
    cout << "\n출구를 찾지 못했습니다." << endl;
}

int main() {
    solveMaze(1, 0);
    return 0;
}
```

---

## 12. 핵심 요약

```
스택 핵심 정리
┌──────────────────────────────────────────┐
│ 구조   : LIFO (후입선출)                  │
│ 연산   : push / pop / top / empty / size  │
│ 구현   : 배열 또는 STL stack<T> 사용      │
│         (경쟁 프로그래밍에서는 STL 권장)   │
│ 응용   : 괄호 검사, 수식 계산, DFS, 콜스택 │
└──────────────────────────────────────────┘
```

---

## 📋 연습 문제

- **ascode 1311번** - 스택 기초
- **ascode 1106번** - 괄호 검사
- **ascode 1539번** - 균형잡힌 세상 #2
- **ascode 1313번** - 후위 표기식 계산하기
- **ascode 1314번** - Web Navigation