```
단순 연결 리스트에 다음 순서로 연산을 수행하라.

insertBack(1) → insertBack(2) → insertBack(3)
→ insertFront(0) → remove(2) → insertAfter(1, 5)

최종 리스트를 그리고, 각 단계를 설명하라.
```

```
텍스트 에디터의 "undo(실행 취소)" 기능을 구현하려 한다. 사용자의 입력 이력을 저장하고, undo 시 가장 최근 입력부터 취소된다. 어떤 자료구조를 사용할 것인가? 이유와 함께 핵심 연산의 시간복잡도를 제시하라.
```