# 📘 제12장 템플릿 (Template)

#cpp #template #일반화프로그래밍 #STL #OOP

---

## 📋 목차

- [[#🧩 템플릿이란?]]
- [[#🔧 함수 템플릿 (Function Template)]]
- [[#🏗️ 클래스 템플릿 (Class Template)]]
- [[#📁 파일 분할 시 주의사항]]
- [[#📦 스택 예제 (템플릿 클래스 응용)]]
- [[#⚠️ 핵심 정리]]

---

## 🧩 템플릿이란?

- **템플릿(template)**: 함수나 클래스를 **찍어내기 위한 틀**
- 붕어빵 틀(template) → 팥/슈크림 등 다양한 속 재료(타입) 가능
- **일반화 프로그래밍(Generic Programming)**: 하나의 코드로 다양한 타입에 재사용

|종류|설명|
|---|---|
|함수 템플릿|함수를 찍어내는 틀|
|클래스 템플릿|클래스를 찍어내는 틀|

> 컴파일러가 **호출 시점에 전달된 타입**을 보고 실제 함수/클래스 본체를 자동 생성

---

## 🔧 함수 템플릿 (Function Template)

### 기본 문법

```cpp
template <typename T>      // T는 타입 변수처럼 사용
T get_max(T x, T y) {
    if (x > y) return x;
    else return y;
}
// "template<typename T>" 는 바로 아래 함수 하나에만 적용 → 함수당 1개 필요
```

### 호출 방법

```cpp
// 방법 1: 타입 자동 추론 (인자에서 타입 결정)
cout << get_max(1, 3);       // T = int  자동 결정
cout << get_max(1.2, 3.9);   // T = double 자동 결정

// 방법 2: 타입 명시 호출
cout << get_max<int>(1, 3);
cout << get_max<double>(1.2, 3.9);
```

### 두 개의 타입 매개변수

```cpp
template <typename T1, typename T2>
void copy(T1 a1[], T2 a2[], int n) {
    for (int i = 0; i < n; ++i)
        a1[i] = a2[i];
}

// 사용: 서로 다른 타입 배열 복사 가능
int a[] = {1, 2, 3, 4, 5};
float x[5];
copy(x, a, 5);   // T1=float, T2=int
```

### 함수 원형(시그니처) 작성 시

```cpp
// 원형과 본체 모두에 template<typename T> 작성 필요
template <typename T>
T get_max(T x, T y);           // 원형

template <typename T>
T get_max(T x, T y) { ... }    // 본체
```

---

### 🔀 템플릿 특수화 & 함수 중복

#### 우선순위: **함수 중복 > 함수 템플릿**

```cpp
template <typename T>
T Max(T a, T b) {
    return a > b ? a : b;
}

// char* 인 경우 템플릿으로 처리 시 주소값을 비교 → 의도와 다른 결과
// → 별도 중복 함수 작성
char* Max(char* a, char* b) {
    return strlen(a) > strlen(b) ? a : b;  // 길이 비교
}
// char* 인자 → 중복 함수 먼저 호출
```

#### 템플릿 특수화 (`template <>`)

```cpp
template <typename T>
void print_array(T a[], int n) { ... }        // 일반 버전

template <>
void print_array(char a[], int n) {           // char 특수화
    cout << a << endl;
}
// template<> 없이 일반 함수로 작성해도 동일 효과 (중복 함수)
```

> ⚠️ **주의**: `max(6, 3.52)` 처럼 두 인자의 타입이 다르면 오류 → T 는 하나의 타입만

---

## 🏗️ 클래스 템플릿 (Class Template)

### 기본 문법

```cpp
template <typename T>
class Box {
    T data;           // T는 타입(type)을 나타냄
public:
    Box() { }
    void set(T value) { data = value; }
    T get() { return data; }
};
```

### 객체 생성 방법

```cpp
// 반드시 타입을 명시해야 함: 클래스이름<type> 객체명
Box<int>    box;
box.set(100);

Box<double> box1;
box1.set(3.141592);
```

### 클래스 외부에 멤버함수 정의

```cpp
template <typename T>
class Box {
    T data;
public:
    Box();
    void set(T value);
    T get();
};

// 외부 정의 형식: template<typename T> + 클래스이름<T>::함수이름
template <typename T>
Box<T>::Box() { }

template <typename T>
void Box<T>::set(T value) { data = value; }

template <typename T>
T Box<T>::get() { return data; }
// cf) 일반 멤버함수 외부 형식: 반환형 클래스이름::함수이름(...)
```

### 두 종류의 타입 매개변수

```cpp
template <typename T1, typename T2>
class Box2 {
    T1 f_data;
    T2 s_data;
public:
    Box2() { }
    void set_f(T1 v) { f_data = v; }
    void set_s(T2 v) { s_data = v; }
    T1 get_f() { return f_data; }
    T2 get_s() { return s_data; }
};

// 외부 정의 시: T1, T2 모두 작성 필요
template <typename T1, typename T2>
T1 Box2<T1, T2>::get_first() { return f_data; }

// 사용
Box2<int, double> b;
b.set_f(10);
b.set_s(3.14);
```

### 템플릿 매개변수에 일반 변수 선언

```cpp
template <typename T, int len>
class SimpleArray {
    T arr[len];
public:
    T& operator[](int idx) { return arr[idx]; }
};

SimpleArray<int, 5>    i5arr;  // int 배열 크기 5
SimpleArray<double, 7> d7arr;  // double 배열 크기 7
// SimpleArray<int,5> 와 SimpleArray<int,7> 은 서로 다른 타입으로 인식
```

---

## 📁 파일 분할 시 주의사항

### ✅ 한 파일에 모두 작성 → 문제 없음

```
[하나의 .cpp 파일]
template 정의 + main() 모두 작성 → 컴파일 시 함수 본체 자동 생성
```

### ❌ 템플릿 본체를 별도 .cpp 에 분리 → **링크 오류 (LNK Error)**

```
main.cpp   - #include "signature.h" (원형만 있음)
fn.cpp     - template 본체 있음
signature.h - template 원형만 있음

문제: main.cpp 컴파일 시 template 정의가 없으므로
     함수 본체를 생성 못함 → 링크 시 찾을 수 없음 → ERROR
```

### ✅ 올바른 파일 분할 해결책

```cpp
// signature.h (또는 .hpp) 에 template 본체까지 모두 포함
#pragma once
template <typename T>
T sum(T a, T b) {
    return (a + b);    // 원형이 아닌 본체를 헤더에 작성
}

// main.cpp
#include "signature.h"  // include 하면 한 파일에 있는 것과 동일 효과
```

> ⚠️ **핵심**: 템플릿은 **정의(틀)**이므로 헤더파일(.h/.hpp)에 본체까지 모두 작성하는 것이 일반적

|구분|일반 함수|템플릿 함수|
|---|---|---|
|헤더파일|원형만|**본체까지**|
|소스파일|본체|(불필요)|

---

## 📦 스택 예제 (템플릿 클래스 응용)

### 스택 개념

- **스택(Stack)**: 후입선출(LIFO: Last-In First-Out) 자료구조
- `top`: 가장 나중에 저장된 데이터의 인덱스
    - 빈 상태: `top = -1`
    - push: `top++` 후 `stack[top]`에 저장
    - pop: `stack[top]` 반환 후 `top--`

### 공백/포화 상태

|상태|조건|
|---|---|
|공백(empty)|`top == -1`|
|포화(full)|`top == size - 1`|

### 스택 연산 알고리즘

```
push(x):
  if isFull() → throw FullStack 예외
  else: top++ → stack[top] = x

pop():
  if isEmpty() → throw EmptyStack 예외
  else: e = stack[top] → top-- → return e
```

### 템플릿 스택 구현 코드

```cpp
// 예외 클래스
class FullStack {
    int top, size;
public:
    FullStack(int t, int s) : top(t), size(s) { }
    void Show() { cout << "stack full -> size:" << size << " top:" << top << endl; }
};

class EmptyStack {
    int top, size;
public:
    EmptyStack(int t, int s) : top(t), size(s) { }
    void Show() { cout << "stack empty -> size:" << size << " top:" << top << endl; }
};

// 스택 클래스 템플릿
template <typename T>
class Stack {
    T* s;
    int size, top;
public:
    Stack(int n = 100) : size(n), top(-1) { s = new T[size]; }
    ~Stack() { delete[] s; }
    bool isEmpty() const { return top == -1; }
    bool isFull() const { return top == size - 1; }
    void push(T v);
    T pop();
};

template <typename T>
void Stack<T>::push(T v) {
    if (isFull()) throw FullStack(top, size);
    s[++top] = v;
}

template <typename T>
T Stack<T>::pop() {
    if (isEmpty()) throw EmptyStack(top, size);
    return s[top--];
}
```

### 사용 예시

```cpp
Stack<int> s;   // int 타입 스택, 크기 100
try {
    s.push(100); s.push(200); s.push(300);
    cout << s.pop() << endl;  // 300
    cout << s.pop() << endl;  // 200
    cout << s.pop() << endl;  // 100
    cout << s.pop() << endl;  // 비어있음 → EmptyStack 예외
}
catch (FullStack e)  { e.Show(); }
catch (EmptyStack e) { e.Show(); }
```

---

## ⚠️ 핵심 정리

### 함수 vs 클래스 템플릿 문법 비교

|구분|함수 템플릿|클래스 템플릿|
|---|---|---|
|선언|`template<typename T>`|`template<typename T>`|
|사용|호출 시 타입 자동 추론|객체 생성 시 타입 명시 `Box<int>`|
|외부 정의|`template<typename T>` + `반환형 함수이름(...)`|`template<typename T>` + `반환형 클래스<T>::함수이름(...)`|

### 자주 하는 실수

> ⚠️ 클래스 템플릿 객체 생성 시 타입 명시 필수: `Box box;` ❌ → `Box<int> box;` ✅ ⚠️ 템플릿 본체를 별도 `.cpp`에 분리하면 **링크 오류** → **헤더파일에 본체까지** 작성 ⚠️ `template<typename T>` 는 바로 **아래 함수/클래스 하나에만** 적용됨 ⚠️ 외부 멤버함수 정의 시 클래스명 형식: `Box<T>::함수이름` (일반 클래스는 `Box::함수이름`) ⚠️ 함수 중복과 템플릿 특수화 중복 시: **함수 중복(일반 함수)이 우선** ⚠️ 같은 템플릿 함수에서 타입이 다른 두 인자는 오류: `max(6, 3.52)` ❌