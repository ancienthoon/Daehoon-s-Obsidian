# 📘 논리설계 7장 — 순차회로의 분석과 설계

---

## 🔖 목차

1. [[#순차회로 개요]]
2. [[#7.1 래치 Latch]]
3. [[#SR 래치 NOR 게이트]]
4. [[#S'R' 래치 NAND 게이트]]
5. [[#En 신호를 가진 SR 래치]]
6. [[#D 래치]]
7. [[#7.2 플립-플롭 Flip-flop]]
8. [[#D 플립-플롭]]
9. [[#SR 플립-플롭]]
10. [[#JK 플립-플롭]]
11. [[#T 플립-플롭]]
12. [[#핵심 정리]]

---

## 📌 순차회로 개요

### 조합회로 vs 순차회로

|구분|조합회로|순차회로|
|---|---|---|
|출력 결정 요소|현재 입력만|현재 입력 + 과거 입력/출력|
|기억 소자|없음|있음 (래치, 플립-플롭)|
|예시|가산기, MUX|레지스터, 카운터|

### 상태 (State)

- 어떤 한 시점에서 순차회로의 **기억 소자에 저장된 2진 정보**
- 현재 상태(Q(t)) → 입력과 함께 → 다음 상태(Q(t+1)) 결정

### 기억 소자의 분류

```
래치 (Latch)
  └── 기본 게이트 회로로 구성
  └── 비동기 회로 — 입력 변하면 즉시 출력 변화

플립-플롭 (Flip-flop)
  └── 래치에 별도 회로 추가
  └── 동기 회로 — CLK 에지(순간)에만 상태 변화
```

---

## 📌 7.1 래치 Latch

> **핵심**: 0 또는 1, 두 가지 안정 상태 중 하나를 **기억(저장)** 하는 회로

### 구현 방법 두 가지

| 방식       | 게이트      | 이름      | 금지 입력             |
| -------- | -------- | ------- | ----------------- |
| NOR 게이트  | NOR × 2  | SR 래치   | S=R=1             |
| NAND 게이트 | NAND × 2 | S'R' 래치 | S'=R'=0 (즉 S=R=0) |

---

## 📌 SR 래치 (NOR 게이트)

### 동작 원리

- NOR 게이트 특성: **입력 중 1이 하나라도 있으면 출력 = 0**
- Q와 Q'이 서로 피드백되어 상태 유지

### 특성표

| S   | R   | Q(t+1)  | 상태       |
| --- | --- | ------- | -------- |
| 0   | 0   | Q(t) 불변 | 이전 상태 유지 |
| 0   | 1   | 0       | 리셋 상태    |
| 1   | 0   | 1       | 세트 상태    |
| 1   | 1   | (부정)    | ⚠️ 금지 입력 |

### 각 입력 케이스 요약

```
S=0, R=0 → Q 값 그대로 유지 (안정 상태까지 수렴 후 고정)
S=0, R=1 → Q=0 으로 리셋 (초기값 상관없이 결국 0)
S=1, R=0 → Q=1 로 세트  (초기값 상관없이 결국 1)
S=1, R=1 → Q=Q'=0 → ⚠️ 보수 관계 깨짐 → 금지!
```

> **⚠️ 실수 주의**: S=R=1은 Q와 Q'이 둘 다 0이 되어 보수 관계(Q≠Q')가 깨지므로 **절대 사용 금지**

---

## 📌 S'R' 래치 (NAND 게이트)

> SR 래치와 입력 논리가 **반전**됨에 주의!

### 특성표

|S'|R'|Q(t+1)|상태|
|---|---|---|---|
|0|0|(부정)|⚠️ 금지 입력|
|0|1|1|세트 상태|
|1|0|0|리셋 상태|
|1|1|Q(t) 불변|이전 상태 유지|

### SR 래치 vs S'R' 래치 비교

| 항목       | SR 래치 (NOR) | S'R' 래치 (NAND) |
| -------- | ----------- | -------------- |
| 이전 상태 유지 | S=0, R=0    | S'=1, R'=1     |
| 세트 (Q→1) | S=1, R=0    | S'=0, R'=1     |
| 리셋 (Q→0) | S=0, R=1    | S'=1, R'=0     |
| 금지 입력    | S=R=1       | S'=R'=0        |

---

## 📌 En 신호를 가진 SR 래치

> **목적**: Enable 신호가 활성화될 때만 래치가 동작하도록 제어

### 동작

```
En = 0 → 입력 무시, 이전 상태 유지
En = 1 → SR 래치와 동일하게 동작
```

### 특성표

| En  | S   | R   | Q(t+1)  |
| --- | --- | --- | ------- |
| 0   | x   | x   | Q(t) 불변 |
| 1   | 0   | 0   | Q(t) 불변 |
| 1   | 0   | 1   | 0       |
| 1   | 1   | 0   | 1       |
| 1   | 1   | 1   | (부정)    |

---

## 📌 D 래치

> **SR 래치에 인버터 추가** → S와 R이 항상 반대 값 → 금지 입력 원천 차단

### 구조

```
D 입력 ──┬──── S (S'R' 래치)
         └── NOT → R
```

### 특성표

|En|D|Q(t+1)|
|---|---|---|
|0|x|Q(t) 불변 (저장)|
|1|0|0 (입력 반영)|
|1|1|1 (입력 반영)|

### 래치의 문제점 → 플립-플롭의 필요성

- En=1을 **길게** 유지하면 → D 입력이 바뀔 때마다 출력 Q도 즉시 변함
- **잡음(noise)**에 의해 D가 순간 변하면 → 원하지 않는 출력 변화 발생
- **해결책**: En(CLK) 신호를 아주 짧은 순간만 활성화 → **플립-플롭**

---

## 📌 7.2 플립-플롭 Flip-flop

### 래치와의 핵심 차이

| 구분     | 래치          | 플립-플롭                |
| ------ | ----------- | -------------------- |
| 동작 시점  | En=1인 동안 계속 | CLK **에지(전이 순간)**에만  |
| 회로 유형  | 비동기         | **동기 (synchronous)** |
| 잡음 강인성 | 약함          | 강함                   |

### 클록 에지 트리거

```
상승 에지 트리거 (Positive Edge / Rising Edge)
  → CLK: 0 → 1 전이 순간에 입력 반영
  → 그래픽 기호: CLK 단에 ▷ 만 표시

하강 에지 트리거 (Negative Edge / Falling Edge)
  → CLK: 1 → 0 전이 순간에 입력 반영
  → 그래픽 기호: CLK 단에 버블(○) + ▷ 표시
```

---

## 📌 D 플립-플롭

> **구성**: D 래치 2개 (마스터-슬레이브 구조)

### 동작

- CLK: 1→0 전이 순간의 D 입력값 → Q에 저장
- CLK = 0 또는 1 상태에서는 상태 변화 없음

### 특성표 (하강 에지 트리거)

| D   | CLK | Q(t+1)  |
| --- | --- | ------- |
| 0   | ↓   | 0 (리셋)  |
| 1   | ↓   | 1 (세트)  |
| X   | 0/1 | Q(t) 불변 |

### 특성 함수

$$Q(t+1) = D$$

> D 플립-플롭은 **입력을 그대로 저장**하는 가장 단순한 F/F

---

## 📌 SR 플립-플롭

> SR 래치에 CLK 에지 트리거 추가 → 동기식 회로

### 특성표 (하강 에지 트리거)

| S   | R   | CP  | Q(t+1)  |
| --- | --- | --- | ------- |
| 0   | 0   | ↓   | Q(t) 불변 |
| 0   | 1   | ↓   | 0 (리셋)  |
| 1   | 0   | ↓   | 1 (세트)  |
| 1   | 1   | ↓   | (부정) ⚠️ |

### 특성 함수

$$Q(t+1) = S + R'Q(t), \quad SR = 0 \text{ 조건}$$

---

## 📌 JK 플립-플롭

> **SR F/F의 문제점 해결** — S=R=1 금지 입력 문제를 보완

### 4가지 동작 모드

| J   | K   | Q(t+1) | 동작                |
| --- | --- | ------ | ----------------- |
| 0   | 0   | Q(t)   | 불변 (저장)           |
| 0   | 1   | 0      | 리셋                |
| 1   | 0   | 1      | 세트                |
| 1   | 1   | Q(t)'  | **토글** ← SR과의 차이! |

### 구성 방법

- D 플립-플롭에 외부 회로 추가
- 입력 함수 (D에 넣는 값):

$$D = JQ'(t) + K'Q(t)$$

### 각 입력별 확인

```
J=0, K=0 → D = 0 + Q = Q     → Q(t+1) = Q(t)    (불변)
J=0, K=1 → D = 0 + 0 = 0     → Q(t+1) = 0       (리셋)
J=1, K=0 → D = Q' + Q = 1    → Q(t+1) = 1       (세트)
J=1, K=1 → D = Q' + 0 = Q'   → Q(t+1) = Q(t)'  (토글)
```

### 특성 함수

$$Q(t+1) = K'Q(t) + JQ'(t)$$

---

## 📌 T 플립-플롭

> **JK F/F에서 J=K=T로 묶은 것** → 입력 하나만 사용

### 동작

```
T = 0 (JK=00) → 상태 불변
T = 1 (JK=11) → 토글 (Q가 0이면 1, 1이면 0)
```

### 특성표

| T   | Q(t+1)   |
| --- | -------- |
| 0   | Q(t) 불변  |
| 1   | Q(t)' 토글 |

### 특성 함수

$$Q(t+1) = T'Q(t) + TQ'(t) = T \oplus Q(t)$$

> T 플립-플롭의 특성 함수는 **XOR 연산**과 동일!

---

## 🗂️ 핵심 정리

### 4가지 플립-플롭 특성 함수 한눈에

| F/F 종류     | 특성 함수                     | 특이사항       |
| ---------- | ------------------------- | ---------- |
| **D F/F**  | Q(t+1) = D                | 입력 그대로 저장  |
| **SR F/F** | Q(t+1) = S + R'Q(t), SR=0 | S=R=1 금지   |
| **JK F/F** | Q(t+1) = K'Q(t) + JQ'(t)  | J=K=1 → 토글 |
| **T F/F**  | Q(t+1) = T ⊕ Q(t)         | T=1 → 토글   |

### 래치 → 플립-플롭으로의 발전 과정

```
SR 래치 (NOR)
  └── En 추가 → SR 래치 with En
        └── 인버터 추가 → D 래치
              └── 에지 트리거 추가 → D 플립-플롭
                    └── JK/SR/T 플립-플롭으로 확장
```

### ⚠️ 시험 출제 포인트 정리

|포인트|내용|
|---|---|
|SR 래치 금지 입력|NOR: S=R=1 / NAND: S'=R'=0|
|래치 vs 플립-플롭|래치=비동기, 플립-플롭=동기(CLK 에지)|
|JK의 토글 조건|J=K=1일 때만 토글 발생|
|T F/F 특성 함수|XOR 연산과 동일|
|D F/F 특성 함수|Q(t+1) = D (가장 단순)|

---

> 📅 작성일: 2026-05-11  
> 🏫 과목: 논리설계  
> 📖 단원: 7장 — 순차회로 (래치, 플립-플롭)