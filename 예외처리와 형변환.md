# 📘 제11장 예외 처리와 형변환

#cpp #예외처리 #exception #OOP

---

## 📋 목차

- [[#💥 예외(Exception)란?]]
- [[#🔧 예외 처리기 구조 (try / throw / catch)]]
- [[#🔄 예외 전달]]
- [[#📚 다중 catch 문장]]
- [[#🏗️ 자신의 예외 클래스 작성]]
- [[#🧬 상속 관계의 예외 클래스]]
- [[#⚠️ 핵심 정리]]

---

## 💥 예외(Exception)란?

- **예외(exception)**: 잘못된 코드, 부정확한 데이터, 예외적인 상황에 의해 발생하는 **런타임 오류**
- 예시: 0으로 나누기, 배열 인덱스 초과, 디스크 하드웨어 에러

### 전통적 오류 처리 vs 예외 처리

| 방식             | 특징                      | 단점/장점                  |
| -------------- | ----------------------- | ---------------------- |
| `if-else` 사용   | 조건 검사로 오류 처리            | 정상 코드와 오류 코드가 뒤섞여 복잡해짐 |
| `try-catch` 사용 | 정상 코드와 오류 처리 코드를 **분리** | 코드가 깔끔하고 유지보수 쉬움       |
|                |                         |                        |

---

## 🔧 예외 처리기 구조 (try / throw / catch)

```cpp
try {
    // 예외가 발생할 수 있는 위험한 코드
    if (persons == 0)
        throw persons;       // 예외를 던진다
    // 정상 실행 코드
}
catch (int e) {              // 던진 타입과 일치해야 처리됨
    cout << "사람이 " << e << "명입니다." << endl;
}
```

### 실행 흐름

```
예외 없는 경우: try 블록 전체 실행 → catch 건너뜀
예외 발생 경우: throw 이후 코드 실행 안됨 → catch 블록으로 점프
```

> ⚠️ **잘못된 코드 패턴**: 관련 있는 코드는 반드시 같은 `try` 블록 안에 묶어야 한다. catch 이후에도 실행되면 안 되는 코드가 try 밖에 있으면 오류 발생!

### catch 블록 매개변수 규칙

| 규칙                                   | 예시                              |
| ------------------------------------ | ------------------------------- |
| throw 타입과 catch 타입이 **정확히 일치**해야 처리됨 | `throw int` → `catch(int e)` ✅  |
| 타입 불일치 시 처리 안 됨                      | `throw int` → `catch(char e)` ❌ |
| 모든 타입을 잡으려면 `...` 사용                 | `catch(...) { }`                |

```cpp
catch(...) {
    // 모든 타입의 예외를 처리
}
```

---

## 🔄 예외 전달

### 개념

- `throw`가 없어도, **호출된 함수 내부에서 throw**가 발생하면 그 예외가 호출한 함수로 **자동 전달**된다.

```
main() → dividePizza() 안에서 throw 발생
         ↓ 예외 전달
main()의 catch 블록에서 처리
```

```cpp
int dividePizza(int slices, int persons) {
    if (persons == 0)
        throw persons;       // 여기서 던짐
    return slices / persons;
}

int main() {
    try {
        dividePizza(12, 0);  // throw 없음, 함수가 던짐
    }
    catch (int e) {          // 함수에서 전달된 예외를 여기서 처리
        cout << "사람이 " << e << "명입니다." << endl;
    }
}
```

### 함수를 통한 예외 전달 (호출 스택 언와인딩)

- 예외 발생 시 호출 스택을 거슬러 올라가며 catch를 찾음
- 스택 언와인딩 과정에서 **지역 객체의 소멸자가 자동 호출**됨

```cpp
// func3 → func2 → func1 → main 순서로 호출
// func3에서 throw 발생 시:
// 리소스 해제: 3 (func3의 지역 객체)
// 리소스 해제: 2 (func2의 지역 객체)
// 리소스 해제: 1 (func1의 지역 객체)
// → main의 catch에서 처리
```

### 자신이 처리 후 다시 던지기

```cpp
int dividePizza(int slices, int persons) {
    try {
        if (persons == 0)
            throw persons;
    }
    catch (int e) {
        cout << "사람이 " << e << "명(dividePizza)" << endl;
        throw;      // e 없이 throw → 자신이 받은 예외를 다시 위로 전달
    }
}
// main의 catch에서도 동일한 예외를 다시 처리
```

### 함수 헤더에 예외 명시 (의무 아님)

```cpp
int dividePizza(int s, int p) throw ()          // 예외 던지지 않음
int dividePizza(int s, int p) throw (..)        // 예외 던짐 (타입 미지정)
int dividePizza(int s, int p) throw (int)       // int 타입 예외 던짐
int dividePizza(int s, int p) throw (int, double) // 두 타입 모두 던질 수 있음
```

---

## 📚 다중 catch 문장

- 하나의 `try` 블록에 **여러 개의 throw** 가능
- 각 throw 타입에 맞는 **여러 개의 catch** 블록 작성

```cpp
try {
    if (persons < 0)  throw 'negative';  // char 타입 throw
    if (persons == 0) throw persons;      // int 타입 throw
    slices_per_person = pizza_slices / persons;
}
catch (const char* e) {   // char 타입 예외 처리
    cout << "오류: 사람수가 음수입니다." << endl;
}
catch (int e) {           // int 타입 예외 처리
    cout << "오류: 사람이 " << e << "명입니다." << endl;
}
```

### ⚠️ catch 순서 주의: 구체적인 것을 먼저!

```cpp
// ✅ 올바른 순서: 구체적인 예외 → 포괄적인 예외
catch (TooSmallException e) { ... }  // 먼저
catch (...) { ... }                   // 나머지

// ❌ 잘못된 순서
catch (...) { ... }                   // 모든 예외를 가로챔
catch (TooSmallException e) { ... }  // 절대 실행 안 됨!
```

---

## 🏗️ 자신의 예외 클래스 작성

- `throw`는 **클래스 객체**도 던질 수 있다
- 예외에 관한 **다양한 정보를 멤버변수에 담아** 전달 가능

```cpp
// 예외 클래스 정의
class NoPersonException {
public:
    NoPersonException() {}
    NoPersonException(int p) { persons = p; }
    int get_persons() { return persons; }
private:
    int persons;
};

// 사용
try {
    if (persons <= 0)
        throw NoPersonException(persons);  // 객체를 던짐
}
catch (NoPersonException e) {
    cout << "오류: 사람이 " << e.get_persons() << "명 입니다." << endl;
}
```

> **장점**: 멤버변수에 오류 코드, 메시지, 발생 위치 등 다양한 정보 저장 가능

### 표준 라이브러리 예외 클래스

```cpp
#include <stdexcept>

throw runtime_error("오류 메시지");  // 런타임 오류

catch (std::exception& e) {   // 대부분의 표준 예외를 받을 수 있음
    cout << e.what();          // 오류 메시지 출력
}
```

---

## 🧬 상속 관계의 예외 클래스

```cpp
class ParentException {
public:
    void display() { cout << "ParentException" << endl; }
};

class ChildException : public ParentException {
public:
    void display() { cout << "ChildException" << endl; }
};
```

### 핵심 규칙

|상황|결과|
|---|---|
|`catch(ParentException)`이 먼저|**자식 예외도 부모에 걸림** (자식 catch 실행 안 됨)|
|`catch(ChildException)`이 먼저|자식은 자식에, 부모는 부모에 정확히 걸림|

```cpp
// ❌ 잘못된 순서: 자식 예외도 부모에 걸려 버림
catch (ParentException& e) { e.display(); }  // ChildException도 여기 걸림
catch (ChildException& e) { e.display(); }   // 절대 실행 안 됨

// ✅ 올바른 순서: 구체적(자식)을 먼저
catch (ChildException& e) { e.display(); }   // 자식만 처리
catch (ParentException& e) { e.display(); }  // 나머지 처리
```

---

## ⚠️ 핵심 정리

### try-catch-throw 3줄 요약

|키워드|역할|
|---|---|
|`try`|예외가 발생할 수 있는 코드 블록|
|`throw`|예외를 던짐 (타입 자유, 값/객체 모두 가능)|
|`catch`|던진 타입과 일치하는 예외를 받아 처리|

### 자주 하는 실수

> ⚠️ try 블록 **밖**에 예외 발생 시 실행되면 안 되는 코드를 두면 안 된다 ⚠️ catch 타입과 throw 타입이 **정확히 일치**해야 처리된다 ⚠️ 다중 catch에서 **구체적인 예외를 먼저**, `catch(...)` 는 맨 마지막에 ⚠️ 상속 관계 예외 클래스에서 **자식(구체적인 것) catch를 먼저** 작성해야 함 ⚠️ 예외 전달 시 스택 언와인딩으로 **지역 객체 소멸자는 자동 호출**됨

### 예외 처리 흐름 요약

```
try 블록 실행
  ↓ 예외 없음 → 정상 종료
  ↓ 예외 발생 (throw)
    → throw 이후 try 블록 코드 실행 안 됨
    → 타입 일치 catch 블록 실행
    → 해당 함수에 catch 없으면 → 호출한 함수로 전달 (스택 거슬러 올라감)
    → 끝까지 catch 없으면 → 프로그램 비정상 종료
```