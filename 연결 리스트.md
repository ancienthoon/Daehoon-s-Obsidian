# 📘 자료구조 — 연결 리스트 (Linked List)

---

## 🔖 목차

1. [[#개념 및 배경]]
2. [[#배열 vs 연결 리스트]]
3. [[#단순 연결 리스트 Singly Linked List]]
4. [[#이중 연결 리스트 Doubly Linked List]]
5. [[#원형 연결 리스트 Circular Linked List]]
6. [[#주요 연산 구현]]
7. [[#STL list 사용법]]
8. [[#시간 복잡도 정리]]
9. [[#핵심 정리]]

---

## 📌 개념 및 배경

> **연결 리스트**: 각 데이터(노드)가 **다음 노드의 주소(포인터)** 를 가지고 있어 체인처럼 연결된 자료구조

```
[데이터|next] → [데이터|next] → [데이터|next] → nullptr
```

- 배열과 달리 **메모리 상에 연속적으로 저장되지 않음**
- 노드가 흩어져 있어도 포인터로 연결해 순서를 유지
- **동적 크기** — 실행 중에 노드 추가/삭제 자유로움

### 노드의 구조

```cpp
struct Node {
    int data;       // 데이터 (페이로드)
    Node* next;     // 다음 노드의 주소
};
```

---

## 📌 배열 vs 연결 리스트

|항목|배열|연결 리스트|
|---|---|---|
|메모리 구조|**연속** 할당|**비연속** 할당|
|크기|고정 (정적)|유동 (동적)|
|임의 접근|✅ O(1) — `arr[i]`|❌ O(n) — 처음부터 순회|
|삽입/삭제 (중간)|❌ O(n) — 뒤 원소 이동|✅ O(1) — 포인터 변경만|
|삽입/삭제 (앞/뒤)|❌ O(n) / O(1)|✅ O(1)|
|메모리 효율|높음|낮음 (포인터 공간 추가)|
|캐시 효율|✅ 높음|❌ 낮음|

### 선택 기준

```
삽입/삭제가 잦고 크기 변동이 큰 경우  → 연결 리스트
임의 접근(인덱스)이 빈번한 경우       → 배열
```

---

## 📌 단순 연결 리스트 Singly Linked List

> 각 노드가 **다음 노드만** 가리키는 가장 기본적인 형태

```
head
 ↓
[1|→] → [2|→] → [3|→] → nullptr
```

### 노드 구조

```cpp
struct Node {
    int data;
    Node* next;
    Node(int val) : data(val), next(nullptr) {}
};
```

### 핵심 연산

#### 맨 앞에 삽입 (prepend)

```cpp
void prepend(Node*& head, int val) {
    Node* newNode = new Node(val);
    newNode->next = head;   // 새 노드가 기존 head를 가리킴
    head = newNode;         // head를 새 노드로 갱신
}
```

```
삽입 전: head → [2] → [3] → nullptr
삽입 후: head → [1] → [2] → [3] → nullptr
```

#### 맨 뒤에 삽입 (append)

```cpp
void append(Node*& head, int val) {
    Node* newNode = new Node(val);
    if (!head) { head = newNode; return; }
    Node* cur = head;
    while (cur->next) cur = cur->next;  // 끝까지 이동
    cur->next = newNode;
}
```

#### 중간 삽입 (after 노드 뒤에)

```cpp
void insertAfter(Node* prev, int val) {
    if (!prev) return;
    Node* newNode = new Node(val);
    newNode->next = prev->next;  // ① 새 노드가 prev의 다음을 가리킴
    prev->next = newNode;        // ② prev가 새 노드를 가리킴
}
```

```
① 전: prev → [A] → [C]
          ↗
       newNode[B]

② 후: prev → [A] → [B] → [C]
```

> ⚠️ 순서 중요: ①번 먼저 해야 [C] 주소를 잃지 않음

#### 삭제 (특정 값 삭제)

```cpp
void remove(Node*& head, int val) {
    if (!head) return;
    // 삭제 노드가 head인 경우
    if (head->data == val) {
        Node* tmp = head;
        head = head->next;
        delete tmp;
        return;
    }
    // 중간/끝 노드 삭제
    Node* cur = head;
    while (cur->next && cur->next->data != val)
        cur = cur->next;
    if (cur->next) {
        Node* tmp = cur->next;
        cur->next = tmp->next;  // 이전 노드가 삭제 노드 다음을 가리킴
        delete tmp;
    }
}
```

```
삭제 전: [1] → [2] → [3] → nullptr
삭제 후: [1] → [3] → nullptr  (2 삭제)
              ↑ 포인터만 변경, O(1)
```

#### 탐색

```cpp
Node* search(Node* head, int val) {
    Node* cur = head;
    while (cur) {
        if (cur->data == val) return cur;
        cur = cur->next;
    }
    return nullptr;  // 없음
}
```

#### 전체 출력

```cpp
void print(Node* head) {
    Node* cur = head;
    while (cur) {
        cout << cur->data;
        if (cur->next) cout << " → ";
        cur = cur->next;
    }
    cout << " → nullptr" << endl;
}
```

---

## 📌 이중 연결 리스트 Doubly Linked List

> 각 노드가 **이전 노드와 다음 노드** 양쪽을 모두 가리키는 형태

```
nullptr ← [1|←|→] ↔ [2|←|→] ↔ [3|←|→] → nullptr
           ↑head                   ↑tail
```

### 노드 구조

```cpp
struct Node {
    int data;
    Node* prev;   // 이전 노드
    Node* next;   // 다음 노드
    Node(int val) : data(val), prev(nullptr), next(nullptr) {}
};
```

### 단순 vs 이중 비교

|항목|단순 연결 리스트|이중 연결 리스트|
|---|---|---|
|이전 노드 접근|❌ 처음부터 재탐색|✅ O(1) — `prev` 포인터|
|역방향 순회|❌ 불가|✅ 가능|
|노드당 메모리|포인터 1개|포인터 2개|
|삽입/삭제 구현|단순|포인터 4개 갱신 필요|

### 삽입 (노드 cur 앞에)

```cpp
void insertBefore(Node*& head, Node* cur, int val) {
    if (!cur) return;
    Node* newNode = new Node(val);
    newNode->next = cur;
    newNode->prev = cur->prev;
    if (cur->prev) cur->prev->next = newNode;
    else           head = newNode;  // cur이 head였을 경우
    cur->prev = newNode;
}
```

```
삽입 전: [A] ↔ [C]
삽입 후: [A] ↔ [B] ↔ [C]
             ↑ B의 prev=A, next=C
               A의 next=B, C의 prev=B
```

### 삭제

```cpp
void remove(Node*& head, Node* target) {
    if (!target) return;
    if (target->prev) target->prev->next = target->next;
    else              head = target->next;  // head 삭제
    if (target->next) target->next->prev = target->prev;
    delete target;
}
```

---

## 📌 원형 연결 리스트 Circular Linked List

> 마지막 노드의 `next`가 **다시 첫 번째 노드**를 가리키는 형태

```
head
 ↓
[1] → [2] → [3]
 ↑           ↓
 └───────────┘
```

### 특징

```
- 끝이 없음 (무한 순환 가능)
- 어느 노드에서든 전체 순회 가능
- 큐, 버퍼, 라운드 로빈 스케줄링에 활용
```

### 삽입 / 순회 시 주의

```cpp
// 원형 리스트 순회 — nullptr이 아닌 head로 종료 조건 설정
void print(Node* head) {
    if (!head) return;
    Node* cur = head;
    do {
        cout << cur->data << " → ";
        cur = cur->next;
    } while (cur != head);  // head로 돌아오면 종료
    cout << "(head)" << endl;
}
```

> ⚠️ while(cur) 조건 쓰면 무한 루프 → **반드시 `cur != head`** 로 종료

---

## 📌 주요 연산 구현

### 리스트 뒤집기 (Reverse)

```cpp
Node* reverse(Node* head) {
    Node *prev = nullptr, *cur = head, *next = nullptr;
    while (cur) {
        next = cur->next;   // ① 다음 저장
        cur->next = prev;   // ② 방향 뒤집기
        prev = cur;         // ③ prev 전진
        cur = next;         // ④ cur 전진
    }
    return prev;  // 새 head
}
```

```
진행 과정:
nullptr ← [1] → [2] → [3]
           cur

nullptr ← [1] ← [2] → [3]
                 cur

nullptr ← [1] ← [2] ← [3]
                        cur → (prev가 새 head)
```

### 중간 노드 찾기 (Floyd의 토끼와 거북이)

```cpp
Node* findMiddle(Node* head) {
    Node* slow = head;
    Node* fast = head;
    while (fast && fast->next) {
        slow = slow->next;        // 1칸씩
        fast = fast->next->next;  // 2칸씩
    }
    return slow;  // fast가 끝에 도달할 때 slow는 중간
}
```

```
[1] → [2] → [3] → [4] → [5]
 s           s           s
 f                  f
             slow = 3 (중간) ✅
```

### 사이클 감지 (Floyd 알고리즘)

```cpp
bool hasCycle(Node* head) {
    Node* slow = head;
    Node* fast = head;
    while (fast && fast->next) {
        slow = slow->next;
        fast = fast->next->next;
        if (slow == fast) return true;  // 만나면 사이클
    }
    return false;
}
```

### 메모리 해제 (전체 삭제)

```cpp
void clear(Node*& head) {
    while (head) {
        Node* tmp = head;
        head = head->next;
        delete tmp;
    }
}
```

> ⚠️ new로 할당한 노드는 반드시 `delete` 로 해제 — 메모리 누수 방지

---

## 📌 STL list 사용법

> C++ STL의 `list`는 **이중 연결 리스트**로 구현되어 있음

```cpp
#include <list>
using namespace std;

list<int> lst;

// 삽입
lst.push_back(3);    // 뒤에 추가
lst.push_front(1);   // 앞에 추가
lst.insert(++lst.begin(), 2);  // 이터레이터 위치에 삽입

// 삭제
lst.pop_back();      // 뒤 삭제
lst.pop_front();     // 앞 삭제
lst.remove(2);       // 값 2인 노드 모두 삭제

// 탐색 / 순회
for (int val : lst)
    cout << val << " ";

// 크기
cout << lst.size();
cout << lst.empty();  // 비어있는지

// 정렬 / 뒤집기
lst.sort();
lst.reverse();

// 이터레이터로 삽입/삭제
auto it = lst.begin();
advance(it, 2);          // 2칸 이동
lst.insert(it, 99);      // 해당 위치에 삽입
lst.erase(it);           // 해당 위치 삭제
```

### STL list vs vector

|항목|`list`|`vector`|
|---|---|---|
|내부 구조|이중 연결 리스트|동적 배열|
|임의 접근|❌ O(n)|✅ O(1)|
|앞/중간 삽입|✅ O(1)|❌ O(n)|
|메모리|포인터 오버헤드|연속, 캐시 친화적|
|이터레이터 무효화|삭제한 것만|삽입/삭제 시 전체 무효화|

---

## 📌 시간 복잡도 정리

|연산|단순 연결 리스트|이중 연결 리스트|배열|
|---|---|---|---|
|맨 앞 삽입/삭제|O(1)|O(1)|O(n)|
|맨 뒤 삽입|O(n)*|O(1)†|O(1)|
|맨 뒤 삭제|O(n)|O(1)†|O(1)|
|중간 삽입/삭제|O(1)‡|O(1)‡|O(n)|
|탐색 (값 기준)|O(n)|O(n)|O(n)|
|임의 접근 (인덱스)|O(n)|O(n)|O(1)|

> * tail 포인터 없는 경우 O(n), tail 포인터 있으면 O(1) † tail 포인터 있는 경우 ‡ 해당 위치의 포인터(이터레이터)를 이미 알고 있는 경우

---

## 🗂️ 핵심 정리

### 연결 리스트 3종 비교

|종류|포인터|역방향|특징|
|---|---|---|---|
|**단순**|next 1개|❌|구현 단순, 메모리 절약|
|**이중**|prev + next|✅|삭제 O(1), 양방향 순회|
|**원형**|next (head 연결)|❌/✅|사이클 구조, 버퍼 활용|

### 포인터 조작 황금 규칙

```
삽입 시: 새 노드의 포인터 먼저 설정 → 기존 포인터 변경
삭제 시: 삭제할 노드 주변 포인터 연결 → delete

순서를 반대로 하면 주소를 잃어버려 메모리 누수 또는 크래시 발생!
```

### ⚠️ 최다 실수 포인트

|실수|올바른 내용|
|---|---|
|삽입 시 포인터 순서 역전|새 노드 포인터 **먼저** 설정 후 기존 포인터 변경|
|원형 리스트 순회 조건|`while(cur != nullptr)` ❌ → `while(cur != head)` ✅|
|delete 없이 포인터만 변경|반드시 `delete` → 메모리 누수 방지|
|head 삭제 후 head 갱신 안 함|`head = head->next` 반드시 수행|
|역방향 탐색 시 단순 리스트 사용|역방향 필요하면 이중 연결 리스트 사용|

---

> 📅 작성일: 2026-05-18 🏫 과목: C++ / 자료구조 📖 단원: 연결 리스트 (Linked List)

#자료구조 #연결리스트 #LinkedList #포인터 #cpp