---

title: "1부. 강의 소개 & 문제 제기" course: 딥러닝기초 week: 1 tags: [딥러닝, 딥러닝기초, 손실함수, 경사하강법, 활성화함수, 2학기전공과목] 관련교재:

- "모두의 딥러닝 (2장)"
- "처음 배우는 딥러닝 수학 (1~2장)"

---

#딥러닝기초

# 1부. 강의 소개 & 문제 제기

> [!quote] 이번 강의의 핵심 문장 **Deep Learning is to minimize the Loss function.** (딥러닝 = 손실함수를 최소화하는 것)

---

## 1. 첫 번째 질문 — 함수 그래프에서 한 점을 고른다면?

> [!question] 교수님의 질문
> 
> 1. 어느 지점이 좋은 곳인가?
> 2. "좋다"의 기준은 무엇인가?
> 3. 함수의 그래프를 볼 수 없다면 어떻게 찾을 것인가?

- 이 질문이 **딥러닝이 푸는 문제의 원형(原形)**이다.
- 딥러닝의 목표 = "가장 좋은 지점(오차가 최소인 지점)"을 찾는 것.
- 수학적 힌트: **닫힌 구간(closed interval) [c, d]에서는 최솟값이 반드시 존재**한다 (최대·최솟값 정리).
- 진짜 문제는 → 그래프를 눈으로 볼 수 없을 때, 최소 지점을 어떻게 찾는가?

> [!tip] 비유 눈을 가리고 산속에서 가장 낮은 골짜기를 찾아야 한다면? 전체 지형은 못 보지만, **발밑의 기울기**는 느낄 수 있다. → 기울기를 느끼며 한 걸음씩 낮은 쪽으로 이동 = 훗날 배울 **[[경사하강법]]**의 원리
> 
> ➡️ 이 질문의 답은 → [[04_최소값찾기_경사하강법#Local Approach 경사하강법|5부. 경사하강법]]에서 완성됨

---

## 2. 딥러닝 맛보기 — 공부시간으로 시험점수 예측하기

### 이번 강의의 두 가지 목표

1. 딥러닝은 **손실함수(Loss function)**를 최소화하는 것이다.
2. 딥러닝 프로그래밍은 생각보다 간단하다.

### 두 가지 문제 유형 (미리보기)

|문제|유형|
|---|---|
|7시간 공부하면 기말시험 몇 점?|**회귀 (Regression)**|
|폐암 환자의 생존 가능 여부는?|**분류 (Classification)**|

---

## 3. 실습 코드 뜯어보기

```python
import numpy as np
import matplotlib.pyplot as plt
from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import Dense

# ① 데이터 준비
x = np.array([2, 4, 6, 8])       # 공부 시간
y = np.array([81, 93, 91, 97])   # 시험 점수

# ② 모델 설계
model = Sequential()
model.add(Dense(1, input_dim=1, activation='linear'))

# ③ 학습 방법 설정
model.compile(optimizer='sgd', loss='mse')

# ④ 학습 실행
model.fit(x, y, epochs=200)

# ⑤ 예측
hour = 7
prediction = model.predict(np.array([hour]))
print("%d 시간을 공부할 경우의 예상 점수는 %.02f 점 입니다." % (hour, prediction))
```

### 코드 한 줄씩 정리

|코드|의미|처음 등장 개념|
|---|---|---|
|`Sequential()`|층을 순서대로 쌓는 딥러닝 모델의 뼈대|—|
|`Dense(1, input_dim=1, activation='linear')`|입력 1개 → 출력 1개, 가장 단순한 층|**활성화 함수**|
|`optimizer='sgd'`|오차를 줄여나가는 방법|**경사하강법**|
|`loss='mse'`|오차를 측정하는 방법 (평균제곱오차)|**손실함수(비용함수)**|
|`model.fit(x, y, epochs=200)`|오차 최소화 과정을 200번 반복|**학습(training)**|

> [!important] 3대 핵심 개념 최초 등장
> 
> - **손실함수(Loss function)**: "내 예측이 정답과 얼마나 다른가"를 숫자로 나타낸 것
> - **경사하강법(Gradient Descent)**: 그 숫자(오차)를 줄이는 방향으로 조금씩 이동하는 방법
> - **활성화 함수(Activation function)**: 층의 출력을 어떻게 변형해 다음 층에 넘길지 정하는 함수

> [!success] 한 문장 요약 **딥러닝 학습 = "손실함수 값이 최소가 되는 지점을, 경사하강법으로 찾아가는 것"**

📌 **교재 연결**: 이 코드는 **모두의 딥러닝 2장**과 정확히 대응. `mse`와 `sgd`의 수학적 원리는 **처음 배우는 딥러닝 수학 3~4장** 참고. → 자세한 원리는 [[03_비용함수와_최소제곱법|4부]], [[04_최소값찾기_경사하강법|5부]]에서 이어짐.

---

## 4. 다음 화두로의 연결

교과목 개요 설명 후, 새로운 질문이 던져진다:

> [!question] "원은 어떤 모양인가?"

→ 이 질문은 [[02_원의_모양_사고방식전환|2부. 사고방식의 전환]]에서 다룸.

---

## 관련 노트

- [[02_원의_모양_사고방식전환]]
- [[03_비용함수와_최소제곱법]]
- [[04_최소값찾기_경사하강법]]
- [[딥러닝수학 함수]]