# 📘 C++ 10장 — 프렌드와 연산자 중복

---

## 🔖 목차

1. [[#프렌드 함수 friend function]]
2. [[#프렌드 클래스]]
3. [[#연산자 중복 Operator Overloading]]
4. [[#연산자 중복 구현 방법 비교]]
5. [[#피연산자 타입이 다른 경우 스칼라 곱]]
6. [[#연산자별 중복 정리]]
7. [[#핵심 정리]]

---

## 📌 프렌드 함수 friend function

> **개념**: 멤버 함수가 아닌 **일반 함수**이지만, 클래스의 `private` 멤버에 직접 접근할 수 있는 특수 함수

```
일반 함수  ──────────────────────────────────▶  private 접근 불가
friend 함수 ──── 클래스가 "너는 내 친구" 선언 ──▶  private 접근 가능 ✅
```

### ⚠️ 주의사항

- `friend`는 **정보은닉 원칙에 위배** → 꼭 필요한 곳에만 사용
- 남발하면 캡슐화가 무너짐

### 선언 방법

```cpp
class MyClass {
    friend void sub();   // ← 클래스 안에 원형만 선언 (friend 키워드 붙임)
    // ...
};

// 본체는 클래스 외부에서 일반 함수로 정의
void sub() {
    // MyClass의 private 멤버 모두 접근 가능
}
```

|항목|내용|
|---|---|
|원형 위치|클래스 **내부** (public/private 어디든 동일)|
|본체 위치|클래스 **외부**|
|`this` 포인터|❌ 없음 (멤버 함수가 아니므로)|
|private 접근|✅ 가능|

### 예제 — Company 클래스

```cpp
class Company {
private:
    int sales, profit;
    friend void sub(Company& c);   // friend 선언
public:
    Company() : sales(0), profit(0) {}
};

// 일반 함수 — Company의 private에 접근 가능
void sub(Company& c) {
    cout << c.profit << endl;   // ✅ private 접근
}

int main() {
    Company c1;
    sub(c1);   // 출력: 0
}
```

> **참고**: 객체를 매개변수로 받을 때는 **참조자(`&`)** 로 받아야 복사 생성자 호출을 피할 수 있음

### 프렌드 함수의 주요 용도

```
1. 두 객체를 비교할 때
   if( equals(obj1, obj2) )  ← 자연스러운 표현 가능

2. 연산자 중복 구현 시 (피연산자가 2개인 경우)
   operator+(v1, v2)
```

### 예제 — equals (Date 비교)

```cpp
class Date {
private:
    int year, month, day;
    friend bool equals(Date d1, Date d2);
public:
    Date(int y, int m, int d) : year(y), month(m), day(d) {}
};

bool equals(Date d1, Date d2) {
    return (d1.year == d2.year) &&
           (d1.month == d2.month) &&
           (d1.day == d2.day);
}
```

---

## 📌 프렌드 클래스

> **개념**: 클래스 전체를 `friend`로 선언 → 해당 클래스의 모든 멤버 함수가 접근 권한을 가짐

```cpp
class Employee {
private:
    int salary;
    friend class Manager;   // Manager 클래스 전체가 친구
};

class Manager {
public:
    void check(Employee& e) {
        cout << e.salary;   // ✅ Employee의 private 접근 가능
    }
};
```

---

## 📌 연산자 중복 Operator Overloading

> **개념**: 개발자가 만든 클래스 객체에 대해 `+`, `-`, `==` 같은 연산자가 동작하도록 직접 정의하는 것

### 왜 필요한가?

```cpp
// 함수 사용 — 직관적이지 않음
sum = add(x, add(y, z));

// 연산자 사용 — 훨씬 자연스러움
sum = x + y + z;
```

- `string` 클래스의 `+` 연산자도 연산자 중복으로 구현된 것
- **개발자가 만든 클래스**의 연산자는 기본으로 제공되지 않음 → 직접 정의해야 함

### 기본 문법

```cpp
반환형 operator연산자(매개변수 목록) {
    // 연산 수행
}
```

### 연산자 → 함수 이름 변환표

| 연산자  | 중복 함수 이름       |
| ---- | -------------- |
| `+`  | `operator+()`  |
| `-`  | `operator-()`  |
| `*`  | `operator*()`  |
| `/`  | `operator/()`  |
| `==` | `operator==()` |
| `!=` | `operator!=()` |
| `[]` | `operator[]()` |
| `=`  | `operator=()`  |

---

## 📌 연산자 중복 구현 방법 비교

### 두 가지 구현 방식

|구분|멤버 함수|friend(일반) 함수|
|---|---|---|
|매개변수 수|**1개** (나머지 하나는 `this`)|**2개**|
|해석 방식|`v1 + v2` → `v1.operator+(v2)`|`v1 + v2` → `operator+(v1, v2)`|
|우선순위|둘 다 있으면 멤버 함수 우선|—|

### 멤버 함수로 구현

```cpp
class Vector {
    double x, y;
public:
    Vector(double x, double y) : x(x), y(y) {}

    Vector operator+(Vector& a) {        // 매개변수 1개
        Vector v(0.0, 0.0);
        v.x = this->x + a.x;
        v.y = this->y + a.y;
        return v;
    }
    void display() { cout << "(" << x << ", " << y << ")" << endl; }
};

// v3 = v1 + v2;  →  v3 = v1.operator+(v2);
```

### friend 함수로 구현

```cpp
class Vector {
    double x, y;
public:
    Vector(double x, double y) : x(x), y(y) {}
    friend Vector operator+(const Vector& v1, const Vector& v2);  // 매개변수 2개
    void display() { cout << "(" << x << ", " << y << ")" << endl; }
};

Vector operator+(const Vector& v1, const Vector& v2) {
    Vector v(0.0, 0.0);
    v.x = v1.x + v2.x;
    v.y = v1.y + v2.y;
    return v;
}

// v3 = v1 + v2;  →  v3 = operator+(v1, v2);
```

> **`const Vector&` 사용 이유**: 매개변수가 함수 내부에서 변경되지 않도록 보호 + 참조자로 복사 비용 절약

---

## 📌 피연산자 타입이 다른 경우 (스칼라 곱)

> 벡터 × 실수 처럼 **피연산자 타입이 서로 다른 경우**

### 스칼라 곱 (scalar product)

- 벡터 `(x, y)`에 스칼라 `α`를 곱하면 → `(αx, αy)`
- 교환 법칙이 성립해야 함: `v * 2.0` 과 `2.0 * v` 둘 다 가능해야 함

### ⚠️ 멤버 함수로 구현이 불가능한 이유

```cpp
v * 2.0  →  v.operator*(2.0)    // ✅ 가능
2.0 * v  →  (2.0).operator*(v)  // ❌ double은 클래스가 아님!
```

→ **반드시 friend 함수(일반 함수)로 2개 작성**

### 구현 코드

```cpp
class Vector {
    friend Vector operator*(const Vector& v, double alpha);   // v * 2.0
    friend Vector operator*(double alpha, const Vector& v);   // 2.0 * v
private:
    double x, y;
public:
    Vector(double x=0.0, double y=0.0) : x(x), y(y) {}
    void display() { cout << "(" << x << ", " << y << ")" << endl; }
};

Vector operator*(const Vector& v, double alpha) {
    return Vector(alpha * v.x, alpha * v.y);
}
Vector operator*(double alpha, const Vector& v) {
    return Vector(alpha * v.x, alpha * v.y);
}

int main() {
    Vector v(1.0, 1.0);
    Vector w = v * 2.0;   // (2, 2)
    Vector z = 2.0 * v;   // (2, 2)
}
```

---

## 📌 연산자별 중복 정리

### == / != 연산자

```cpp
// friend 함수로 구현
bool operator==(const Vector& v1, const Vector& v2) {
    return (v1.x == v2.x) && (v1.y == v2.y);
}
bool operator!=(const Vector& v1, const Vector& v2) {
    return !(v1 == v2);   // 중복된 == 재활용
}

// 출력 시 0/1 대신 true/false로 출력하려면
cout.setf(cout.boolalpha);
cout << (v1 == v2) << endl;   // true 또는 false
```

### 멤버 함수로만 중복 가능한 연산자

|연산자|설명|
|---|---|
|`=`|대입 연산자 (복사생성자 아님!)|
|`()`|함수 호출 연산자|
|`[]`|배열 원소 참조 연산자|
|`->`|멤버 참조 연산자|

### 중복이 불가능한 연산자

|연산자|설명|
|---|---|
|`::`|범위 지정 연산자|
|`.`|멤버 선택 연산자|
|`.*`|멤버 포인터 연산자|
|`?:`|조건 연산자|

---

## 🗂️ 핵심 정리

### friend vs 멤버 함수 선택 기준

```
피연산자가 같은 타입 (Vector + Vector)
  ├── 멤버 함수:  Vector operator+(const Vector&)        매개변수 1개
  └── friend 함수: operator+(const Vector&, const Vector&) 매개변수 2개

피연산자가 다른 타입 (Vector * double, double * Vector)
  └── friend 함수만 가능 → 2개 작성 필수
```

### 연산자 중복 전체 요약표

|연산자|구현 방식|매개변수 수|특이사항|
|---|---|---|---|
|`+`, `-`|멤버 or friend|1개 or 2개|둘 다 가능|
|`*` (스칼라곱)|**friend만**|2개 × 2함수|교환법칙 때문|
|`==`, `!=`|멤버 or friend|1개 or 2개|`!=`는 `==` 재활용|
|`=`, `[]`, `()`, `->`|**멤버만**|—|friend 불가|

### ⚠️ 최다 실수 포인트

|실수 유형|올바른 내용|
|---|---|
|스칼라 곱을 멤버 함수로 구현|❌ → `2.0 * v` 처리 불가, friend 함수 사용|
|스칼라 곱 함수 1개만 작성|❌ → `v*2.0`과 `2.0*v` 처리 함수 **2개** 필요|
|friend 함수에 `this` 사용|❌ → friend는 일반 함수, `this` 없음|
|`=` 연산자를 friend로 구현|❌ → 반드시 멤버 함수|

---

> 📅 작성일: 2026-05-14 🏫 과목: C++ / 자료구조 📖 단원: 10장 — 프렌드와 연산자 중복

#cpp #연산자중복 #friend #operator_overloading