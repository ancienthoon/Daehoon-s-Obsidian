# 📘 제10장 프렌드와 연산자 중복

#cpp #OOP #연산자중복 #프렌드

---

## 📋 목차

- [[#🤝 프렌드 함수 (Friend Function)]]
- [[#⚙️ 연산자 중복 (Operator Overloading)]]
- [[#🔢 피연산자 타입이 다른 연산]]
- [[#🖨️ << >> 연산자 중복]]
- [[#📋 = 대입 연산자 중복]]
- [[#➕➖ 증가/감소 연산자 중복]]
- [[#📦 [] 인덱스 연산자 중복]]
- [[#📞 () 함수 호출 연산자 중복]]
- [[#⚠️ 핵심 정리]]

---

## 🤝 프렌드 함수 (Friend Function)

### 개념

|구분|내용|
|---|---|
|정의|멤버함수가 **아닌** 일반함수이지만, 클래스의 private 멤버에 접근 가능한 특수 함수|
|선언 위치|클래스 **내부**에 원형만 선언 (`friend` 키워드 사용)|
|구현 위치|클래스 **외부**에서 일반 함수처럼 정의|
|주의사항|정보은닉에 위배 → **꼭 필요한 경우에만** 사용|

```cpp
class MyClass {
    friend void sub();  // ← 프렌드 함수 원형 선언
    int data;
};

void sub() {           // ← 외부에서 본체 정의
    // MyClass의 private 멤버 접근 가능
}
```

### 프렌드 클래스

```cpp
class Employee {
    int salary;
    friend class Manager;  // Manager는 Employee의 private 접근 가능
};
```

### 프렌드 함수 활용 예시 (두 객체 비교)

```cpp
class Date {
private:
    int year, month, day;
    friend bool equals(Date d1, Date d2);
public:
    Date(int y, int m, int d) : year(y), month(m), day(d) {}
};

bool equals(Date d1, Date d2) {
    return (d1.year == d2.year) && (d1.month == d2.month) && (d1.day == d2.day);
}

// 사용: if (equals(d1, d2)) { ... }
```

> ⚠️ **참고**: 함수에서 객체를 인자로 받을 때는 **참조자(&)** 로 받아야 복사 생성자 호출을 막을 수 있다.

---

## ⚙️ 연산자 중복 (Operator Overloading)

### 개념

- 개발자가 만든 클래스에 `+`, `-`, `*` 등 연산자를 직접 정의하는 기능
- 함수 이름 = `operator연산자` (예: `operator+`, `operator-`)
- **멤버함수** 또는 **friend(일반) 함수** 두 가지 방법으로 구현

### 구현 방법 비교

|방법|매개변수 수|함수 호출 해석|특징|
|---|---|---|---|
|멤버함수|1개|`v1.operator+(v2)`|왼쪽 피연산자가 호출 객체|
|friend(일반) 함수|2개|`operator+(v1, v2)`|두 피연산자 모두 인자|

> ⚠️ **같은 이름의 중복 함수**로 멤버함수와 friend 함수가 **동시에** 존재하면 → **멤버함수 우선**

### 멤버함수로 구현 예제

```cpp
class Vector {
    double x, y;
public:
    Vector(double x, double y) : x(x), y(y) {}
    
    Vector operator+(Vector& a) {       // 매개변수 1개
        return Vector(x + a.x, y + a.y);
    }
    // v3 = v1 + v2; → v1.operator+(v2) 로 해석
};
```

### friend 함수로 구현 예제

```cpp
class Vector {
    double x, y;
public:
    friend Vector operator+(const Vector& v1, const Vector& v2);
};

Vector operator+(const Vector& v1, const Vector& v2) {  // 매개변수 2개
    return Vector(v1.x + v2.x, v1.y + v2.y);
}
// v3 = v1 + v2; → operator+(v1, v2) 로 해석
```

### 멤버함수로만 구현 가능한 연산자

|연산자|설명|
|---|---|
|`=`|대입 연산자 (복사생성자 아님)|
|`()`|함수 호출 연산자|
|`[]`|배열 원소 접근 연산자|
|`->`|멤버 참조 연산자|

### 중복 불가능한 연산자

|연산자|설명|
|---|---|
|`::`|범위 지정 연산자|
|`.`|멤버 선택 연산자|
|`.*`|멤버 포인터 연산자|
|`?:`|조건 연산자|

---

## 🔢 피연산자 타입이 다른 연산

### 벡터 스칼라 곱 (scalar product)

- 벡터 `(x, y)` × 스칼라 `α` = `(αx, αy)`
- `2 * v` 와 `v * 2` 모두 가능해야 함 → **교환법칙** 지원 필요

### ⚠️ 멤버함수로는 구현 불가능한 이유

```cpp
// v * 2.0  →  v.operator*(2.0)  ✅ 가능
// 2.0 * v  →  (2.0).operator*(v)  ❌ 불가능 (double은 클래스가 아님)
```

### friend 함수로 구현 (2개 필요)

```cpp
class Vector {
    friend Vector operator*(const Vector& v, double alpha);
    friend Vector operator*(double alpha, const Vector& v);
};

Vector operator*(const Vector& v, double alpha) {
    return Vector(alpha * v.x, alpha * v.y);
}
Vector operator*(double alpha, const Vector& v) {
    return Vector(alpha * v.x, alpha * v.y);
}
```

---

## 🖨️ `<<` `>>` 연산자 중복

### `<<` 출력 연산자 중복

|항목|내용|
|---|---|
|`cout`|`ostream` 클래스의 객체|
|구현 방식|**반드시 friend(일반) 함수** (ostream 클래스 수정 불가)|
|반환형|`ostream&` (참조자 반환 필수)|
|이유|`cout << a << b;` 처럼 연쇄 호출 가능하려면 `cout` 자체를 반환해야 함|

```
cout << a << b;
→ ((cout << a) << b)
→ (cout << a) 실행 후 cout 반환 → cout << b 실행
```

```cpp
class Vector {
    friend ostream& operator<<(ostream& os, const Vector& v);
    // 두 번째 매개변수: const (출력만 하므로 변경 불필요)
};

ostream& operator<<(ostream& os, const Vector& v) {
    os << "(" << v.x << ", " << v.y << ")" << endl;
    return os;  // cout(os)를 참조자로 반환
}
```

### `>>` 입력 연산자 중복

|항목|내용|
|---|---|
|`cin`|`istream` 클래스의 객체|
|구현 방식|**반드시 friend(일반) 함수**|
|반환형|`istream&`|
|주의|두 번째 매개변수는 **const 불가** (입력값을 저장해야 하므로)|

```cpp
istream& operator>>(istream& in, Vector& v) {
    in >> v.x >> v.y;
    if (!in)             // 입력 오류 처리
        v = Vector(0.0, 0.0);
    return in;
}
```

---

## 📋 `=` 대입 연산자 중복

### 개념

- **멤버함수로만 구현 가능**
- 컴파일러가 자동으로 디폴트 대입연산자 생성 (멤버 대 멤버 단순 복사)
- 포인터(동적 할당) 멤버가 있으면 **반드시 직접 작성**해야 함

### 얕은 대입 문제 (Shallow Copy Problem)

```
Student a("철수", 1), b("순이", 2);
b = a;  // 디폴트 대입: b.name → a.name 같은 주소 가리킴
        // 소멸자 호출 시 같은 메모리 두 번 delete → 오류!
```

> ⚠️ **포인터 멤버 존재 시 반드시 3가지 작성**: 대입연산자 / 소멸자 / 복사생성자

### Vector 클래스 구현 예제

```cpp
Vector& operator=(const Vector& v2) {
    this->x = v2.x;
    this->y = v2.y;
    return *this;  // 호출한 객체(*this) 참조자 반환
}
// v3 = v2 = v1; → v2.operator=(v1) 후 v3.operator=(v2)
```

### Student 클래스 (포인터 멤버) 구현 예제

```cpp
Student& operator=(const Student& s) {
    if (&s != this) {           // 자기 자신 대입 방지
        delete[] name;          // 기존 메모리 해제
        name = new char[strlen(s.name) + 1];
        strcpy(name, s.name);
        number = s.number;
    }
    return *this;
}
```

---

## ➕➖ 증가/감소 연산자 중복

### 전위 vs 후위 구별

|연산자|함수 이름|반환형|반환 내용|
|---|---|---|---|
|`++v` (전위)|`operator++()`|`Vector&`|증가 후 자신(`*this`) 참조자 반환|
|`v++` (후위)|`operator++(int)`|`Vector` (const)|증가 전 값(복사본) 반환|

> ⚠️ `int` 매개변수는 **의미 없음** - 전위/후위를 구별하기 위한 표기일 뿐

```cpp
// 전위 ++v
Vector& operator++() {
    x += 1.0;
    y += 1.0;
    return *this;           // 자신 반환
}

// 후위 v++
const Vector operator++(int) {
    Vector saveObj = *this; // 증가 전 복사본 저장
    x++;
    y++;
    return saveObj;         // 복사본(증가 전 값) 반환
}
```

### C 언어 전위/후위 반환값 복습

```cpp
int a = 10;
b = ++a;       // a=11, b=11  (++a는 변수 a 자체 반환)
b = a++;       // a=11, b=10  (a++는 증가 전 값(상수) 반환)

++a = 10;      // ✅ 가능 (++a는 변수 반환)
a++ = 20;      // ❌ 오류 (a++는 상수 반환)
```

---

## 📦 `[]` 인덱스 연산자 중복

### 개념

- **멤버함수로만 구현 가능**
- `A[3] = 2` → `A.operator[](3)` → `A.data[3]` 의 저장공간 반환
- 메모리 공간 자체를 반환해야 하므로 **참조자로 반환**

```cpp
class MyArray {
    int* data;
    int size;
public:
    int& operator[](int index) {
        assert(0 <= index && index < size);  // 범위 체크
        return data[index];                  // 참조자 반환
    }
};

// 사용:
MyArray a1(10);
a1[0] = 1;  // a1.operator[](0) = 1 → a1.data[0] = 1
```

---

## 📞 `()` 함수 호출 연산자 중복

### 개념

- 객체를 **함수처럼** 사용할 수 있게 해주는 연산자
- `a(50, "white")` → `a.operator()(50, "white")`

```cpp
class Negate {
public:
    int operator()(int value) {
        return -value;
    }
};

Negate neg;
cout << neg(100);  // -100 출력
```

---

## ⚠️ 핵심 정리

### 구현 방법 선택 가이드

|상황|권장 방법|
|---|---|
|일반 산술/비교 (`+`, `-`, `==`)|멤버함수 또는 friend 함수 모두 가능|
|교환법칙 필요 (`2 * v`, `v * 2`)|**반드시 friend(일반) 함수** 2개|
|`=`, `[]`, `()`, `->`|**반드시 멤버함수**|
|`<<`, `>>`|**반드시 friend(일반) 함수**|

### 반환형 정리

|연산자|반환형|이유|
|---|---|---|
|`+`, `-`, `*`|`ClassName` (값)|새로운 객체 생성|
|`=`|`ClassName&` (참조)|`a = b = c` 연쇄 가능|
|`++` 전위|`ClassName&` (참조)|자신 변경 후 반환|
|`++` 후위|`ClassName` (값/const)|증가 전 복사본 반환|
|`[]`|`타입&` (참조)|저장공간 자체 반환|
|`<<`, `>>`|`ostream&` / `istream&`|연쇄 출력/입력 가능|

### 자주 하는 실수

> ⚠️ `<<` 연산자에서 `ostream&`를 반환하지 않으면 `cout << a << b` 불가 ⚠️ 포인터 멤버가 있는데 `=` 연산자를 작성하지 않으면 소멸자에서 이중 `delete` 오류 ⚠️ 스칼라 곱 `2 * v` 형태는 멤버함수로 구현 불가 → friend 함수 필요 ⚠️ `[]`, `=`, `()`, `->` 는 멤버함수로만 가능 — 잊지 말 것!