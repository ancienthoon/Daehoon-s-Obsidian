# 🧩 오픈소스프로젝트 6강 — 알고리즘 문제 모음

---

## 📌 목차

| 번호        | 문제명                   | 핵심 키워드             |
| --------- | --------------------- | ------------------ |
| 1236      | 잔혹한 물벼룩의 세계           | 콜라츠 추측, 반복 횟수      |
| 1237      | The 3n+1 Problem      | 콜라츠, 수열 길이, EOF 처리 |
| 1249/1250 | 수 뒤집기                 | 정수/문자열 역순 변환       |
| 1370/1250 | 수 뒤집기 #2              | 좌우 대칭 판별           |
| 1251      | Binary Conversion     | 이진수 → 십진수          |
| 1252      | IP Address            | 32비트 이진수 → IP      |
| 1253      | IP Address #2         | IP → 32비트 이진수      |
| 1255      | 더블 미역                 | 사각형 겹침 면적          |
| 1233      | 소수 구하기 I              | 소수 판별, 에라토스테네스의 체  |
| 1234      | 소수 구하기 II             | 범위 내 소수 개수         |
| 1235      | 소수 구하기 III            | 에라토스테네스 최적화        |
| 1256      | Goldbach's conjecture | 골드바흐 추측, 소수 쌍      |
| 1257      | Goldbach              | 소수 쌍 개수 세기         |

---

## 🦠 1236 — 잔혹한 물벼룩의 세계 (콜라츠 추측)

### 📋 문제 요약

- 짝수 → `n / 2`, 홀수 → `3*n + 1`
- `1`이 될 때까지의 **횟수** 출력
- ⚠️ **전체 데이터 개수 - 1** 을 출력해야 함 (1이 될 때까지 변환된 횟수)

### 💡 예시

```
입력 22 → 22 11 34 17 52 26 13 40 20 10 5 16 8 4 2 1 → 출력: 15
입력 34 → 34 17 52 26 13 40 20 10 5 16 8 4 2 1     → 출력: 13
```

### 💻 C++ 코드

```cpp
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    int count = 0;
    while (n != 1) {
        if (n % 2 == 0) n /= 2;
        else n = 3 * n + 1;
        count++;
    }
    cout << count << endl;
    return 0;
}
```

---

## 🔢 1237 — The 3n+1 Problem

### 📋 문제 요약

- 1236번과 같은 원리이나 **수열의 총 개수** 출력 (횟수 + 1)
- 두 수 사이 모든 정수에 대해 수열 전개 → **가장 긴 수열의 개수** 출력
- ⚠️ 문제 개수가 미리 주어지지 않음 → **EOF 처리** 필수

### 💡 예시

```
입력 22 → 수열 16개 (22 포함, 1 포함)
입력 34 → 수열 14개
```

### 💻 EOF 처리 방법들 (C++)

```cpp
// 방법 1 — 가장 권장 (C++ 스타일)
while (cin >> ia >> ib) {
    // 처리
}

// 방법 2
while (true) {
    if (!(cin >> ia >> ib)) break;
    // 처리
}

// 방법 3 — C 스타일 (scanf 사용)
while (scanf("%d %d", &ia, &ib) != EOF) {
    // 처리
}

// 방법 4
do {
    if (scanf("%d %d", &ia, &ib) == EOF) break;
    // 처리
} while (true);
```

### 💻 수열 길이 계산 함수 (C++)

```cpp
#include <iostream>
#include <algorithm>
using namespace std;

int collatzLength(long long n) {
    int count = 1;  // 시작값 포함
    while (n != 1) {
        if (n % 2 == 0) n /= 2;
        else n = 3 * n + 1;
        count++;
    }
    return count;
}

int main() {
    int ia, ib;
    while (cin >> ia >> ib) {
        int lo = min(ia, ib), hi = max(ia, ib);
        int maxLen = 0;
        for (int i = lo; i <= hi; i++) {
            maxLen = max(maxLen, collatzLength(i));
        }
        cout << ia << " " << ib << " " << maxLen << "\n";
    }
    return 0;
}
```

> ⚠️ `n`이 커질 수 있으므로 `long long` 사용 권장

---

## 🔄 1249 / 1250 — 수 뒤집기

### 📋 문제 요약

정수 또는 문자열로 입력받아 **뒤집은 수** 출력

### 방법 비교표

|방식|장점|단점|
|---|---|---|
|정수로 처리|이후 계산에 편리|자릿수 파악 필요|
|문자열로 처리|구현이 단순|계산 필요 시 정수 변환 필요|

### 💻 정수 방식 #1 (C++)

```cpp
// 4자리 수 예: 1824 → 4281
int reverse = 0;
int mul = 1000;  // 자릿수에 따라 달라짐
int origin = input;

while (origin != 0) {
    reverse += (origin % 10) * mul;
    origin /= 10;
    mul /= 10;
}
```

### 💻 정수 방식 #2 — iReverse 함수 (C++)

```cpp
int iReverse(int iX) {
    const int MAX_DIGIT = 10;
    int iArray[MAX_DIGIT];
    int iRlt = 0, iCount = 1, iM;

    // 각 자리수를 낮은 자리부터 배열에 저장
    for (int j = 0; j < MAX_DIGIT; j++) {
        iArray[j] = iX % 10;  // 123 → {3,2,1,...}
        iX /= 10;
        if (iX == 0) break;
        iCount++;
    }

    // 배열을 역순으로 읽어 정수 복원
    iM = 1;
    for (int j = iCount - 1; j >= 0; j--) {
        iRlt += iArray[j] * iM;
        iM *= 10;
    }
    return iRlt;
}
```

### 💻 문자열 방식 (C++)

```cpp
#include <string>
#include <algorithm>
using namespace std;

string reverseStr(string s) {
    reverse(s.begin(), s.end());
    return s;
}
// 또는 직접 구현
string origin = "1824";
string rev = "";
for (int i = origin.size() - 1; i >= 0; i--) {
    rev += origin[i];
}
```

> ⚠️ C 스타일 char 배열 대신 C++의 `std::string` + `std::reverse` 활용 권장

---

## 🪞 1370 / 1250 — 수 뒤집기 #2 (좌우 대칭 판별)

### 💡 팰린드롬(좌우 대칭) 판별 방법

|방법|설명|
|---|---|
|방법 1|양 끝부터 중앙까지 비교 (인덱스 0↔n-1, 1↔n-2 ...)|
|방법 2|원래 수 == 뒤집은 수 이면 대칭|

### 💻 방법 1 (C++)

```cpp
#include <string>
using namespace std;

bool isPalindrome(const string& s) {
    int n = s.size();
    for (int i = 0; i < n / 2; i++) {
        if (s[i] != s[n - 1 - i]) return false;
    }
    return true;
}
```

### 💻 방법 2 (C++)

```cpp
bool isPalindrome(int x) {
    return x == iReverse(x);  // iReverse는 위에서 정의한 함수
}
```

---

## 💾 1251 — Binary Conversion (이진수 → 십진수)

### 📋 문제 요약

- 최대 20자리 이진수 문자열 → 십진수 정수 출력
- ⚠️ 20자리는 `int` 범위 초과 가능 → `long long` 사용

### 💡 처리 방법

```
뒤에서부터 처리:
인덱스 n-1부터 → 2^0, 2^1, 2^2 ... 순서로 곱하여 합산
```

### 💻 C++ 코드

```cpp
#include <iostream>
#include <string>
#include <cmath>
using namespace std;

int main() {
    string bin;
    cin >> bin;
    
    long long result = 0;
    int n = bin.size();
    for (int i = 0; i < n; i++) {
        if (bin[i] == '1') {
            result += (long long)pow(2, n - 1 - i);
        }
    }
    cout << result << endl;
    return 0;
}
```

> ⚠️ `pow()`는 부동소수점이므로 정확도 문제 발생 가능 → 비트 시프트(`1LL << k`) 권장

```cpp
// 안전한 방법 (비트 시프트)
for (int i = 0; i < n; i++) {
    if (bin[i] == '1') {
        result += (1LL << (n - 1 - i));
    }
}
```

---

## 🌐 1252 — IP Address (이진수 → IP 주소)

### 📋 문제 요약

- 32자리 이진수 → 8비트씩 나눠 십진수 4개를 `.`으로 출력
- 예: `00000011100000001111111111111111` → `3.128.255.255`

### 💻 C++ 코드

```cpp
#include <iostream>
#include <string>
using namespace std;

int binToInt(const string& s, int start) {
    int result = 0;
    for (int i = 0; i < 8; i++) {
        result = result * 2 + (s[start + i] - '0');
    }
    return result;
}

int main() {
    string bin;
    cin >> bin;
    
    for (int i = 0; i < 4; i++) {
        if (i > 0) cout << ".";
        cout << binToInt(bin, i * 8);
    }
    cout << endl;
    return 0;
}
```

---

## 🌐 1253 — IP Address #2 (IP 주소 → 이진수)

### 📋 문제 요약

- `3.128.255.255` → `00000011100000001111111111111111`
- 각 십진수를 8자리 이진수 문자열로 변환 후 이어 붙임

### 💻 C++ 코드

```cpp
#include <iostream>
#include <string>
using namespace std;

string toBinary8(int x) {
    string result = "";
    for (int i = 7; i >= 0; i--) {
        result += ((x >> i) & 1) ? '1' : '0';
    }
    return result;
}

int main() {
    int a, b, c, d;
    scanf("%d.%d.%d.%d", &a, &b, &c, &d);  // 점 구분 입력
    cout << toBinary8(a) << toBinary8(b)
         << toBinary8(c) << toBinary8(d) << endl;
    return 0;
}
```

> ⚠️ `scanf`의 `%d.%d.%d.%d` 포맷은 `.`을 자동으로 처리해 줘서 편리

---

## 📐 1255 — 더블 미역 (사각형 겹침 면적)

### 📋 문제 요약

- 두 사각형의 대각선 좌표 입력 → 겹치는 영역의 **셀 수** 출력
- ⚠️ 대각선 방향이 명시되지 않음 → min/max로 정규화 필수

### 💡 핵심 아이디어

```
겹침 면적 = x방향 겹친 길이 × y방향 겹친 길이
x 겹침: min(axmax, bxmax) - max(axmin, bxmin)  → 양수인 경우만 겹침
y 겹침: min(aymax, bymax) - max(aymin, bymin)  → 양수인 경우만 겹침
```

### 💻 C++ 코드

```cpp
#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int ax1, ay1, ax2, ay2;
    int bx1, by1, bx2, by2;
    cin >> ax1 >> ay1 >> ax2 >> ay2
        >> bx1 >> by1 >> bx2 >> by2;

    // 정규화: min/max로 좌하단, 우상단 통일
    int axMin = min(ax1, ax2), axMax = max(ax1, ax2);
    int ayMin = min(ay1, ay2), ayMax = max(ay1, ay2);
    int bxMin = min(bx1, bx2), bxMax = max(bx1, bx2);
    int byMin = min(by1, by2), byMax = max(by1, by2);

    int dx = min(axMax, bxMax) - max(axMin, bxMin);
    int dy = min(ayMax, byMax) - max(ayMin, byMin);

    if (dx > 0 && dy > 0)
        cout << dx * dy << endl;
    else
        cout << 0 << endl;

    return 0;
}
```

> ⚠️ `dx`나 `dy` 중 하나라도 0 이하이면 겹치지 않으므로 반드시 두 조건 모두 확인

---

## 🔍 1233 — 소수 구하기 I

### 📋 문제 요약

- 입력된 정수가 소수인지 판별 → `Prime` 또는 `Not Prime` 출력

### 소수 판별 방법 비교

|방법|범위|속도|
|---|---|---|
|방법 1: 기본 (2 ~ n-1)|느림|O(n)|
|방법 2: 홀수만 검사|약 2배 빠름|O(n/2)|
|방법 3: sqrt(n)까지|**권장**|O(√n)|
|방법 4: 에라토스테네스의 체|범위 고정 시 최고|O(n log log n)|

### 💻 방법 3 — sqrt 활용 (C++)

```cpp
#include <cmath>
using namespace std;

bool isPrime(unsigned int x) {
    if (x == 2) return true;
    if (x <= 1 || x % 2 == 0) return false;

    unsigned int limit = (unsigned int)sqrt((double)x);
    for (unsigned int i = 3; i <= limit; i += 2) {
        if (x % i == 0) return false;
    }
    return true;
}
```

### 💻 방법 4 — 에라토스테네스의 체 (C++) — 기본

```cpp
#include <vector>
using namespace std;

const int LIMIT = 10001;
vector<bool> isNotPrime(LIMIT, false);

void primeInit() {
    isNotPrime[0] = isNotPrime[1] = true;
    for (int i = 2; i < LIMIT; i++) {
        if (!isNotPrime[i]) {
            for (int j = i + i; j < LIMIT; j += i) {
                isNotPrime[j] = true;
            }
        }
    }
}
// 사용: if (!isNotPrime[n]) → 소수
```

### 💻 방법 4 — 에라토스테네스의 체 (C++) — 최적화

```cpp
#include <vector>
#include <cmath>
using namespace std;

const int LIMIT = 10001;
vector<bool> isNotPrime(LIMIT, false);

void primeInit() {
    isNotPrime[0] = isNotPrime[1] = true;
    int sqrtLimit = (int)sqrt((double)LIMIT);
    for (int i = 2; i <= sqrtLimit; i++) {
        if (!isNotPrime[i]) {
            // i*i 부터 시작 (그 이전은 이미 처리됨)
            for (int j = i * i; j < LIMIT; j += i) {
                isNotPrime[j] = true;
            }
        }
    }
}
```

> ⚠️ C의 `char a[LIMIT]` 대신 C++에서는 `vector<bool>` 또는 `bool a[LIMIT]` 사용 권장 ⚠️ 큰 배열은 **전역 변수**로 선언해야 스택 오버플로 방지

---

## 🔢 1234 — 소수 구하기 II

### 📋 문제 요약

- 두 정수 `a`, `b` 사이의 소수 **개수** 출력 (a, b 포함)
- 최대값 10,000 → 어떤 방법이든 속도 차이 없음

### 💻 C++ 코드

```cpp
#include <iostream>
using namespace std;

bool isPrime(int x) { /* 위의 방법 3 함수 사용 */ }

int main() {
    int a, b;
    cin >> a >> b;
    if (a > b) swap(a, b);
    int count = 0;
    for (int i = a; i <= b; i++) {
        if (isPrime(i)) count++;
    }
    cout << count << endl;
    return 0;
}
```

---

## 🔢 1235 — 소수 구하기 III

### 📋 문제 요약

- 1234번과 동일하나 최대값이 **100,000**
- ⚠️ sqrt 방법보다 **에라토스테네스의 체**가 훨씬 빠름
- LIMIT을 100,001로 바꾸고 1234번 코드 그대로 활용

---

## 🥇 1256 — Goldbach's Conjecture

### 📋 문제 요약

- 4 이상의 짝수 N = p1 + p2 (두 소수의 합)
- 쌍이 여러 개이면 **차이가 가장 큰 쌍** 출력
- 예: 8 = 3+5 / 12 = 5+7 / 16 = 3+13

### 💡 핵심 전략

```
p1을 2부터 올리고, p2 = N - p1
→ 둘 다 소수이면 차이는 점점 줄어듦
→ 처음 발견된 쌍이 차이가 가장 큰 쌍
```

### 💻 C++ 코드

```cpp
#include <iostream>
#include <vector>
#include <cmath>
using namespace std;

const int LIMIT = 1000001;
vector<bool> isNotPrime(LIMIT, false);

void primeInit() {
    isNotPrime[0] = isNotPrime[1] = true;
    int sqrtLimit = (int)sqrt((double)LIMIT);
    for (int i = 2; i <= sqrtLimit; i++) {
        if (!isNotPrime[i]) {
            for (int j = i * i; j < LIMIT; j += i) {
                isNotPrime[j] = true;
            }
        }
    }
}

int main() {
    primeInit();
    int n;
    while (cin >> n) {
        for (int p1 = 2; p1 <= n / 2; p1++) {
            int p2 = n - p1;
            if (!isNotPrime[p1] && !isNotPrime[p2]) {
                cout << p1 << " " << p2 << "\n";
                break;  // 처음 발견 = 차이 최대
            }
        }
    }
    return 0;
}
```

> ⚠️ 큰 배열(`vector<bool>`)은 **전역 선언** 필수

---

## 🥈 1257 — Goldbach (소수 쌍 개수)

### 📋 문제 요약

- 1256번과 유사하지만 **소수 쌍의 개수** 출력
- 예: 42 = 5+37 = 11+31 = 13+29 = 19+23 → 4쌍

### 💻 C++ 코드

```cpp
int main() {
    primeInit();
    int n;
    while (cin >> n) {
        int count = 0;
        for (int p1 = 2; p1 <= n / 2; p1++) {
            int p2 = n - p1;
            if (!isNotPrime[p1] && !isNotPrime[p2]) {
                count++;
            }
        }
        cout << count << "\n";
    }
    return 0;
}
```

---

## 🎯 핵심 정리

> **이 강의에서 자주 쓰이는 패턴 요약**

|패턴|C++ 핵심 코드|
|---|---|
|EOF 입력|`while (cin >> a >> b)`|
|정수 뒤집기|`% 10`으로 자리수 추출 반복|
|문자열 뒤집기|`std::reverse(s.begin(), s.end())`|
|소수 판별 (단발)|`sqrt(n)`까지 홀수로 나누기|
|소수 판별 (다수)|에라토스테네스의 체 (`vector<bool>`)|
|이진수 변환|`1LL << k` 비트 시프트|
|사각형 겹침|`dx = min(max1,max2) - max(min1,min2)`|

### ⚠️ 자주 하는 실수

- `pow()` 사용 시 부동소수점 오차 → **비트 시프트** 사용
- 배열이 클 때 지역 선언 → **전역 변수** 선언으로 스택 오버플로 방지
- 콜라츠 계산 중 `int` 범위 초과 → **`long long`** 사용
- 사각형 좌표 정규화 누락 → 반드시 `min`/`max`로 정리
- 소수 판별 시 2 예외처리 누락 → `if (x == 2) return true`

---

#오픈소스프로젝트 #알고리즘 #CPP #콜라츠 #소수 #에라토스테네스 #골드바흐 #이진수변환