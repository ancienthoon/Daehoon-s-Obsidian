# 14장. 고유벡터와 고유값 (Eigenvector & Eigenvalue)

---

## 14.1 고유벡터·고유값의 정의

### 📌 핵심 정의 (정의 14.1)

> [!important] 고유방정식 $$Av = \lambda v \quad (v \neq 0)$$
> 
> - $A$: $n \times n$ 정방행렬 (선형 변환)
> - $v$: **고유벡터** (Eigenvector) — 변환 후에도 **방향 불변**
> - $\lambda$: **고유값** (Eigenvalue) — 벡터가 늘어나거나 줄어드는 **스칼라 배율**

### 직관적 이해

```
선형변환 L: Rⁿ → Rⁿ

  [입력 공간]  →L→  [출력 공간]
      v        →→       Av

Av ∥ v  ←  방향이 바뀌지 않는다!
∴ Av = λv
```

**Q&A 정리**

|질문|답|
|---|---|
|임의의 벡터 x는 어디로 변환되는가?|Ax|
|벡터 x는 선형공간으로 표현 가능한 기저의 축은?|무수히 많다|
|변환 전후 방향을 통틀어 나타낼 수 있는 기저가 존재하는가?|존재한다|
|호환성이란?|방향이 바뀌지 않는 것|

---

## 14.2 고유벡터 구하는 방법

### 📐 유도 과정

$$Av = \lambda v = \lambda I v$$

$$\Rightarrow \quad (A - \lambda I)v = 0$$

$B = A - \lambda I$ 로 놓으면 $Bv = 0$ $(v \neq 0)$

> [!warning] 핵심 조건 $v \neq 0$ 이어야 하므로, $B$는 **특이행렬**(역행렬 없음)이어야 함 $$\therefore \det(B) = \det(A - \lambda I) = 0$$ 이것이 **λ에 관한 n차 방정식** → **특성방정식**

---

### 📌 특성방정식 (정의 14.3)

> [!important] 특성방정식 (Characteristic Equation) $$\det(A - \lambda I) = 0$$
> 
> - 이 방정식의 **근** = 행렬 $A$의 **고유값 $\lambda$**
> - $n \times n$ 행렬 → **n차 방정식** → 최대 n개의 고유값

> [!caution] 주의 실수 행렬이라도 고유값은 **복소수**가 나올 수 있다.  
> → **대칭 행렬**이면 고유값과 고유벡터 모두 **실수**임이 보장됨!

---

### 🔢 계산 절차 (Step-by-Step)

```
Step 1. 특성방정식 구하기
        det(A - λI) = 0  →  λ값들 구함

Step 2. 각 λ 대입
        (A - λI)v = 0  →  가우스 소거법으로 풀기

Step 3. 자유변수 선택 (간단한 수: 1 등)
        방향만 의미있으므로 크기 = 1 아니어도 OK

Step 4. 고유벡터 완성
```

---

### 📝 예제: 2×2 행렬

$$A = \begin{pmatrix} 0 & 1 \ -1 & 0 \end{pmatrix}$$

**Step 1. 특성방정식**

$$\det(A - \lambda I) = \det\begin{pmatrix} -\lambda & 1 \ -1 & -\lambda \end{pmatrix} = \lambda^2 + 1 = 0$$

$$\therefore \lambda = \pm i \quad \text{(복소수 고유값)}$$

---

### 📝 예제: 3×3 행렬 (실수 고유값)

$$A = \begin{pmatrix} 2 & 1 & 0 \ 1 & 2 & 0 \ 0 & 0 & 3 \end{pmatrix}$$

**Step 1. 고유값**

$$\det(A - \lambda I) = 0 \Rightarrow (\lambda-1)^2(\lambda-3) = 0$$

$$\therefore \lambda_1 = \lambda_2 = 1 \text{ (중근)}, \quad \lambda_3 = 3$$

**Step 2. $\lambda = 1$ 에 대응하는 고유벡터**

$(A - I)v = 0$ → 가우스 소거 → 조건식: $x_1 + x_2 = 0$

이는 **평면의 방정식** → 독립벡터 **2개** 선택 가능

$$v_1 = \begin{pmatrix}1\-1\0\end{pmatrix}, \quad v_2 = \begin{pmatrix}0\0\1\end{pmatrix}$$

**Step 3. $\lambda = 3$ 에 대응하는 고유벡터**

$$v_3 = \begin{pmatrix}1\1\0\end{pmatrix}$$

---

## 14.3 고유벡터의 성질

### 📌 정리 14.2 — 고유벡터 부분공간

> [!note] 부분공간 성질 동일한 고유값 $\lambda$에 대응하는 고유벡터들은 **하나의 부분공간**을 이룬다.

**증명 아이디어**: $Av_1 = \lambda v_1$, $Av_2 = \lambda v_2$ 이면  
$A(v_1 + v_2) = \lambda(v_1 + v_2)$ → 합도 같은 고유값의 고유벡터 ✓

---

### 📌 고유벡터의 유일성 문제

- 고유벡터는 **유일하지 않다**
- 고유벡터에 스칼라 배를 해도 고유벡터 → **방향만 의미 있음**
- 일반적으로 **크기 = 1인 단위벡터**로 다룸 (계산 편의상 꼭 그럴 필요는 없음)

---

### 📌 정리 14.4 — 고유값의 성질

> [!important] 고유값 성질 $\lambda$가 $A$의 고유값, $v$가 대응 고유벡터일 때:
> 
> 1. **거듭제곱**: $\lambda^k$는 $A^k$의 고유값, $v$는 대응 고유벡터
> 2. **역행렬**: $A$가 가역이면, $\dfrac{1}{\lambda}$는 $A^{-1}$의 고유값, $v$는 대응 고유벡터

**증명 요약**:

```cpp
// ① 귀납법
A^k v = A·(A^{k-1}v) = A·(λ^{k-1}v) = λ^{k-1}(Av) = λ^k v  ✓

// ② 역행렬
Av = λv  양변 왼쪽에 A⁻¹ 곱하기
→ v = λ A⁻¹v
→ A⁻¹v = (1/λ)v  ✓
```

---

### 📌 정리 14.5 — 고유벡터의 선형독립성

> [!important] 행렬 $A$의 **서로 다른 고유값**에 대응하는 고유벡터들은 **상호 선형독립**이다.

**증명 핵심**: 귀류법(Contradiction)

- ${v_1, \cdots, v_p}$가 선형 종속이라 가정
- $A$를 곱한 식 ①과 $\lambda_{r+1}$ 곱한 식 ② 의 차:  
    $\alpha_i(\lambda_i - \lambda_{r+1}) = 0$
- $\lambda_i$가 서로 다르므로 → $\alpha_i = 0$ → 모순 → **독립** ✓

---

## 14.4 예제: 행렬의 거듭제곱 계산

> 고유벡터 성질을 이용하면 $A^{20}v$ 같은 계산도 쉽게 처리 가능!

**문제**: $A = \begin{pmatrix}1&1\0&2\end{pmatrix}$, $A^{20}\begin{pmatrix}2\1\end{pmatrix}$을 구하라

**풀이 전략**:

```
① A의 고유값·고유벡터 구하기
② 주어진 벡터를 고유벡터의 선형결합으로 표현
③ Aⁿ(c₁v₁ + c₂v₂) = c₁λ₁ⁿv₁ + c₂λ₂ⁿv₂ 적용
```

**결과**:

- 고유값: $\lambda_1 = 1$, $\lambda_2 = 2$
- 고유벡터: $v_1 = \begin{pmatrix}1\0\end{pmatrix}$, $v_2 = \begin{pmatrix}1\1\end{pmatrix}$
- 선형결합: $\begin{pmatrix}2\1\end{pmatrix} = 1 \cdot v_1 + 1 \cdot v_2$

$$A^{20}\begin{pmatrix}2\1\end{pmatrix} = 1^{20}v_1 + 2^{20}v_2 = \begin{pmatrix}1+2^{20}\2^{20}\end{pmatrix}$$

---

## 14.5 고유값 분해 (Eigenvalue Decomposition)

### 📌 정리 14.7

> [!important] 고유값 분해 (EVD) $n \times n$ 행렬 $A$가 $n$개의 서로 다른 고유값을 가지면: $$A = PDP^{-1}$$
> 
> - $P$: 고유벡터를 **열벡터**로 갖는 행렬
> - $D$: 고유값을 **대각성분**으로 하는 대각행렬

$$P = \begin{bmatrix} | & | & & | \ v_1 & v_2 & \cdots & v_n \ | & | & & | \end{bmatrix}, \quad D = \begin{pmatrix} \lambda_1 & & 0 \ & \ddots & \ 0 & & \lambda_n \end{pmatrix}$$

**증명 핵심**:

$$AP = A[v_1 \cdots v_n] = [Av_1 \cdots Av_n] = [\lambda_1 v_1 \cdots \lambda_n v_n] = PD$$

$$AP = PD ;\Rightarrow; A = PDP^{-1}$$

> [!note] P가 가역인 이유 고유벡터들이 **선형독립** (정리 14.5) → $P$의 역행렬 존재

---

## 14.6 스펙트럼 분해 (Spectral Decomposition)

### 📌 정리 14.8

> [!important] 스펙트럼 분해 행렬 $A$가 **대칭 행렬** ($A = A^T$) 또는 **Hermitian 행렬**일 때: $$A = PDP^T$$
> 
> - $P$: **정규직교** 고유벡터들을 열벡터로 갖는 행렬 ($P^{-1} = P^T$)
> - 고유값 분해의 **특수한 경우**

### 왜 대칭 행렬이 중요한가?

|성질|일반 행렬|대칭 행렬 ($A=A^T$)|
|---|---|---|
|고유값|복소수 가능|**실수** 보장|
|고유벡터 간 관계|무관|**서로 수직** (직교)|
|분해 형태|$A = PDP^{-1}$|$A = QDQ^T$|

### 대칭 행렬의 직교성 증명

$Av_i = \lambda_i v_i$, $Av_j = \lambda_j v_j$, $\lambda_i \neq \lambda_j$ 일 때:

$$\lambda_i (v_i \cdot v_j) = (Av_i)^T v_j = v_i^T A^T v_j = v_i^T (Av_j) = \lambda_j(v_i \cdot v_j)$$

$$\Rightarrow (\lambda_i - \lambda_j)(v_i \cdot v_j) = 0$$

$$\lambda_i \neq \lambda_j ;\Rightarrow; v_i \cdot v_j = 0 \quad \text{(직교!)}$$

---

### 스펙트럼 분해의 외적 표현

$$A = \lambda_1 \hat{q}_1 \hat{q}_1^T + \lambda_2 \hat{q}_2 \hat{q}_2^T + \cdots + \lambda_n \hat{q}_n \hat{q}_n^T$$

- $\hat{q}_i \hat{q}_i^T$: $n \times n$ **투영 행렬** (Projection Matrix)
- 가장 큰 $|\lambda_1|$ 항이 행렬 $A$를 **지배**함
- 근사: $A \approx \lambda_1 \hat{q}_1 \hat{q}_1^T$ (데이터 압축의 원리!)

> [!note] Spectrum **Spectrum** = 행렬 $A$의 **고유값들의 집합** ${\lambda_1, \lambda_2, \cdots, \lambda_n}$

---

## 📌 닮음 행렬과 특성방정식

> [!important] 닮음 행렬 정리 $n \times n$ 행렬 $A$와 $B$가 닮았다면:
> 
> - **같은 특성다항식**을 가짐
> - **같은 고유값**을 가짐

**증명**: $$B = P^{-1}AP ;\Rightarrow; B - \lambda I = P^{-1}(A - \lambda I)P$$ $$\det(B - \lambda I) = \det(P^{-1}) \cdot \det(A-\lambda I) \cdot \det(P) = \det(A-\lambda I)$$

---

## 💡 핵심 정리 요약

> [!summary] 14장 핵심 요약
> 
> **① 고유벡터·고유값이란?**
> 
> - $Av = \lambda v$ 를 만족하는 비영벡터 $v$ (방향 불변!)
> 
> **② 구하는 방법**
> 
> - 특성방정식 $\det(A - \lambda I) = 0$ → 고유값 $\lambda$ 먼저
> - $(A - \lambda I)v = 0$ → 가우스 소거 → 고유벡터
> 
> **③ 중요 성질**
> 
> - 고유벡터는 부분공간을 이룸
> - 서로 다른 고유값의 고유벡터 → **선형독립**
> - $A^k$의 고유값 = $\lambda^k$, 역행렬의 고유값 = $1/\lambda$
> 
> **④ 행렬 분해**
> 
> - 일반 행렬: $A = PDP^{-1}$ (고유값 분해)
> - 대칭 행렬: $A = QDQ^T$ (스펙트럼 분해, 고유벡터 직교)

---

## ⚠️ 시험 포인트

> [!warning] 자주 출제되는 포인트
> 
> 1. **특성방정식** 세우고 풀기 (2×2, 3×3)
> 2. **중근**인 경우 → 고유벡터가 여러 개 나올 수 있음 (평면의 방정식)
> 3. 대칭 행렬의 고유벡터 → 반드시 **서로 수직**인지 확인
> 4. $A = PDP^{-1}$ 분해 — $P$, $D$ 구성 방법
> 5. **스펙트럼 분해**: 대칭 행렬에서만 $P^{-1} = P^T$ 사용 가능

---

#선형대수 #고유벡터 #고유값 #Eigenvector #Eigenvalue #특성방정식 #고유값분해 #스펙트럼분해 #PCA #SVD