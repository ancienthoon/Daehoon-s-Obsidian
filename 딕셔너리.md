# 딕셔너리 (Dictionary)

#파이썬 #자료구조 #딕셔너리

---

## 📌 목차

1. [[#1. 딕셔너리란?]]
2. [[#2. 딕셔너리 생성]]
3. [[#3. 값 접근 및 수정]]
4. [[#4. 주요 메서드]]
5. [[#5. 순회 (for문)]]
6. [[#6. 딕셔너리 컴프리헨션]]
7. [[#7. 자주 실수하는 포인트]]

---

## 1. 딕셔너리란?

> [!info] 핵심 개념 **키(key)와 값(value)의 쌍**으로 데이터를 저장하는 자료구조. 중괄호 `{}`로 표현하며, `키: 값` 형태로 저장함.

```
{"이름": "홍길동", "나이": 20, "학번": "20253334"}
   키       값       키    값     키           값
```

|특징|설명|
|---|---|
|**키로 값 접근**|인덱스(숫자) 대신 키를 사용|
|키는 고유함|같은 키가 두 번 오면 마지막 값으로 덮어씀|
|순서 유지|Python 3.7+ 부터 **삽입 순서 유지**|
|수정 가능|값 추가/삭제/변경 가능|

> [!tip] C++ map과 비교 C++의 `std::map<string, int>` 또는 `unordered_map`과 유사
> 
> ```cpp
> // C++
> map<string, int> m;
> m["age"] = 20;
> 
> # Python
> d = {}
> d["age"] = 20
> ```
> 
> 파이썬 딕셔너리는 **`unordered_map`에 더 가깝고** (해시 기반), **값의 자료형 제한이 없음**

---

## 2. 딕셔너리 생성

```python
# 기본 생성
student = {
    "name": "홍길동",
    "age": 20,
    "grade": "A"
}

# 빈 딕셔너리
empty1 = {}
empty2 = dict()

# dict() 함수로 생성
d = dict(name="철수", age=19)   # {"name": "철수", "age": 19}

# 키-값 쌍 리스트로 생성
pairs = [("a", 1), ("b", 2), ("c", 3)]
d = dict(pairs)                  # {"a": 1, "b": 2, "c": 3}

# fromkeys() — 동일한 초기값으로 생성
keys = ["국어", "영어", "수학"]
scores = dict.fromkeys(keys, 0)  # {"국어": 0, "영어": 0, "수학": 0}
```

### 키(Key)로 사용 가능한 자료형

|자료형|키로 사용 가능?|
|---|---|
|`str`|✅|
|`int`, `float`|✅|
|`tuple`|✅ (내부에 불변 값만 있을 때)|
|`list`|❌ (mutable이라 불가)|
|`dict`|❌ (mutable이라 불가)|

---

## 3. 값 접근 및 수정

### 값 접근

```python
student = {"name": "홍길동", "age": 20}

# 방법 1: 대괄호 []
print(student["name"])    # '홍길동'

# 방법 2: .get() — 키 없어도 에러 안 남
print(student.get("age"))          # 20
print(student.get("phone"))        # None (키 없으면 None 반환)
print(student.get("phone", "없음"))# "없음" (기본값 지정 가능)
```

> [!warning] `[]` vs `.get()` 차이
> 
> ```python
> d = {"a": 1}
> print(d["b"])       # ← KeyError 발생!
> print(d.get("b"))   # ← None 반환 (에러 없음)
> ```
> 
> 키가 있는지 확실하지 않을 때는 **`.get()` 사용 권장**

### 값 추가 / 수정

```python
student = {"name": "홍길동", "age": 20}

# 추가 (없는 키에 값 할당)
student["email"] = "hong@example.com"

# 수정 (있는 키에 값 할당)
student["age"] = 21

# 여러 키 한 번에 업데이트
student.update({"age": 22, "grade": "A"})
```

### 값 삭제

```python
d = {"a": 1, "b": 2, "c": 3}

del d["a"]           # 키 "a" 삭제
val = d.pop("b")     # 키 "b" 삭제 + 값 반환 → val = 2
d.clear()            # 전체 삭제 → {}
```

---

## 4. 주요 메서드

|메서드|기능|반환|
|---|---|---|
|`.keys()`|모든 키 반환|`dict_keys` 객체|
|`.values()`|모든 값 반환|`dict_values` 객체|
|`.items()`|모든 키-값 쌍 반환|`dict_items` 객체|
|`.get(k, default)`|키 k의 값 반환 (없으면 default)|값 또는 `None`|
|`.update(d2)`|d2로 값 업데이트|`None`|
|`.pop(k)`|키 k 삭제 후 값 반환|값|
|`.popitem()`|마지막 삽입 항목 삭제 후 반환|`(key, value)` 튜플|
|`.setdefault(k, v)`|키 없을 때만 k:v 추가|값|
|`.clear()`|전체 삭제|`None`|
|`.copy()`|얕은 복사|새 딕셔너리|

```python
d = {"a": 1, "b": 2, "c": 3}

print(d.keys())    # dict_keys(['a', 'b', 'c'])
print(d.values())  # dict_values([1, 2, 3])
print(d.items())   # dict_items([('a', 1), ('b', 2), ('c', 3)])

# 리스트로 변환
list(d.keys())     # ['a', 'b', 'c']
```

### setdefault 활용 (그룹화에 유용)

```python
# 단어 빈도수 세기
words = ["apple", "banana", "apple", "cherry", "banana", "apple"]
count = {}
for word in words:
    count[word] = count.get(word, 0) + 1

print(count)  # {'apple': 3, 'banana': 2, 'cherry': 1}
```

---

## 5. 순회 (for문)

```python
student = {"name": "홍길동", "age": 20, "grade": "A"}

# 키만 순회 (기본)
for key in student:
    print(key)

# 값만 순회
for value in student.values():
    print(value)

# 키-값 동시 순회 (가장 많이 사용)
for key, value in student.items():
    print(f"{key}: {value}")
# name: 홍길동
# age: 20
# grade: A
```

### 포함 여부 확인

```python
d = {"a": 1, "b": 2}

"a" in d           # True  ← 키를 검색
1 in d.values()    # True  ← 값을 검색
```

> [!tip] `in` 연산자는 기본적으로 **키**를 검색함

---

## 6. 딕셔너리 컴프리헨션

```python
# 기본 형태: {키표현식: 값표현식 for 변수 in 반복가능객체}
squares = {x: x**2 for x in range(5)}
# {0: 0, 1: 1, 2: 4, 3: 9, 4: 16}

# 조건 추가
even_squares = {x: x**2 for x in range(10) if x % 2 == 0}
# {0: 0, 2: 4, 4: 16, 6: 36, 8: 64}

# 기존 딕셔너리 변환 (값에 10 곱하기)
scores = {"국어": 80, "수학": 90, "영어": 70}
scaled = {k: v * 10 for k, v in scores.items()}
# {"국어": 800, "수학": 900, "영어": 700}
```

---

## 7. 자주 실수하는 포인트

> [!warning] 없는 키에 `[]`로 접근하면 에러
> 
> ```python
> d = {"a": 1}
> print(d["b"])    # KeyError: 'b'
> 
> # 해결책 1: in으로 먼저 확인
> if "b" in d:
>     print(d["b"])
> 
> # 해결책 2: .get() 사용
> print(d.get("b", 0))   # 없으면 0 반환
> ```

> [!warning] 키는 중복 불가 — 나중 값으로 덮어쓰기
> 
> ```python
> d = {"a": 1, "a": 99, "b": 2}
> print(d)   # {"a": 99, "b": 2}  ← 앞의 1은 사라짐
> ```

> [!tip] 딕셔너리를 활용한 switch-case 대체 파이썬 3.10 이하에는 switch-case가 없어서 딕셔너리로 대체함
> 
> ```python
> def get_day(num):
>     days = {1: "월", 2: "화", 3: "수", 4: "목", 5: "금"}
>     return days.get(num, "없는 요일")
> 
> print(get_day(3))   # 수
> ```

---

## 🔗 관련 노트

- [[파이썬_기초_입문]] — 변수, 자료형, 반복문
- [[파이썬_자료구조_리스트]] — 순서형 자료구조
- [[파이썬_자료구조_튜플]] — 딕셔너리 키로 사용 가능
- [[파이썬_자료구조_세트]] — 중복 없는 집합