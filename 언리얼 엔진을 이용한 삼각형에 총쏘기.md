# 🎮 UE5 Ray-Triangle Intersection 사용설명서

> [!info] 선형대수 과제인 선분-삼각형 교차 시뮬레이션을 **Unreal Engine 5.7 C++** 로 구현한 프로젝트입니다. 마우스 드래그, 좌표 입력, 실시간 교차 판정 기능을 포함합니다.

---

## 📋 목차

- [[#🛠️ 환경 설정]]
- [[#📁 프로젝트 구조]]
- [[#💻 핵심 코드]]
- [[#🎮 사용 방법]]
- [[#⚠️ 트러블슈팅]]
- [[#📐 알고리즘 설명]]

---

## 🛠️ 환경 설정

### 필수 설치 목록

|소프트웨어|버전|비고|
|---|---|---|
|Unreal Engine|5.7|Epic Games Launcher|
|Visual Studio|2022 Community|C++ 워크로드 필수|

### Visual Studio 설치 시 필수 체크 항목

- ✅ C++를 사용한 데스크톱 개발
- ✅ .NET 데스크톱 개발

### VS 통합 설정

> [!warning] Visual Studio Integration Tool VS에서 언리얼 프로젝트 열면 자동으로 뜨는 설정 창에서:
> 
> - `Visual Studio Integration Tool` → **엔진에 설치** 클릭
> - **백그라운드에서 실행** 체크박스 활성화
> - `명명 규칙 검사기`, `HLSL 지원` → 무시해도 됨

### 프로젝트 경로 규칙

> [!danger] 반드시 지켜야 할 경로 규칙
> 
> ```
> ❌ C:\Users\user\OneDrive\바탕 화면\...  (한글, OneDrive 금지)
> ✅ C:\Users\user\MyNewProject\...        (영문, 로컬 경로)
> ```
> 
> - 한글 폴더명 금지
> - OneDrive/Google Drive 등 클라우드 폴더 금지
> - 바탕화면 금지

---

## 📁 프로젝트 구조

```
MyNewProject/
├── Source/
│   └── MyNewProject/
│       ├── Public/
│       │   └── RayTriangleActor.h       ← 헤더 파일
│       ├── Private/
│       │   ├── RayTriangleActor.cpp     ← 메인 코드
│       │   ├── MyActor.cpp
│       │   └── MyNewProject.cpp
│       └── MyNewProject.Build.cs        ← 모듈 의존성
├── Content/
│   └── RayTriangleWidget (블루프린트)   ← 미사용 (C++로 대체)
└── MyNewProject.uproject
```

### Build.cs 의존성 설정

```c#
// MyNewProject.Build.cs
PublicDependencyModuleNames.AddRange(new string[] {
    "Core", "CoreUObject", "Engine", "InputCore",
    "UMG", "Slate", "SlateCore"   // UMG UI 사용을 위해 필수
});
```

### .uproject 설정

> [!tip] VisualStudioTools 비활성화 UE 5.7에서 `VisualStudioTools` 플러그인 충돌 문제 방지:
> 
> ```json
> {
>     "Name": "VisualStudioTools",
>     "Enabled": false
> }
> ```

---

## 💻 핵심 코드

### RayTriangleActor.h

```cpp
#pragma once
#include "CoreMinimal.h"
#include "GameFramework/Actor.h"
#include "Blueprint/UserWidget.h"
#include "Components/EditableTextBox.h"
#include "RayTriangleActor.generated.h"

UCLASS()
class MYNEWPROJECT_API ARayTriangleActor : public AActor
{
    GENERATED_BODY()
public:
    ARayTriangleActor();
protected:
    virtual void BeginPlay() override;
    virtual void Tick(float DeltaTime) override;
public:
    // 삼각형 꼭짓점
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "삼각형")
    FVector A = FVector(200.f, 0.f, 0.f);
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "삼각형")
    FVector B = FVector(-100.f, 200.f, 0.f);
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "삼각형")
    FVector C = FVector(-100.f, -100.f, 200.f);

    // 선분 끝점
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "선분")
    FVector P = FVector(-200.f, 100.f, 100.f);
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "선분")
    FVector Q = FVector(300.f, 0.f, 0.f);

    // 교점 결과
    UPROPERTY(VisibleAnywhere, Category = "결과")
    FVector IntersectPoint = FVector::ZeroVector;
    UPROPERTY(VisibleAnywhere, Category = "결과")
    bool bHasIntersect = false;

private:
    void CheckAndDraw();
    void HandleMouseInput();
    void DrawHUD();
    void CreateInputUI();
    void UpdateInputBoxes();
    void ReadInputBoxes();

    // 드래그 관련
    FVector* SelectedPoint  = nullptr;
    bool     bIsDragging    = false;
    FVector  DragPlaneNormal;
    FVector  DragOffset;

    // UI 입력창
    TArray<UEditableTextBox*> InputBoxes;  // 순서: Ax,Ay,Az,Bx,...,Qz (15개)
    UUserWidget* UIWidget = nullptr;
};
```

### RayTriangleActor.cpp

```cpp
#include "RayTriangleActor.h"
#include "DrawDebugHelpers.h"
#include "GameFramework/PlayerController.h"
#include "Engine/World.h"
#include "Engine/Engine.h"
#include "Blueprint/UserWidget.h"
#include "Blueprint/WidgetTree.h"
#include "Components/EditableTextBox.h"
#include "Components/TextBlock.h"
#include "Components/VerticalBox.h"
#include "Components/VerticalBoxSlot.h"
#include "Components/HorizontalBox.h"
#include "Components/HorizontalBoxSlot.h"
#include "Components/CanvasPanel.h"
#include "Components/CanvasPanelSlot.h"
#include "Components/Border.h"
#include "Styling/SlateTypes.h"
#include "Styling/CoreStyle.h"

ARayTriangleActor::ARayTriangleActor()
{
    PrimaryActorTick.bCanEverTick = true;
}

void ARayTriangleActor::BeginPlay()
{
    Super::BeginPlay();
    APlayerController* PC = GetWorld()->GetFirstPlayerController();
    if (PC)
    {
        PC->bShowMouseCursor   = true;
        PC->bEnableClickEvents = true;
    }
    CreateInputUI();
}

void ARayTriangleActor::CreateInputUI()
{
    UIWidget = CreateWidget<UUserWidget>(
        GetWorld()->GetFirstPlayerController(),
        UUserWidget::StaticClass());
    if (!UIWidget) return;

    UWidgetTree* WT = UIWidget->WidgetTree.Get();
    if (!WT) return;

    // 캔버스 루트
    UCanvasPanel* Canvas = NewObject<UCanvasPanel>(WT, UCanvasPanel::StaticClass());
    WT->RootWidget = Canvas;

    // 배경 패널
    UBorder* BG = NewObject<UBorder>(WT, UBorder::StaticClass());
    BG->SetBrushColor(FLinearColor(0.05f, 0.05f, 0.05f, 0.85f));

    UCanvasPanelSlot* BGSlot = Canvas->AddChildToCanvas(BG);
    BGSlot->SetAnchors(FAnchors(1.f, 0.f, 1.f, 0.f));  // 오른쪽 상단 고정
    BGSlot->SetAlignment(FVector2D(1.f, 0.f));
    BGSlot->SetPosition(FVector2D(-20.f, 20.f));
    BGSlot->SetSize(FVector2D(400.f, 460.f));

    UVerticalBox* VBox = NewObject<UVerticalBox>(WT, UVerticalBox::StaticClass());
    BG->SetContent(VBox);

    // 각 점 정보
    struct FPtInfo { FString Name; FLinearColor Color; FVector* Ptr; };
    TArray<FPtInfo> Pts = {
        { TEXT("A"), FLinearColor(0.5f,0.47f,0.87f,1.f), &A },
        { TEXT("B"), FLinearColor(0.11f,0.62f,0.46f,1.f), &B },
        { TEXT("C"), FLinearColor(0.85f,0.35f,0.19f,1.f), &C },
        { TEXT("P"), FLinearColor(0.21f,0.54f,0.87f,1.f), &P },
        { TEXT("Q"), FLinearColor(0.73f,0.46f,0.09f,1.f), &Q },
    };

    for (auto& Pt : Pts)
    {
        UHorizontalBox* HBox = NewObject<UHorizontalBox>(WT, UHorizontalBox::StaticClass());

        // 점 이름 라벨
        UTextBlock* Label = NewObject<UTextBlock>(WT, UTextBlock::StaticClass());
        Label->SetText(FText::FromString(Pt.Name));
        Label->SetColorAndOpacity(FSlateColor(Pt.Color));
        FSlateFontInfo Font = Label->GetFont();
        Font.Size = 14;
        Label->SetFont(Font);

        UHorizontalBoxSlot* LabelSlot = HBox->AddChildToHorizontalBox(Label);
        LabelSlot->SetSize(FSlateChildSize(ESlateSizeRule::Automatic));
        LabelSlot->SetPadding(FMargin(8.f, 4.f));

        // X, Y, Z 입력창
        for (int32 i = 0; i < 3; i++)
        {
            UEditableTextBox* Box = NewObject<UEditableTextBox>(
                WT, UEditableTextBox::StaticClass());

            float Val = (i == 0) ? Pt.Ptr->X
                      : (i == 1) ? Pt.Ptr->Y
                      :            Pt.Ptr->Z;
            Box->SetText(FText::FromString(FString::Printf(TEXT("%.1f"), Val)));

            // 텍스트 색깔 검은색
            FEditableTextBoxStyle Style = Box->GetWidgetStyle();
            Style.SetForegroundColor(FSlateColor(FLinearColor::Black));
            Box->SetWidgetStyle(Style);

            UHorizontalBoxSlot* BoxSlot = HBox->AddChildToHorizontalBox(Box);
            BoxSlot->SetSize(FSlateChildSize(ESlateSizeRule::Fill));
            BoxSlot->SetPadding(FMargin(4.f, 4.f));

            InputBoxes.Add(Box);
        }

        UVerticalBoxSlot* VSlot = VBox->AddChildToVerticalBox(HBox);
        VSlot->SetPadding(FMargin(4.f, 6.f));
    }

    // 안내 텍스트
    UTextBlock* Hint = NewObject<UTextBlock>(WT, UTextBlock::StaticClass());
    Hint->SetText(FText::FromString(TEXT("좌표 입력 후 자동 적용")));
    Hint->SetColorAndOpacity(FSlateColor(FLinearColor::Gray));
    VBox->AddChildToVerticalBox(Hint);

    UIWidget->AddToViewport();
}

void ARayTriangleActor::UpdateInputBoxes()
{
    if (InputBoxes.Num() < 15) return;
    FVector* Pts[] = { &A, &B, &C, &P, &Q };
    for (int32 i = 0; i < 5; i++)
    {
        InputBoxes[i*3+0]->SetText(FText::FromString(
            FString::Printf(TEXT("%.1f"), Pts[i]->X)));
        InputBoxes[i*3+1]->SetText(FText::FromString(
            FString::Printf(TEXT("%.1f"), Pts[i]->Y)));
        InputBoxes[i*3+2]->SetText(FText::FromString(
            FString::Printf(TEXT("%.1f"), Pts[i]->Z)));
    }
}

void ARayTriangleActor::ReadInputBoxes()
{
    if (InputBoxes.Num() < 15) return;
    FVector* Pts[] = { &A, &B, &C, &P, &Q };
    for (int32 i = 0; i < 5; i++)
    {
        Pts[i]->X = FCString::Atof(*InputBoxes[i*3+0]->GetText().ToString());
        Pts[i]->Y = FCString::Atof(*InputBoxes[i*3+1]->GetText().ToString());
        Pts[i]->Z = FCString::Atof(*InputBoxes[i*3+2]->GetText().ToString());
    }
}

void ARayTriangleActor::Tick(float DeltaTime)
{
    Super::Tick(DeltaTime);

    ReadInputBoxes();  // 매 프레임 입력창 값 읽기

    APlayerController* PC = GetWorld()->GetFirstPlayerController();

    HandleMouseInput();

    if (bIsDragging)
        UpdateInputBoxes();  // 드래그 중이면 입력창 업데이트

    CheckAndDraw();
    DrawHUD();
}

void ARayTriangleActor::HandleMouseInput()
{
    APlayerController* PC = GetWorld()->GetFirstPlayerController();
    if (!PC) return;

    FVector MouseWorldPos, MouseWorldDir;
    if (!PC->DeprojectMousePositionToWorld(MouseWorldPos, MouseWorldDir)) return;

    bool bMouseDown = PC->IsInputKeyDown(EKeys::LeftMouseButton);

    if (bMouseDown && !bIsDragging)
    {
        FVector* Points[] = { &A, &B, &C, &P, &Q };
        float    MinDist  = 50.f;
        SelectedPoint     = nullptr;

        for (FVector* Pt : Points)
        {
            FVector ToPoint = *Pt - MouseWorldPos;
            float   Along   = FVector::DotProduct(ToPoint, MouseWorldDir);
            FVector Closest = MouseWorldPos + MouseWorldDir * Along;
            float   Dist    = FVector::Dist(Closest, *Pt);

            if (Dist < MinDist)
            {
                MinDist         = Dist;
                SelectedPoint   = Pt;
                DragPlaneNormal = -MouseWorldDir;
                DragOffset      = *Pt - Closest;
            }
        }
        if (SelectedPoint) bIsDragging = true;
    }
    else if (bMouseDown && bIsDragging && SelectedPoint)
    {
        float Denom = FVector::DotProduct(DragPlaneNormal, MouseWorldDir);
        if (FMath::Abs(Denom) > 1e-6f)
        {
            float t = FVector::DotProduct(
                DragPlaneNormal,
                (*SelectedPoint - MouseWorldPos)) / Denom;
            *SelectedPoint = MouseWorldPos + MouseWorldDir * t + DragOffset;
        }
    }
    else if (!bMouseDown)
    {
        bIsDragging   = false;
        SelectedPoint = nullptr;
    }
}

void ARayTriangleActor::DrawHUD()
{
    if (!GEngine) return;

    GEngine->AddOnScreenDebugMessage(1, 0.f, FColor(127,119,221),
        FString::Printf(TEXT("A: (%.1f, %.1f, %.1f)"), A.X, A.Y, A.Z));
    GEngine->AddOnScreenDebugMessage(2, 0.f, FColor(29,158,117),
        FString::Printf(TEXT("B: (%.1f, %.1f, %.1f)"), B.X, B.Y, B.Z));
    GEngine->AddOnScreenDebugMessage(3, 0.f, FColor(216,90,48),
        FString::Printf(TEXT("C: (%.1f, %.1f, %.1f)"), C.X, C.Y, C.Z));
    GEngine->AddOnScreenDebugMessage(4, 0.f, FColor(55,138,221),
        FString::Printf(TEXT("P: (%.1f, %.1f, %.1f)"), P.X, P.Y, P.Z));
    GEngine->AddOnScreenDebugMessage(5, 0.f, FColor(186,117,23),
        FString::Printf(TEXT("Q: (%.1f, %.1f, %.1f)"), Q.X, Q.Y, Q.Z));

    if (bHasIntersect)
        GEngine->AddOnScreenDebugMessage(6, 0.f, FColor::Red,
            FString::Printf(TEXT("교점: (%.1f, %.1f, %.1f)"),
                IntersectPoint.X, IntersectPoint.Y, IntersectPoint.Z));
}

void ARayTriangleActor::CheckAndDraw()
{
    FlushPersistentDebugLines(GetWorld());

    FVector VA = A, VB = B, VC = C, VP = P, VQ = Q;
    FVector AB = VB - VA;
    FVector AC = VC - VA;
    FVector N  = FVector::CrossProduct(AB, AC);
    FVector D  = VQ - VP;
    float   dn = FVector::DotProduct(N, D);

    // 삼각형 (노란색)
    DrawDebugLine(GetWorld(), VA, VB, FColor::Yellow, false, -1.f, 0, 5.f);
    DrawDebugLine(GetWorld(), VB, VC, FColor::Yellow, false, -1.f, 0, 5.f);
    DrawDebugLine(GetWorld(), VC, VA, FColor::Yellow, false, -1.f, 0, 5.f);

    DrawDebugSphere(GetWorld(), VA, 15.f, 8,
        SelectedPoint == &A ? FColor::White : FColor(127,119,221), false, -1.f);
    DrawDebugSphere(GetWorld(), VB, 15.f, 8,
        SelectedPoint == &B ? FColor::White : FColor(29,158,117),  false, -1.f);
    DrawDebugSphere(GetWorld(), VC, 15.f, 8,
        SelectedPoint == &C ? FColor::White : FColor(216,90,48),   false, -1.f);

    // 선분 (파란색)
    DrawDebugLine(GetWorld(), VP, VQ, FColor::Blue, false, -1.f, 0, 5.f);
    DrawDebugSphere(GetWorld(), VP, 15.f, 8,
        SelectedPoint == &P ? FColor::White : FColor(55,138,221),  false, -1.f);
    DrawDebugSphere(GetWorld(), VQ, 15.f, 8,
        SelectedPoint == &Q ? FColor::White : FColor(186,117,23),  false, -1.f);

    // 교차 판정
    bHasIntersect = false;
    if (FMath::Abs(dn) < 1e-9f)
    {
        float coplanar = FVector::DotProduct(N, VP - VA);
        if (FMath::Abs(coplanar) < 0.1f)
        {
            if (GEngine)
                GEngine->AddOnScreenDebugMessage(6, 0.f, FColor::Cyan,
                    TEXT("교점: 무한개 (동일 평면)"));
        }
        else
        {
            if (GEngine)
                GEngine->AddOnScreenDebugMessage(6, 0.f, FColor::White,
                    TEXT("교점: 없음 (평행)"));
        }
        return;
    }

    float t = FVector::DotProduct(N, VA - VP) / dn;
    if (t < 0.f || t > 1.f)
    {
        if (GEngine)
            GEngine->AddOnScreenDebugMessage(6, 0.f, FColor::White,
                TEXT("교점: 없음 (선분 범위 밖)"));
        return;
    }

    FVector I = VP + t * D;

    FVector Cross1 = FVector::CrossProduct(VB - VA, I - VA);
    FVector Cross2 = FVector::CrossProduct(VC - VB, I - VB);
    FVector Cross3 = FVector::CrossProduct(VA - VC, I - VC);

    bool bInside = FVector::DotProduct(N, Cross1) >= 0.f
                && FVector::DotProduct(N, Cross2) >= 0.f
                && FVector::DotProduct(N, Cross3) >= 0.f;

    if (bInside)
    {
        bHasIntersect  = true;
        IntersectPoint = I;
        DrawDebugSphere(GetWorld(), I, 20.f, 16, FColor::Red, false, -1.f);
    }
    else
    {
        if (GEngine)
            GEngine->AddOnScreenDebugMessage(6, 0.f, FColor::White,
                TEXT("교점: 없음 (삼각형 밖)"));
    }
}
```

---

## 🎮 사용 방법

### 액터 배치

```
1. 콘텐츠 드로어 (Ctrl + Space)
2. C++ 클래스 → MyNewProject → RayTriangleActor
3. 뷰포트로 드래그 앤 드롭
4. 위치: X=0, Y=0, Z=0
5. Ctrl + S 저장
```

### 플레이 조작법

|동작|방법|
|---|---|
|플레이 시작|`Alt + P`|
|마우스 커서 활성화|`Shift + F1`|
|구(점) 드래그|마우스 좌클릭 후 드래그|
|좌표 직접 입력|우측 입력창에 숫자 입력|
|플레이 종료|`Esc`|

### 화면 구성

```
┌─────────────────────────────────┐
│ 좌측 상단: 실시간 좌표값        │
│   A: (x, y, z)  ← 보라색       │
│   B: (x, y, z)  ← 초록색       │
│   C: (x, y, z)  ← 주황색       │
│   P: (x, y, z)  ← 파란색       │
│   Q: (x, y, z)  ← 갈색         │
│   교점: (x,y,z) ← 빨간색       │
│                                 │
│ 우측: 좌표 입력 패널            │
│   A  [x] [y] [z]               │
│   B  [x] [y] [z]               │
│   C  [x] [y] [z]               │
│   P  [x] [y] [z]               │
│   Q  [x] [y] [z]               │
└─────────────────────────────────┘
```

### 시각적 표현

| 색깔       | 의미           |
| -------- | ------------ |
| 🟡 노란색 선 | 삼각형 변        |
| 🔵 파란색 선 | 선분 P→Q       |
| 🟣 보라색 구 | A 꼭짓점        |
| 🟢 초록색 구 | B 꼭짓점        |
| 🟠 주황색 구 | C 꼭짓점        |
| 🔴 빨간색 구 | 교점           |
| ⚪ 흰색 구   | 선택(드래그 중)인 점 |

---

## ⚠️ 트러블슈팅

### 빌드 오류 모음

> [!bug] 한글 경로 오류
> 
> ```
> fatal error C1083: 소스 파일을 열 수 없습니다
> ```
> 
> **해결:** 프로젝트를 `C:\Users\user\MyNewProject` 같은 영문 경로로 이동

> [!bug] VisualStudioTools 오류
> 
> ```
> Expecting to find a type to be declared in a module rules named 'VisualStudioTools'
> ```
> 
> **해결:** `.uproject` 파일에서 `"Enabled": true → false` 변경

> [!bug] 변수 이름 충돌
> 
> ```
> 'A': 선언되지 않은 식별자
> ```
> 
> **해결:** `FVector::CrossProduct(A, B)` 의 매개변수명이 멤버변수 A, B와 충돌 → 지역변수를 `VA, VB, VC` 로 이름 변경

> [!bug] UWidgetTree 접근 오류 (UE 5.7)
> 
> ```
> TObjectPtr<UWidgetTree> → UObject* 변환 불가
> ```
> 
> **해결:** `UIWidget->WidgetTree` → `UIWidget->WidgetTree.Get()` 로 변경

> [!bug] 추상 클래스 오류
> 
> ```
> 추상(Abstract) 클래스는 유저 위젯 구성 시 사용할 수 없습니다
> ```
> 
> **해결:** `WidgetTree.Get()` 으로 위젯 트리 직접 접근 후 `NewObject` 사용

### 플레이 관련

> [!tip] 액터가 안 보일 때
> 
> - 아웃라이너 검색창에 `Ray` 입력
> - 찾은 액터 클릭 후 `F` 키 (카메라 이동)
> - WorldPartition 때문에 멀리 있으면 로드 안 됨 → 위치를 `(0,0,0)` 근처로

> [!tip] 플레이 종료 시 저장 창
> 
> - **저장 안 함** 권장 (플레이 중 드래그한 위치가 저장되면 다음 실행 시 엉뚱한 위치에서 시작)

> [!tip] VS에서 빨간 줄 오류
> 
> - VS의 IntelliSense 오류는 **무시해도 됨**
> - 실제 빌드는 **언리얼 에디터**에서 `Ctrl + Alt + F11` 로 수행

---

## 📐 알고리즘 설명

### 선분-삼각형 교차 판정 (Möller–Trumbore 방식)

> [!note] 핵심 공식
> 
> ```
> 1단계: 삼각형 법선벡터
>    N = (B-A) × (C-A)
> 
> 2단계: 선분과 평면의 평행 여부
>    dn = N · (Q-P)
>    |dn| < ε  →  평행 (교점 없음 또는 무한개)
> 
> 3단계: 매개변수 t 계산
>    t = N · (A-P) / dn
>    t < 0 또는 t > 1  →  선분 범위 밖
> 
> 4단계: 교점 계산
>    I = P + t*(Q-P)
> 
> 5단계: 삼각형 내부 판정 (외적 부호 검사)
>    C1 = (B-A) × (I-A)
>    C2 = (C-B) × (I-B)
>    C3 = (A-C) × (I-C)
>    N·C1 ≥ 0, N·C2 ≥ 0, N·C3 ≥ 0  →  내부 ✅
> ```

### 교차 판정 결과 분류

|상황|조건|결과|
|---|---|---|
|평행|`|dn|
|동일 평면|`|dn|
|선분 범위 밖|`t < 0 or t > 1`|교점 없음 (범위 밖)|
|삼각형 밖|외적 부호 불일치|교점 없음 (삼각형 밖)|
|교점 존재|모든 조건 통과|교점 1개 ✅|

### 드래그 구현 원리

> [!note] 마우스 드래그로 점 이동
> 
> ```
> 1. 마우스 클릭 시
>    → DeprojectMousePositionToWorld() 로 마우스 레이 계산
>    → 레이와 각 점 사이 거리 계산
>    → 가장 가까운 점 선택 (반경 50cm 이내)
> 
> 2. 드래그 중
>    → 카메라를 향하는 평면 위에서 점 이동
>    → 매 프레임 좌표 업데이트
> 
> 3. 마우스 버튼 놓으면
>    → 드래그 종료, 선택 해제
> ```

---

## 🔧 빌드 방법

```
코드 수정
  ↓
Ctrl + S (Visual Studio 저장)
  ↓
언리얼 에디터로 이동
  ↓
Ctrl + Alt + F11 (Live Coding 빌드)
  ↓
하단에 "Live coding succeeded" 확인
  ↓
Alt + P (플레이 테스트)
```

> [!warning] 주의사항
> 
> - 언리얼 에디터에서 **직접 빌드** (`Ctrl + Alt + F11`)
> - Visual Studio에서 `Ctrl + Shift + B` 로 빌드하면 안 됨
> - 헤더 파일(`.h`) 변경 시 전체 재빌드 필요 (시간이 더 걸림)

---

_작성일: 2026-05-28_ _엔진 버전: UE 5.7.4_ _Visual Studio: 2022 Community 14.44_