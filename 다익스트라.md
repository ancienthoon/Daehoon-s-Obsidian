# 📘 그래프 — 다익스트라 알고리즘 (Dijkstra's Algorithm)

---

## 🔖 목차

1. [[#개념 및 배경]]
2. [[#핵심 아이디어]]
3. [[#동작 과정 단계별]]
4. [[#예제 손으로 풀기]]
5. [[#코드 구현 C++]]
6. [[#시간 복잡도]]
7. [[#한계와 주의사항]]
8. [[#핵심 정리]]

---

## 📌 개념 및 배경

> **다익스트라 알고리즘**: 하나의 시작 정점에서 다른 모든 정점까지의 **최단 경로**를 구하는 알고리즘

- 1956년 에츠허르 다익스트라(Edsger Dijkstra)가 고안
- **가중치 그래프**에서 동작 (방향/무방향 모두 가능)
- **탐욕 알고리즘(Greedy)** 기반 — 매 단계에서 가장 가까운 정점을 선택

### 사용 예시

```
GPS 내비게이션, 네트워크 라우팅, 게임 맵 경로 탐색
```

### 그래프 용어 복습

|용어|설명|
|---|---|
|정점 (Vertex)|노드, 교차점|
|간선 (Edge)|두 정점을 잇는 선|
|가중치 (Weight)|간선의 비용 (거리, 시간 등)|
|최단 경로|시작→도착까지 가중치 합이 최소인 경로|

---

## 📌 핵심 아이디어

> **"지금까지 확정된 최단 거리 중 가장 짧은 정점을 골라서, 그 정점을 통해 이웃을 갱신한다"**

### 완화 (Relaxation) 조작

```
현재까지 알려진 dist[v] 보다
dist[u] + weight(u, v) 가 더 작으면
  → dist[v] = dist[u] + weight(u, v) 로 갱신
```

```
if (dist[u] + w < dist[v]):
    dist[v] = dist[u] + w
```

---

## 📌 동작 과정 단계별

```
① 시작 정점의 거리 = 0, 나머지 = INF(∞)로 초기화
② 미방문 정점 중 dist 가 가장 작은 정점 u 선택
③ u 를 방문 처리 (확정)
④ u 의 모든 인접 정점 v 에 대해 완화 수행
   if dist[u] + w(u,v) < dist[v]:
       dist[v] = dist[u] + w(u,v)
⑤ 모든 정점이 방문될 때까지 ②~④ 반복
```

### 상태 추적에 필요한 자료구조

|자료|설명|
|---|---|
|`dist[]`|시작점에서 각 정점까지의 현재 최단 거리|
|`visited[]`|해당 정점이 확정(방문)됐는지 여부|
|`prev[]`|최단 경로 역추적용 이전 정점 (선택)|
|우선순위 큐|다음에 선택할 정점을 빠르게 찾기 위해 사용|

---

## 📌 예제 손으로 풀기

### 그래프

```
      2       3
  A ───── B ───── C
  │       │       │
 6│      8│      7│
  │       │       │
  D ───── E ───── F
      1       4
```

간선 목록: A-B(2), A-D(6), B-C(3), B-E(8), C-F(7), D-E(1), E-F(4)

### 시작: A, 초기 dist

|정점|A|B|C|D|E|F|
|---|---|---|---|---|---|---|
|dist|0|∞|∞|∞|∞|∞|

### 단계별 진행

**Step 1 — A 선택 (dist=0)**

```
A→B: 0+2=2  < ∞  → dist[B] = 2
A→D: 0+6=6  < ∞  → dist[D] = 6
```

| A✅ | B=2 | C=∞ | D=6 | E=∞ | F=∞ |

**Step 2 — B 선택 (dist=2, 미방문 중 최소)**

```
B→C: 2+3=5  < ∞  → dist[C] = 5
B→E: 2+8=10 < ∞  → dist[E] = 10
```

| A✅ | B✅ | C=5 | D=6 | E=10 | F=∞ |

**Step 3 — C 선택 (dist=5)**

```
C→F: 5+7=12 < ∞  → dist[F] = 12
```

| A✅ | B✅ | C✅ | D=6 | E=10 | F=12 |

**Step 4 — D 선택 (dist=6)**

```
D→E: 6+1=7  < 10 → dist[E] = 7  ← 갱신!
```

| A✅ | B✅ | C✅ | D✅ | E=7 | F=12 |

**Step 5 — E 선택 (dist=7)**

```
E→F: 7+4=11 < 12 → dist[F] = 11 ← 갱신!
```

| A✅ | B✅ | C✅ | D✅ | E✅ | F=11 |

**Step 6 — F 선택 (dist=11) → 완료**

### 최종 최단 거리

|시작(A) →|B|C|D|E|F|
|---|---|---|---|---|---|
|최단 거리|2|5|6|7|11|

---

## 📌 코드 구현 C++

### 우선순위 큐 사용 (효율적)

```cpp
#include <iostream>
#include <vector>
#include <queue>
#include <climits>
using namespace std;

typedef pair<int,int> pii;   // {거리, 정점}

void dijkstra(int start, vector<vector<pii>>& graph, int n) {
    vector<int> dist(n + 1, INT_MAX);
    priority_queue<pii, vector<pii>, greater<pii>> pq;  // 최소 힙

    dist[start] = 0;
    pq.push({0, start});

    while (!pq.empty()) {
        auto [d, u] = pq.top();
        pq.pop();

        // 이미 더 짧은 경로로 처리된 정점은 스킵
        if (d > dist[u]) continue;

        for (auto [w, v] : graph[u]) {
            if (dist[u] + w < dist[v]) {       // 완화
                dist[v] = dist[u] + w;
                pq.push({dist[v], v});
            }
        }
    }

    // 결과 출력
    for (int i = 1; i <= n; i++) {
        cout << "dist[" << i << "] = ";
        if (dist[i] == INT_MAX) cout << "INF\n";
        else cout << dist[i] << "\n";
    }
}

int main() {
    int n = 6, m = 7;   // 정점 수, 간선 수
    vector<vector<pii>> graph(n + 1);

    // 무방향 그래프 간선 추가
    // graph[u].push_back({가중치, v});
    graph[1].push_back({2, 2});  graph[2].push_back({2, 1});  // A-B
    graph[1].push_back({6, 4});  graph[4].push_back({6, 1});  // A-D
    graph[2].push_back({3, 3});  graph[3].push_back({3, 2});  // B-C
    graph[2].push_back({8, 5});  graph[5].push_back({8, 2});  // B-E
    graph[3].push_back({7, 6});  graph[6].push_back({7, 3});  // C-F
    graph[4].push_back({1, 5});  graph[5].push_back({1, 4});  // D-E
    graph[5].push_back({4, 6});  graph[6].push_back({4, 5});  // E-F

    dijkstra(1, graph, n);
    return 0;
}
```

### 경로 역추적 추가

```cpp
vector<int> prev(n + 1, -1);

// 완화 시 prev 갱신
if (dist[u] + w < dist[v]) {
    dist[v] = dist[u] + w;
    prev[v] = u;           // ← 이전 정점 기록
    pq.push({dist[v], v});
}

// 경로 출력 함수
void printPath(int target, vector<int>& prev) {
    if (prev[target] == -1) { cout << target; return; }
    printPath(prev[target], prev);
    cout << " → " << target;
}
```

---

## 📌 시간 복잡도

|구현 방법|시간 복잡도|비고|
|---|---|---|
|배열로 최솟값 탐색|O(V²)|정점 수 적을 때 단순|
|우선순위 큐 (힙)|**O((V + E) log V)**|일반적으로 사용|

- V = 정점 수, E = 간선 수
- **밀집 그래프** (E ≈ V²): 배열 방식이 빠를 수도 있음
- **희소 그래프** (E << V²): 우선순위 큐 방식 권장

---

## 📌 한계와 주의사항

### ⚠️ 음수 가중치 불가

```
다익스트라는 음수 간선이 있으면 틀린 결과를 냄
음수 가중치 → 벨만-포드(Bellman-Ford) 알고리즘 사용
```

### ⚠️ 음수 사이클 감지 불가

```
음수 사이클(돌수록 거리가 줄어드는 사이클) 존재 시 무한 루프
```

### 정상 동작 조건 요약

|조건|다익스트라|
|---|---|
|가중치 모두 양수|✅ 정상|
|가중치 0 포함|✅ 정상|
|음수 가중치 포함|❌ 오답 가능|
|음수 사이클|❌ 사용 불가|

---

## 🗂️ 핵심 정리

```
다익스트라 = 탐욕 + 완화(Relaxation)
  ① 시작점 dist = 0, 나머지 = INF
  ② 미방문 중 최솟값 정점 u 선택
  ③ u의 이웃 v에 대해:
     dist[u] + w < dist[v] 이면 갱신
  ④ 반복
```

### 알고리즘 선택 가이드

|상황|알고리즘|
|---|---|
|단일 출발, 양수 가중치|**다익스트라**|
|단일 출발, 음수 가중치|벨만-포드|
|모든 쌍 최단 경로|플로이드-워셜|
|장애물 있는 격자 맵|A*|

### ⚠️ 최다 실수 포인트

|실수|올바른 처리|
|---|---|
|음수 가중치에 사용|벨만-포드로 교체|
|`dist[u] > d` 체크 생략|중복 처리로 속도 저하, 반드시 스킵|
|INT_MAX에 덧셈|오버플로우 → `if (dist[u] != INF)` 조건 확인|

---

> 📅 작성일: 2026-05-18 🏫 과목: 자료구조 / 알고리즘 📖 단원: 그래프 — 최단 경로

#알고리즘 #그래프 #다익스트라 #최단경로 #dijkstra