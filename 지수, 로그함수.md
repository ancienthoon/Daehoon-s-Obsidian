# 지수함수와 로그함수

> [!info] 강의 정보
> 
> - **주제**: 거듭제곱근, 지수법칙, 지수함수의 그래프, 자연상수 $e$, 로그함수와 그 성질
> - **연관 노트**: [[다항함수|함수의_기초_다항함수]] · [[함수와 집합|딥러닝_수학_함수와_집합]] · [[삼각함수|삼각함수와_삼각비]]

---

## 🎯 학습 목표

- [ ] 거듭제곱근의 정의를 이해한다 ($x^n = a \Rightarrow x = \sqrt[n]{a} = a^{1/n}$)
- [ ] 지수법칙 5가지를 적용한다
- [ ] 지수함수 $y=a^x$의 그래프를 밑 $a$의 범위별로 구분한다
- [ ] 자연상수 $e$의 정의를 안다
- [ ] 로그함수가 지수함수의 역함수임을 이해한다
- [ ] 로그의 성질(곱·거듭제곱·나눗셈)을 증명한다

---

## PART 1 — 거듭제곱근과 지수법칙

### 거듭제곱근의 정의

> [!question] $x$를 2번 곱했을 때 $a$가 되었다면? $$x \cdot x = a ;\Rightarrow; x^2 = a$$ $$x = \sqrt{a} = a^{\frac{1}{2}} \quad (x:, a\text{의 제곱근})$$

> [!question] $x$를 $n$번 곱했을 때 $a$가 되었다면? $$x^n = a ;\Rightarrow; x = \sqrt[n]{a} = a^{\frac{1}{n}}$$

---

### 지수법칙 5가지

|법칙|식|
|---|---|
|곱셈|$a^m \cdot a^n = a^{m+n}$|
|거듭제곱의 거듭제곱|$(a^m)^n = a^{mn}$|
|곱의 거듭제곱|$(ab)^m = a^m b^m$|
|음의 지수|$a^{-n} = \dfrac{1}{a^n}$|
|0 지수|$a^0 = 1$|

> [!note] $(a^m)^n = a^{mn}$의 증명 $$(a^m)^n = \underbrace{a^m \cdot a^m \cdots a^m}_{n\text{번}} = a^{\overbrace{m+m+\cdots+m}^{n\text{번}}} = a^{mn}$$

> [!note] $(ab)^m = a^m b^m$의 증명 $$(ab)^m = \underbrace{(ab)(ab)\cdots(ab)}_{m\text{번}} = \underbrace{(aa\cdots a)}_{m}\underbrace{(bb\cdots b)}_{m} = a^m b^m$$ (실수 곱셈의 교환법칙 이용)

> [!tip] 음의 지수 = 곱셈의 역원 $a^{-1} = \dfrac{1}{a}$ (곱셈의 역원)

---

## PART 2 — 지수함수

### 정의

$$y = a^x \quad (a>0,; a \neq 1)$$

### 예 1: $a = 2$, $y = 2^x$

|x|$2^x$|
|---|---|
|0|1|
|1|2|
|2|4|
|-1|1/2|
|-2|1/4|

그래프: x가 증가할수록 급격히 증가하는 우상향 곡선, x축을 향해 점근

> [!important] 지수함수의 함숫값 $a^x$는 항상 **양수**이다

### 예 2: $a = \frac{1}{2} < 1$, $y = (\frac{1}{2})^x$

$$\left(\frac{1}{2}\right)^{-2} = (2^{-1})^{-2} = 2^2 = 4$$ $$\left(\frac{1}{2}\right)^{-1} = 2, \quad \left(\frac{1}{2}\right)^0 = 1, \quad \left(\frac{1}{2}\right)^1 = \frac{1}{2}$$

그래프: x가 증가할수록 감소하는 **우하향** 곡선

---

### 밑 $a$에 따른 그래프 형태 비교

|$a > 1$|$0 < a < 1$|
|---|---|
|오른쪽으로 갈수록 급격히 증가|오른쪽으로 갈수록 0에 수렴 (감소)|
|$f(x)=2^x < g(x)=3^x$ (x>0일 때 $3^x$가 더 가파름)|왼쪽이 더 크고, 오른쪽으로 갈수록 0에 근접|

---

### 자연상수 $e$

> [!important] 자연상수의 정의 $$e \equiv \lim_{x\to\infty}\left(1+\frac{1}{x}\right)^x ;=; \lim_{x\to 0}(1+x)^{\frac{1}{x}}$$
> 
> **Jakob Bernoulli**가 발견, **Leonhard Euler**가 $e$로 표기

### 시그모이드 함수 (예고)

$$\sigma(x) = \frac{1}{1+e^{-x}}$$

→ 딥러닝의 활성화 함수로 사용되는 "시그모이드" 함수

---

## PART 3 — 로그함수

### 로그함수의 정의 (지수함수의 역함수)

> [!important] 왜 로그함수가 필요한가? 지수함수의 값이 너무 커서 **다루기 힘들다** → 역함수로 스케일을 줄인다

```
X                    Y
    ──────f: aˣ─────▶
x   ◀─────f⁻¹────────  y

y = aˣ  ⟺  x = logₐ y
```

**지수함수의 역함수 = 로그함수**

$$\log_a x \longleftarrow x$$

**기본 항등식** $$\log_a(a^x) = x, \qquad a^{\log_a x} = x$$

---

### 로그의 성질

|성질|식|
|---|---|
|$\log_a 1 = 0$|$a^x=1 \Rightarrow x=0$|
|$\log_a a = 1$|$a^x=a \Rightarrow x=1$|
|**곱의 로그**|$\log_a(MN) = \log_a M + \log_a N$|
|**거듭제곱의 로그**|$\log_a(M^k) = k\log_a M$|
|**나눗셈의 로그**|$\log_a(M/N) = \log_a M - \log_a N$|

### 곱의 로그 성질 증명

> [!example] $\log_a(MN) = \log_a M + \log_a N$
> 
> $\alpha = \log_a M,; \beta = \log_a N$ 라 하면 $$a^\alpha = M, \quad a^\beta = N$$ $$M \cdot N = a^\alpha \cdot a^\beta = a^{\alpha+\beta}$$ $\gamma = \alpha+\beta,; L = M \cdot N$ 라 하면 $$L = a^\gamma ;\Leftrightarrow; \log_a L = \gamma$$ $$\therefore \log_a(MN) = \alpha+\beta = \log_a M + \log_a N ;\blacksquare$$

### 나눗셈의 로그 성질 증명

> [!example] $\log_a(M/N) = \log_a M - \log_a N$
> 
> $$\log_a(M/N) = \log_a\left(M \cdot \frac{1}{N}\right) = \log_a M + \log_a(N^{-1})$$ $$= \log_a M + (-1)\log_a N = \log_a M - \log_a N ;\blacksquare$$

---

### 로그함수의 그래프

$y=e^x$, $y=x$, $y=\log_a x$를 함께 그리면:

- $y=e^x$와 $y=\log_a x$는 **직선 $y=x$에 대해 대칭** (서로 역함수 관계이므로)

### 자연로그함수

$$a = e \text{인 로그함수}: \quad y = \ln x = \log_e x$$

---

## 📌 핵심 요약

> [!summary] 오늘의 핵심 **① 거듭제곱근**: $x^n = a \Rightarrow x = a^{1/n}$
> 
> **② 지수법칙**: $a^m a^n=a^{m+n}$, $(a^m)^n=a^{mn}$, $(ab)^m=a^mb^m$, $a^{-n}=1/a^n$, $a^0=1$
> 
> **③ 지수함수 $a^x$**: 함숫값은 항상 양수. $a>1$이면 증가, $0<a<1$이면 감소
> 
> **④ 자연상수 $e$**: $\lim_{x\to\infty}(1+1/x)^x$
> 
> **⑤ 로그함수 = 지수함수의 역함수**: $y=a^x \Leftrightarrow x=\log_a y$
> 
> **⑥ 로그 성질**: 곱→합, 나눗셈→차, 거듭제곱→계수로 이동
> 
> **⑦ 그래프**: $y=a^x$와 $y=\log_a x$는 $y=x$에 대해 대칭

---

#딥러닝기초 #지수함수 #로그함수 #자연상수 #거듭제곱근 #지수법칙 #시그모이드