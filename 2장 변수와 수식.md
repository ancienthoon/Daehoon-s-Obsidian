---
tags: [2학기전공과목]
---

# 🐍 2장. 변수와 수식

> [!info] 학습 목표
> 
> - 변수와 상수를 정의하고 사용할 수 있다
> - 주석의 개념을 이해한다
> - 산술 연산자와 할당 연산자를 이해한다
> - 연산자의 우선순위를 이해한다
> - 사용자로부터 입력을 받고 출력하는 프로그램을 작성할 수 있다
> - 문자열의 기초 연산을 이해한다

---

## 1️⃣ 파이썬 vs C++ 차이점

|항목|C++|파이썬|
|---|---|---|
|main()|필요|없음, 코드가 위에서부터 순서대로 실행|
|변수 타입|반드시 지정 (`int x`)|지정 안 함|
|문장 끝 `;`|사용|사용 안 함 (써도 오류는 아님)|

```python
x = 10.5
y = 6.5
print(x, y)          # 10.5 6.5

x = 13
y = 4
z1 = x / y   # 나누기   → 3.25
z2 = x // y  # 몫      → 3
z3 = x % y   # 나머지  → 1
```

---

## 2️⃣ 변수(Variable)

> [!note] 정의 변수는 **컴퓨터 메모리 공간에 이름을 붙인 것**. 여기에 값을 저장할 수 있다. 비유: 이름표가 붙은 **사물함(상자)** 에 물건(값)을 넣는 것

### 변수 정의 문법

```
변수이름 = 값
```

파이썬은 **변수에 값을 저장하는 순간 변수가 자동 생성**된다.

### 🔑 C++ vs 파이썬의 변수 저장 방식 (중요!)

|C++|파이썬|
|---|---|
|변수 = 상자, 상자 안에 값이 직접 저장|값은 **객체**로 메모리에 저장되고, 변수는 그 객체를 **참조(주소)** 만 가짐|
|`int x = 100; x = 200;` → 같은 상자 안 내용물이 바뀜|`x = 100` → x가 100 객체를 가리킴<br>`x = 200` → x가 200 객체를 가리키도록 **변경** (100 객체는 그대로 존재)|

> [!tip] 핵심 이미지 파이썬 변수 x는 **포인터(참조)** 라고 생각하면 이해가 쉽다.
> 
> ```
> >>> x = 100      # 100 객체 생성, x가 100을 가리킴
> >>> x = 200      # 200 객체 생성, x가 200을 가리키도록 변경
> ```

### 예제: 두 수의 합

```python
x = 100          # 변수 x 생성, 100을 가리킴
y = 200          # 변수 y 생성, 200을 가리킴
sum = x + y      # 변수 sum 생성, 300을 가리킴
print("합은", sum)   # 합은 300
```

### 재대입도 가능

```python
score = 10
score = score + 1   # score가 11을 가리키도록 재설정
```

---

## 3️⃣ 변수 이름 규칙

- 의미 있는 이름 사용 권장
- **대소문자 구분**됨 (`Score` ≠ `score`)
- 영문자, 숫자, 밑줄(`_`)만 사용 가능
- 중간에 공백 불가 → 단어 구분은 `_` 사용

|변수 이름|가능 여부|설명|
|---|---|---|
|`size`|✅|정상|
|`cloud9`|✅|영문자+숫자+_ 조합|
|`max_size`|✅|중간에 `_` 가능|
|`_count`|✅|`_`로 시작 가능 (클래스 내부용 의미도 있음)|
|`6pack`|❌|숫자로 시작 불가|
|`mid score`|❌|중간 공백 불가|
|`class`|❌|예약어 사용 불가|
|`money#`|❌|기호 사용 불가|

### 🐫 낙타체(camelCase)

첫 글자는 소문자, 이후 단어의 첫 글자만 대문자로 표기

```
myNewCar
```

---

## 4️⃣ 자료형(Data Type)

```
표준 자료형
├─ 숫자: 정수(int), 부동소수점(float), 복소수
├─ 시퀀스: 튜플, 리스트, 문자열(str)
└─ 부울형
```

|자료형|예|
|---|---|
|정수(int)|..., -2, -1, 0, 1, 2, ...|
|부동소수점(float)|3.2, 3.14, 0.12|
|문자열(str)|"Hello World!", "123"|

> [!warning] 주의 파이썬은 한 변수에 어떤 자료형이든 저장 가능하지만(타입 자유), **바람직하지 않음**
> 
> ```python
> radius = 10
> radius = 10.003
> radius = "Unknown"   # 가능은 하지만 지양
> ```

### 자료형 확인: `type()`

```python
>>> type(1234)
<class 'int'>
>>> type(12.30)
<class 'float'>
>>> type("hello")
<class 'str'>
```

---

## 5️⃣ 변수의 참조값과 `id()` 함수

> [!note] 핵심 개념 변수에 저장되는 것은 **실제 값이 아니라 객체의 참조값(주소)** 이다. `id(변수)` → 그 객체의 고유 주소값 반환

```python
>>> x = 3
>>> id(x)
140721955779472
```

### 변수를 복사할 때

```python
x = 3
y = x        # y에 x의 "참조값"이 복사됨 (같은 객체를 가리킴)
id(x) == id(y)   # True, 같은 주소

y = 10       # y에 새 값 할당 → y는 새로운 객체(10)를 가리킴, 주소 달라짐
```

---

## 6️⃣ 불변 객체 vs 가변 객체 ⭐ (시험 포인트)

```
자료형
├─ 불변 객체(Immutable): 숫자, 문자열, 튜플 → 값이 변하지 않음
└─ 가변 객체(Mutable): 리스트, 딕셔너리, 세트 → 값이 변함
```

### 불변 객체 예시

```python
d = 30
print(id(d))     # 2000번지
d = d + 1
print(id(d))     # 3000번지 ← 주소가 바뀜!
```

> [!important] `+` 연산을 해도 기존 30 객체가 31로 바뀌는 게 아니라, **새로운 객체(31)가 생성**되고 변수가 그걸 가리키게 됨. → 즉, 값이 다르면 서로 다른 객체.

### 가변 객체 예시

```python
a = [1, 2, 3, '안녕']
print(id(a))          # 1000번지
a.remove('안녕')
print(a)               # [1, 2, 3]
print(id(a))          # 1000번지 ← 주소 그대로!
```

> [!important] 리스트 등 가변 객체는 연산 후에도 **같은 객체의 내용만 변경**됨 (주소 안 바뀜).

---

## 7️⃣ 주석(Comment)

|형태|문법|
|---|---|
|한 줄 주석|`# 주석 내용`|
|여러 줄 주석|`""" 주석 내용 """` (또는 `'''...'''`)|

```python
ftemp = 100                        # 화씨 온도 100을 변수에 저장한다
ctemp = (ftemp - 32.0) * 5.0 / 9.0  # 화씨온도 -> 섭씨온도
print("섭씨온도:", ctemp)

"""
여러 문장들 주석 처리
print(id(a))
print(id(b))
"""
```

---

## 8️⃣ 상수(Constant)

> [!note] 파이썬에는 C++의 `const`, `#define` 같은 진짜 상수 기능이 **없음**. 관례적으로 **변수 이름을 모두 대문자**로 써서 "이건 상수야"라고 구분한다.

```python
INCOME = 1000       # 상수(관례)
TAX_RATE = 0.35

tax = INCOME * TAX_RATE
net_income = INCOME - tax
```

---

## 9️⃣ 산술 연산자

|연산자|기호|사용예|결과값|
|---|---|---|---|
|덧셈|`+`|`7 + 4`|11|
|뺄셈|`-`|`7 - 4`|3|
|곱셈|`*`|`7 * 4`|28|
|정수 나눗셈(몫)|`//`|`7 // 4`|1|
|실수 나눗셈|`/`|`7 / 4`|1.75|
|나머지|`%`|`7 % 4`|3|
|거듭제곱|`**`|`2 ** 7`|128|

### 활용 예제: 요일 계산

```python
today = 0
print((today + 10) % 7)   # 오늘부터 10일 후는 무슨 요일?  → 3
```

### 활용 예제: 원리금 계산

```python
a = 1000        # 원금
r = 0.05        # 이자율
n = 10          # 기간
result = a * (1 + r) ** n   # a(1+r)^n
print("원리금 합계=", result)   # 1628.894626777442
```

---

## 🔟 할당 연산과 복합 연산자

### 다중 할당

```python
x = y = z = 0
x, y, z = 10, 20, 30   # 한 번에 여러 변수 초기화
x, y = y, x            # x와 y의 값을 서로 교환 (스왑!)
```

### 복합 연산자

|복합 연산자|의미|
|---|---|
|`x += y`|`x = x + y`|
|`x -= y`|`x = x - y`|
|`x *= y`|`x = x * y`|
|`x /= y`|`x = x / y`|
|`x %= y`|`x = x % y`|

---

## 1️⃣1️⃣ 연산자 우선순위

```
x + y * z   →  ① y*z 먼저   ② 그다음 +
(x + y) * z →  ① 괄호 안 먼저 ② 그다음 *
```

> [!tip] 괄호를 쓰면 우선순위를 강제로 바꿀 수 있다
> 
> ```python
> >>> 10 + 20 / 2
> 20.0
> >>> (10 + 20) / 2
> 15.0
> ```

### 전체 우선순위표 (위 → 아래, 높은 순)

|연산자|설명|
|---|---|
|`**`|지수 연산자|
|`~, +, -`|단항 연산자|
|`*, /, %, //`|곱셈·나눗셈·나머지|
|`+, -`|덧셈·뺄셈|
|`>>, <<`|비트 이동|
|`&`|비트 AND|
|`^, \|`|비트 XOR, OR|
|`<=, <, >, >=`|비교 연산자|
|`<>, ==, !=`|동등 연산자|
|`=, %=, /=, //=, -=, +=, *=, **=`|대입/복합 연산자|
|`is, is not`|동등(정체성) 연산자|
|`in, not in`|소속 연산자|
|`not, or, and`|논리 연산자|

---

## 1️⃣2️⃣ 타입 변환(Type Casting)

```
새로운타입(변수)
```

```python
x = 3.14
y = int(x)          # 3.14 → 3
```

> [!note] 재변환 시 동일 객체 참조
> 
> ```python
> x = 3.14
> y = int(x)          # x: 1000번지(3.14), y: 2000번지(3)
> x = int(x)          # 값이 같아지면(3) 동일 객체 → x, y 같은 주소
> ```

### 반올림 — `round()`

```python
price = 12345
tax = price * 0.075
tax = round(tax, 2)   # 소수점 이하 2자리 반올림
print(tax)             # 925.88
```

---

## 1️⃣3️⃣ 문자열(String)

### 큰따옴표 / 작은따옴표

```python
msg = "Hello"
a = 'Hello'
```

> [!warning] 따옴표 중첩 주의
> 
> ```python
> message = "철수가 "안녕"이라고 말했습니다."   # ❌ SyntaxError
> message = "철수가 '안녕'이라고 말했습니다."   # ✅ 큰따옴표 안에 작은따옴표
> ```

### 여러 줄 문자열 — 세 따옴표

```python
a = """TWINKLE, twinkle, little star,
How I wonder what you are!"""
```

### 문자열 결합 / 반복

```python
>>> "Hello " + "World!"
'Hello World!'

>>> lines = "-" * 30
'------------------------------'
```

### 숫자 vs 문자열 구별

```python
>>> print(100 + 200)      # 300 (덧셈)
>>> print("100" + "200")  # "100200" (문자열 결합)
```

### 숫자 ↔ 문자열 변환

```python
>>> movie = "Terminator" + 3
TypeError: can only concatenate str (not "int") to str

>>> movie = "Terminator" + str(3)   # 숫자 → 문자열
'Terminator3'

>>> price = int("100")     # 문자열 → 정수
>>> PI = float("3.14")     # 문자열 → 실수
```

### 특수 문자(이스케이프 시퀀스)

|특수 문자열|의미|
|---|---|
|`\n`|줄바꿈|
|`\t`|탭|
|`\\`|역슬래시 자체|
|`\"`|큰따옴표 자체|
|`\'`|작은따옴표 자체|

### 인덱싱 — 문자 접근

```python
s = "Hello World"
s[0]    # 'H'
s[-1]   # 'd'   ← 음수 인덱스로 뒤에서부터 접근 가능
```

### 예제

```python
a = "Kim"
b = "Park"
acronym = a[0] + "과" + b[0]
print(acronym)   # K과P
```

---

## 1️⃣4️⃣ 문자열은 객체다

> [!info] 객체(object) = **관련 있는 변수(속성) + 함수(메소드)** 를 하나로 묶은 것. 파이썬에서는 **정수, 실수, 문자열도 모두 객체**로 저장된다.

### 문자열 메소드

```python
name = "Harry Parter"
lower_name = name.lower()                    # 'harry parter'
new_name = name.replace("Parter", "Porter")  # 'Harry Porter'
```

---

## 1️⃣5️⃣ 입력 — `input()` 함수

```
변수 = input(안내메시지)
```

> [!important] `input()`은 항상 **문자열**을 반환한다!

```python
name = input("이름을 입력하시오: ")
print(name, "씨, 안녕하세요?")
```

### 🚨 흔한 실수: 문자열 덧셈

```python
x = input("첫 번째 정수:")   # "300"
y = input("두 번째 정수:")   # "400"
sum = x + y
print(sum)   # "300400"  ← 문자열 결합이지 덧셈이 아님!
```

### ✅ 올바른 정수 입력

```python
x = int(input("첫 번째 정수:"))
y = int(input("두 번째 정수:"))
print(x + y)   # 700
```

### 부동소수점 입력

```python
SQMETER_PER_P = 3.3
area = float(input("면적(제곱미터):"))
py = area / SQMETER_PER_P
```

> [!warning] `int()` vs `float()` 변환 차이
> 
> - `float()`: 정수형/소수형 문자열 **둘 다** 변환 가능
> - `int()`: **정수 형태 문자열만** 변환 가능 → `"3.3"`을 `int()`로 변환하면 오류!
> - 결론: 실수 형태는 `float()`로, 정수 형태는 `int()`로 변환

### `eval()` — 문자열을 실행(계산)

```python
e = "5 + 10"
print(eval(e))   # 15

x = eval(input("숫자를 입력하세요: "))   # 입력값을 자동으로 적절한 숫자 타입으로 변환
```

> [!danger] `eval()` 사용 주의 사용자가 악의적인 명령어(예: 파일 삭제 명령)를 입력하면 그대로 실행될 위험 → 사용에 주의 필요

---

## 1️⃣6️⃣ 문자열 포맷팅 (변수와 문자열 함께 출력)

### f-string (권장 방식) ⭐

```python
x = 100
y = 200
print(f"{x}와 {y}의 합={x+y}")   # 100와 200의 합=300
```

### 세 가지 방식 비교

|방식|예시|
|---|---|
|① Old style (`%`)|`"이름: %s, 나이: %d" % (name, age)`|
|② `.format()`|`"이름: {0}, 나이: {1}".format(name, age)`|
|③ f-string (최신, 추천)|`f"이름: {name}, 나이: {age}"`|

### `format()` 세부 옵션

```python
'{0} {1}'.format(1, 2)          # '1 2'
'{1} {0}'.format(1, 2)          # '2 1'   ← 순서 지정 가능
'{0:02d} {1:04d}'.format(1, 2)  # '01 0002'  ← 자릿수 지정(0으로 채움)
'{0:4.2f}'.format(1.2)          # '1.20'  ← 소수점 자리수 지정
```

### 형식화된 출력 — `%` 연산자

```
"형식문자열" % (값1, 값2, ...)
```

```python
p = 7.76
print("%10.2f" % p)   # 전체 10자리, 소수점 이하 2자리
```

---

## 📌 핵심 요약 체크리스트

- [ ] 파이썬은 `main()` 없이 위에서부터 실행되고, 변수 타입을 지정하지 않는다
- [ ] 변수는 값(객체)에 붙는 "이름표"이며, **참조값(주소)** 을 저장한다 → `id()`로 확인
- [ ] **불변 객체**(숫자·문자열·튜플)는 값이 바뀌면 새 객체가 생성됨
- [ ] **가변 객체**(리스트·딕셔너리·세트)는 같은 객체 안에서 내용만 바뀜
- [ ] 상수는 문법이 아니라 **대문자 이름 관례**일 뿐
- [ ] `input()`은 항상 문자열을 반환 → 숫자 계산 전엔 `int()`/`float()` 변환 필수
- [ ] `eval()`은 편리하지만 보안 위험이 있으므로 주의
- [ ] 변수-문자열 혼합 출력은 **f-string**을 우선적으로 사용

---

## 🎯 실습 예제 정리

### Lab 1: 반지름 100픽셀 원 3개 그리기 (변수 활용)

```python
import turtle
t = turtle.Turtle()
t.shape("turtle")

radius = 100
t.circle(radius)
t.fd(30)
t.circle(radius)
t.fd(30)
t.circle(radius)

turtle.mainloop()
turtle.bye()
```

> [!tip] 변수의 유용함 `radius` 변수 하나만 바꾸면 세 원의 크기가 모두 한 번에 바뀐다 → 코드 재사용성 ↑

### Lab 2: 사용자 입력으로 정사각형 그리기

```python
import turtle
t = turtle.Turtle()
t.shape("turtle")

size = int(input("사각형의 크기는 얼마로 할까요? "))

t.forward(size)
t.right(90)
t.forward(size)
t.right(90)
t.forward(size)
t.right(90)
t.forward(size)

turtle.mainloop()
turtle.bye()
```