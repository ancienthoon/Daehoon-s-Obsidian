# 딥러닝 수학 — 함수와 집합, 함수의 그래프

> [!info] 강의 정보
> 
> - **주제**: 딥러닝에서 함수가 왜 중요한가, 집합과 함수의 엄밀한 정의, 함수의 그래프
> - **연관 노트**: [[다항함수|함수의_기초_다항함수]] · [[지수, 로그함수|지수함수와_로그함수]]

---

## 🎯 학습 목표

- [ ] 딥러닝 학습의 목표(비용함수 최소화)와 함수의 관계를 이해한다
- [ ] 집합과 함수를 엄밀하게 정의한다
- [ ] 대응 규칙을 식으로 표현하는 이유를 설명한다
- [ ] 함수의 그래프가 좌표평면 위 점들의 집합임을 이해한다

---

## 딥러닝 수학의 목표

> [!important] 딥러닝 학습의 본질 **주어진 비용함수의 최솟값을 찾는 문제**
> 
> - 최솟값을 갖도록 하는 것 = **독립변수 (매개변수)**
> - 독립변수가 정해지면 → **딥러닝 시스템이 결정**된다

즉, 딥러닝 학습 = 함수의 최솟값을 찾는 수학 문제

---

## 함수란 무엇인가?

> [!note] 함수의 정의 **집합 사이의 대응 관계**, 단 **특정한 성질을 만족**해야 한다

```
     X                    Y
  (정의구역)      f      (공역)
   ○ a  ─────────────▶ ● 1
   ○     ─────────────▶ ● x
                하나씩 대응
```

함수 이름: function → $f, g, \dots$

---

## 집합(Set)이란?

> [!important] 집합의 정의 **특정한 조건을 만족하는 대상들의 모임**

|예시|집합인가?|
|---|---|
|아름다운 사람들의 모임|❌ (조건이 모호)|
|딥러닝 수학을 수강하는 학생들의 모임|✅ (조건이 명확)|

**원소 표기**: $a \in A$ (원소 $a$는 집합 $A$에 속한다)

---

## 함수(Function)의 엄밀한 정의

> [!important] 함수의 정의 집합 $X$의 **각 원소**를 집합 $Y$의 **어떤 원소 하나**에 대응시키는 관계

### 함수가 되기 위한 조건

```
X          Y
a ●───────▶● 1
b ●───┐    ● 2
c ●   └───▶● 3
d ●───────▶● 4
e ●───────▶  (대응 없음 → 함수 아님!)
```

1. 집합 $X$의 원소 중에서 **대응되지 않는 원소는 없다**
2. $X$의 원소 하나에는 **하나의 $Y$만** 대응된다 (다대일은 가능, 일대다는 불가)

---

## 원소가 무한히 많을 때 — 규칙으로 표현

> [!question] 문제 상황 집합 $X$의 원소 개수가 무한히 많다면, 화살표를 다 그릴 수 없다. → **규칙**을 찾아서 식으로 표현하자!

### 예 1: $f(x) = 2x$

```
ℕ                  ℝ
1  ───────────────▶ 2
2  ───────────────▶ 4
3  ───────────────▶ 6
4  ───────────────▶ 8
⋮                   ⋮
```

### 예 2: $f(x) = x^2 - 1$

|x|-1|0|1|2|
|---|---|---|---|---|
|$f(x)=x^2-1$|0|-1|0|3|

---

## 함수의 그래프 — 최솟값을 구하기 위한 도구

> [!important] 핵심 발상 함수의 최솟값을 어떻게 구할 수 있을까? **→ 함수의 규칙을 그림으로 표현하자 (함수의 그래프)**

### $f(x) = 2x$ 그래프 그리기 과정

**1단계**: 대응 관계 확인

```
1 ──▶ 2
2 ──▶ 4
```

**2단계**: 입력을 x축, 출력을 y축에 표시하며 점 찍기

**3단계**: 점들을 계속 찍어나가면 → 점들이 이루는 패턴이 보임

**4단계**: 점들을 연결하면 **직선**이 나타난다

$$y = f(x) = 2x$$

> [!tip] 그래프의 정의 함수의 그래프 = **정의구역의 각 원소 x와, 그에 대응하는 f(x)로 이루어진 순서쌍 (x, f(x))들의 집합**을 좌표평면에 그린 것

### $g(x) = x^2 - 1$ 그래프

|x|-2|-1|0|1|2|
|---|---|---|---|---|---|
|$g(x)$|3|0|-1|0|3|

→ 점을 찍어 이으면 **포물선** (아래로 볼록, 꼭짓점 $(0,-1)$)

---

## 다변수 함수의 그래프 (예고)

$$z = f(x,y)$$

```
       z (높이)
       ↑
       │    ╭─곡면─╮
       │f  ╱        ╲
       │ ╱            ╲
   ────┼─────────────────
      ╱ (x,y)
    ℝ²(정의구역 평면)
```

$(x, y) \mapsto z$ — 평면 위의 각 점에 높이 값 하나가 대응 → **곡면**

---

## 📌 핵심 요약

> [!summary] 오늘의 핵심 **① 딥러닝 학습** = 비용함수의 최솟값을 찾는 문제 → 함수 이해가 필수
> 
> **② 집합**: 조건이 명확한 대상들의 모임
> 
> **③ 함수**: X의 모든 원소가 Y의 원소 딱 하나에 대응
> 
> **④ 무한집합의 함수**: 화살표 대신 **식(규칙)**으로 표현
> 
> **⑤ 그래프**: 함수를 시각화 → 최솟값을 눈으로 확인 가능
> 
> **⑥ 다변수 함수의 그래프**는 곡면 형태로 나타난다 (다음 단계 예고)

---

#딥러닝기초 #함수 #집합 #그래프 #비용함수 #최솟값