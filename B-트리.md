# 📘 자료구조 — B 트리 (B-Tree)

---

## 🔖 목차

1. [[#개념 및 배경]]
2. [[#B 트리의 성질]]
3. [[#차수 m의 의미]]
4. [[#탐색 Search]]
5. [[#삽입 Insert]]
6. [[#삭제 Delete]]
7. [[#B+ 트리 변형]]
8. [[#B 트리 vs 이진 탐색 트리 비교]]
9. [[#시간 복잡도]]
10. [[#핵심 정리]]

---

## 📌 개념 및 배경

> **B 트리**: 하나의 노드에 **여러 키(key)와 여러 자식**을 가질 수 있는 **균형 다진 탐색 트리**

- 1972년 Rudolf Bayer & Edward McCreight 고안
- 데이터베이스, 파일 시스템 인덱스에 핵심적으로 사용
- **디스크 I/O 최소화** 를 목표로 설계 (노드 하나 = 디스크 블록 하나)

### 왜 B 트리인가?

```
이진 탐색 트리 / AVL  →  노드 하나에 키 1개
                         → 100만 개 데이터 = 높이 약 20
                         → 디스크 접근 20회

B 트리 (차수 1000)     →  노드 하나에 키 최대 999개
                         → 100만 개 데이터 = 높이 약 2
                         → 디스크 접근 2~3회  ← 압도적으로 빠름
```

---

## 📌 B 트리의 성질

> 차수(order) m인 B 트리의 규칙

```
① 모든 리프 노드는 같은 깊이에 있다 (완전 균형)
② 루트를 제외한 모든 노드는 최소 ⌈m/2⌉개의 키를 가진다
③ 모든 노드는 최대 m-1개의 키를 가진다
④ 노드에 k개의 키가 있으면 k+1개의 자식 포인터를 가진다
⑤ 각 노드 안의 키는 오름차순으로 정렬되어 있다
⑥ 키 k1 < k2 일 때, 사이의 자식 포인터는 k1 < x < k2 인 키 x만 포함
```

### 노드 구조 시각화 (차수 5)

```
┌─────┬─────┬─────┬─────┐
│ P0  │ K1  │ P1  │ K2  │ P2  │ K3  │ P3  │ K4  │ P4  │
└─────┴─────┴─────┴─────┘
  ↑자식     ↑키     ↑자식      ...

P: 자식 포인터 (최대 m개)
K: 키 값 (최대 m-1개)
```

---

## 📌 차수 m의 의미

|차수 m|최소 키 (루트 제외)|최대 키|최소 자식|최대 자식|
|---|---|---|---|---|
|3|1|2|2|3|
|4|2|3|2|4|
|5|2|4|3|5|
|m|⌈m/2⌉-1|m-1|⌈m/2⌉|m|

### 차수 3 B 트리 예시

```
           [30  60]
          /   |   \
    [10 20] [40 50] [70 80 90]
```

- 루트 키 2개 → 자식 3개 ✅
- 모든 리프가 같은 레벨 ✅
- 각 내부 노드 키 수: 최소 1, 최대 2 ✅

---

## 📌 탐색 Search

### 과정

```
① 루트 노드의 키들과 비교
② 찾는 값 == 키 → 탐색 성공 ✅
   찾는 값 < 첫 번째 키 → 왼쪽 자식으로 이동
   ki < 찾는 값 < ki+1 → i번째와 i+1번째 사이 자식으로 이동
   찾는 값 > 마지막 키 → 오른쪽 자식으로 이동
③ 리프까지 내려왔는데 없으면 탐색 실패 ❌
```

### 예시 — 40 탐색

```
           [30  60]
          /   |   \
    [10 20] [40 50] [70 80]

1단계: 30 < 40 < 60 → 가운데 자식
2단계: [40 50] 에서 40 발견 → 탐색 성공 ✅
```

---

## 📌 삽입 Insert

> 삽입은 항상 **리프 노드**에서 발생 노드가 꽉 차면 (키 m-1개) → **분열(Split)**

### 삽입 과정

```
① 탐색으로 삽입될 리프 노드 찾기
② 리프에 키 삽입 (정렬 유지)
③ 노드에 키가 m개 이상이면 → 분열(Split)
   - 중간 키를 부모로 올림
   - 노드를 왼쪽/오른쪽 두 노드로 분리
④ 부모도 꽉 찼으면 → 부모도 분열 (루트까지 전파 가능)
```

### 삽입 예시 (차수 3, 최대 키 2개)

**초기 트리에 35 삽입:**

```
           [30  60]
          /   |   \
    [10 20] [40 50] [70 80]
```

**35는 30~60 사이 → [40 50] 자식으로 이동**

```
[35 40 50]  ← 키가 3개 = m개 → 분열!
```

**중간값 40을 부모로 올림:**

```
           [30  40  60]
          /   |   |   \
    [10 20] [35] [50] [70 80]
```

### 루트 분열 (트리 높이 증가)

```
루트가 꽉 찰 경우:
  중간 키가 새 루트가 됨
  트리 높이 1 증가
  → B 트리의 유일한 높이 증가 방식
```

---

## 📌 삭제 Delete

> 삭제 후 노드의 키가 최솟값 미만이 되면 **재조정** 필요

### Case 1 — 리프 노드에서 삭제, 여유 있음

```
그냥 삭제
```

### Case 2 — 삭제 후 키 부족 (언더플로우)

```
방법 A — 형제에게 빌리기 (Rotation)
  인접 형제 노드에 여유 키가 있으면
  → 부모 키를 내려오고, 형제의 키를 부모로 올림

방법 B — 병합 (Merge)
  형제도 최솟값이면
  → 현재 노드 + 부모 키 1개 + 형제 노드 합치기
  → 부모 키가 줄어듦 → 부모도 언더플로우 발생 가능 (전파)
```

### Case 3 — 내부 노드에서 삭제

```
BST 삭제 Case 3과 유사:
  inorder predecessor (왼쪽 서브트리 최댓값) 또는
  inorder successor (오른쪽 서브트리 최솟값) 으로 대체
  → 해당 리프 노드에서 삭제 진행
```

### 삭제 흐름 요약

```
리프에서 삭제
  └── 키 수 충분 → 끝
  └── 언더플로우
        ├── 형제 여유 있음 → Rotation (빌리기)
        └── 형제 여유 없음 → Merge (병합) → 부모로 전파
```

---

## 📌 B+ 트리 변형

> 실제 DB 인덱스에는 B 트리보다 **B+ 트리**를 더 많이 사용

|구분|B 트리|B+ 트리|
|---|---|---|
|데이터 저장 위치|내부 노드 + 리프 노드|**리프 노드만**|
|내부 노드 역할|키 + 데이터|**라우팅 키만**|
|리프 노드 연결|❌|✅ **연결 리스트로 연결**|
|범위 탐색|느림|**빠름** (리프 순회만 필요)|
|키 중복|없음|내부 노드 키가 리프에도 존재|

### B+ 트리 구조

```
내부 노드(인덱스 역할):    [30 | 60]
                          /    |    \
리프 노드(데이터 저장): [10,20,30] → [40,50,60] → [70,80]
                              ↑ 연결 리스트로 연결
```

> MySQL InnoDB, PostgreSQL 등 대부분의 DBMS가 B+ 트리 사용

---

## 📌 B 트리 vs 이진 탐색 트리 비교

|항목|BST / AVL|B 트리|
|---|---|---|
|노드당 키 수|1개|최대 m-1개|
|자식 수|최대 2개|최대 m개|
|높이|O(log₂ n)|O(log_m n) ← 훨씬 낮음|
|메모리/디스크|RAM 최적화|**디스크 I/O 최적화**|
|사용처|메모리 내 탐색|**DB 인덱스, 파일 시스템**|

---

## 📌 시간 복잡도

|연산|시간 복잡도|비고|
|---|---|---|
|탐색|O(log n)|노드당 키 이진 탐색 포함|
|삽입|O(log n)|최악 경우 루트까지 분열 전파|
|삭제|O(log n)|최악 경우 루트까지 병합 전파|

> 실제 성능은 m(차수)이 클수록 높이가 낮아지므로 디스크 기반에서 압도적으로 빠름

---

## 🗂️ 핵심 정리

```
B 트리 핵심 3가지:
  1. 노드당 여러 키 → 낮은 높이 → 디스크 접근 최소화
  2. 삽입 시 꽉 차면 → 분열(Split), 중간 키 부모로
  3. 삭제 후 부족하면 → 빌리기(Rotation) 또는 병합(Merge)
```

### 차수 m B 트리 성질 암기

```
최대 키: m - 1
최소 키: ⌈m/2⌉ - 1  (루트 제외)
최대 자식: m
최소 자식: ⌈m/2⌉    (루트 제외)
모든 리프: 같은 깊이
```

### ⚠️ 최다 실수 포인트

|실수|올바른 내용|
|---|---|
|삽입 위치 혼동|삽입은 반드시 **리프**에서 발생|
|분열 기준 혼동|키가 m-1개 **초과** (= m개)가 될 때 분열|
|B vs B+ 혼동|B+는 데이터가 **리프에만** 있고 리프가 연결 리스트|
|최솟값 공식|⌈m/2⌉ - 1 (루트 제외 내부 노드, 루트는 키 1개만 있어도 됨)|

### 알고리즘 선택 가이드

|상황|자료구조|
|---|---|
|메모리 내 탐색, 균형 필요|AVL 트리 또는 Red-Black 트리|
|DB 인덱스, 파일 시스템|**B+ 트리**|
|범위 탐색 빈번|**B+ 트리** (리프 연결 리스트)|
|단순 구현, 소규모 데이터|BST|

---

> 📅 작성일: 2026-05-18 🏫 과목: 자료구조 📖 단원: 트리 — B 트리

#자료구조 #트리 #B트리 #B플러스트리 #데이터베이스 #btree