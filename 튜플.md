# 튜플 (Tuple)

#파이썬 #자료구조 #튜플

---

## 📌 목차

1. [[#1. 튜플이란?]]
2. [[#2. 튜플 생성]]
3. [[#3. 인덱싱과 슬라이싱]]
4. [[#4. 주요 연산 및 메서드]]
5. [[#5. 튜플 언패킹]]
6. [[#6. 언제 튜플을 쓸까?]]
7. [[#7. 자주 실수하는 포인트]]

---

## 1. 튜플이란?

> [!info] 핵심 개념 **여러 개의 값을 순서대로 저장**하되, **한 번 만들면 수정할 수 없는** 자료구조. 소괄호 `()`로 표현하며, **수정 불가(immutable)**.

### 리스트 vs 튜플 비교

|항목|리스트 `[]`|튜플 `()`|
|---|---|---|
|수정 가능 여부|✅ 가능 (mutable)|❌ 불가 (immutable)|
|속도|느림|**빠름**|
|메모리|많이 사용|**적게 사용**|
|용도|변하는 데이터|변하지 않는 데이터|
|표현|`[1, 2, 3]`|`(1, 2, 3)`|

> [!tip] C++ const 배열과 비교 C++의 `const int arr[]` 처럼, 선언 후 값을 바꿀 수 없는 개념과 유사함
> 
> ```cpp
> const int arr[] = {1, 2, 3};  // 수정 불가
> 
> # Python
> t = (1, 2, 3)   # 수정 불가
> ```

---

## 2. 튜플 생성

```python
# 기본 생성
t1 = (1, 2, 3)
t2 = (1, "hello", 3.14, True)  # 혼합 가능
t3 = ()                         # 빈 튜플

# 괄호 없이도 생성 가능 (권장하지 않음)
t4 = 1, 2, 3          # (1, 2, 3)
a, b, c = 1, 2, 3     # 이것도 사실 튜플 언패킹
```

> [!warning] 요소가 1개인 튜플 — 쉼표 필수!
> 
> ```python
> t = (5)    # ← 이건 튜플이 아닌 그냥 정수 5
> t = (5,)   # ← 이게 진짜 1개짜리 튜플. 쉼표 꼭 붙여야 함!
> 
> print(type((5)))    # <class 'int'>
> print(type((5,)))   # <class 'tuple'>
> ```

---

## 3. 인덱싱과 슬라이싱

리스트와 동일한 방식으로 동작함.

```python
t = (10, 20, 30, 40, 50)

# 인덱싱
print(t[0])     # 10
print(t[-1])    # 50

# 슬라이싱
print(t[1:3])   # (20, 30)
print(t[::-1])  # (50, 40, 30, 20, 10)  역순
```

> [!danger] 튜플은 값 변경 불가
> 
> ```python
> t = (1, 2, 3)
> t[0] = 99    # ← TypeError 발생!
> ```
> 
> 수정이 필요하다면 → **리스트로 변환 후 수정 → 다시 튜플로**
> 
> ```python
> t = (1, 2, 3)
> lst = list(t)     # 리스트로 변환
> lst[0] = 99
> t = tuple(lst)    # 다시 튜플로
> print(t)          # (99, 2, 3)
> ```

---

## 4. 주요 연산 및 메서드

### 연산

|연산|기능|예시|결과|
|---|---|---|---|
|`+`|연결|`(1,2) + (3,4)`|`(1, 2, 3, 4)`|
|`*`|반복|`(0,) * 3`|`(0, 0, 0)`|
|`in`|포함 여부|`3 in (1,2,3)`|`True`|
|`len()`|길이|`len((1,2,3))`|`3`|

```python
t1 = (1, 2, 3)
t2 = (4, 5)
t3 = t1 + t2    # (1, 2, 3, 4, 5)
t4 = t1 * 2     # (1, 2, 3, 1, 2, 3)
```

### 메서드 (딱 2개만 있음)

|메서드|기능|예시|
|---|---|---|
|`.count(x)`|x의 개수 반환|`t.count(2)`|
|`.index(x)`|x의 첫 인덱스 반환|`t.index(3)`|

```python
t = (1, 2, 2, 3, 2)
print(t.count(2))   # 3
print(t.index(3))   # 3
```

> [!note] 튜플의 메서드가 2개뿐인 이유 수정 불가이므로 추가/삭제 관련 메서드가 아예 없음. 읽기 전용 연산만 존재함.

---

## 5. 튜플 언패킹 (Unpacking)

> [!info] 개념 튜플의 값을 여러 변수에 **한 번에 꺼내는** 것

```python
# 기본 언패킹
t = (10, 20, 30)
a, b, c = t
print(a, b, c)   # 10 20 30

# 함수 반환값 받기 (가장 많이 쓰임)
def min_max(lst):
    return min(lst), max(lst)  # 튜플로 반환

lo, hi = min_max([3, 1, 4, 1, 5])
print(lo, hi)    # 1 5

# 변수 교환 (swap)
x, y = 1, 2
x, y = y, x     # 내부적으로 튜플 언패킹
print(x, y)     # 2 1

# * 를 이용한 나머지 받기
first, *rest = (1, 2, 3, 4, 5)
print(first)   # 1
print(rest)    # [2, 3, 4, 5]  ← 리스트로 받음

*head, last = (1, 2, 3, 4, 5)
print(head)    # [1, 2, 3, 4]
print(last)    # 5
```

---

## 6. 언제 튜플을 쓸까?

|상황|이유|
|---|---|
|변경하면 안 되는 데이터|실수로 수정되는 것을 방지|
|딕셔너리의 **키(key)** 로 사용|리스트는 키로 못 씀 (mutable이라서)|
|함수에서 **여러 값 반환** 시|`return x, y` 는 튜플로 반환됨|
|성능이 중요한 경우|리스트보다 속도 빠름, 메모리 적게 씀|

```python
# 튜플을 딕셔너리 키로 사용하는 예시
location = {}
location[(37, 127)] = "서울"   # (위도, 경도)를 키로 사용
location[(35, 129)] = "부산"

# 리스트는 키로 사용 불가
# location[[37, 127]] = "서울"  ← TypeError!
```

---

## 7. 자주 실수하는 포인트

> [!warning] 요소 1개짜리 튜플 생성 — 쉼표 빠뜨리기
> 
> ```python
> t = (1)    # int 1 이 됨
> t = (1,)   # 올바른 1개짜리 튜플
> ```

> [!warning] 튜플은 수정 불가이지만, 안에 리스트가 있으면?
> 
> ```python
> t = ([1, 2], [3, 4])
> t[0].append(99)   # 에러 없이 실행됨!
> print(t)          # ([1, 2, 99], [3, 4])
> ```
> 
> 튜플 자체는 못 바꾸지만, **튜플 안의 리스트는 수정 가능** (리스트가 mutable이므로)

---

## 🔗 관련 노트

- [[Python 기초 (변수, 자료형, 연산자, 조건문, 반복문)]] — 변수, 자료형, 반복문
- [[리스트]] — 수정 가능한 순서형 자료구조
- [[딕셔너리]] — 키-값 쌍 저장
- [[집합(세트)]] — 중복 없는 집합