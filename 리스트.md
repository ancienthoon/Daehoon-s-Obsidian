# 리스트 (List)

#파이썬 #자료구조 #리스트

---

## 📌 목차

1. [[#1. 리스트란?]]
2. [[#2. 리스트 생성]]
3. [[#3. 인덱싱과 슬라이싱]]
4. [[#4. 리스트 수정]]
5. [[#5. 주요 메서드]]
6. [[#6. 순회 (for문)]]
7. [[#7. 리스트 컴프리헨션]]
8. [[#8. 자주 실수하는 포인트]]

---

## 1. 리스트란?

> [!info] 핵심 개념 **여러 개의 값을 순서대로 저장**하는 자료구조. 대괄호 `[]`로 표현하며, **수정 가능(mutable)**.

|특징|설명|
|---|---|
|순서 있음|인덱스로 접근 가능 (0부터 시작)|
|중복 허용|같은 값을 여러 번 저장 가능|
|수정 가능|값 추가/삭제/변경 모두 가능|
|혼합 가능|서로 다른 자료형을 함께 저장 가능|

> [!tip] C++ 배열과 비교 C++의 `vector<T>`와 비슷하지만, 파이썬 리스트는 **자료형 제한 없음**
> 
> ```cpp
> // C++ vector
> vector<int> v = {1, 2, 3};
> 
> # Python list
> lst = [1, "hello", 3.14, True]  # 혼합 가능
> ```

---

## 2. 리스트 생성

```python
# 직접 선언
nums = [1, 2, 3, 4, 5]
names = ["철수", "영희", "민수"]
mixed = [1, "hi", 3.14, True]   # 혼합 자료형

# 빈 리스트
empty1 = []
empty2 = list()

# 반복값 초기화
zeros = [0] * 5          # [0, 0, 0, 0, 0]
repeat = ["x"] * 3       # ['x', 'x', 'x']

# 다른 자료형 → 리스트 변환
from_str = list("hello")      # ['h', 'e', 'l', 'l', 'o']
from_range = list(range(5))   # [0, 1, 2, 3, 4]
```

---

## 3. 인덱싱과 슬라이싱

### 인덱싱 (Indexing)

```python
lst = ["a", "b", "c", "d", "e"]
#       0    1    2    3    4     ← 양수 인덱스
#      -5   -4   -3   -2   -1    ← 음수 인덱스

print(lst[0])    # 'a'  (첫 번째)
print(lst[-1])   # 'e'  (마지막)
print(lst[-2])   # 'd'  (뒤에서 두 번째)
```

> [!tip] 음수 인덱스 파이썬은 **음수 인덱스**를 지원함. `-1`이 마지막 요소

### 슬라이싱 (Slicing) — `[start:stop:step]`

```python
lst = [0, 1, 2, 3, 4, 5]

lst[1:4]     # [1, 2, 3]       start~stop-1
lst[:3]      # [0, 1, 2]       처음부터 stop-1
lst[3:]      # [3, 4, 5]       start부터 끝까지
lst[:]       # [0, 1, 2, 3, 4, 5]  전체 복사
lst[::2]     # [0, 2, 4]       2칸씩
lst[::-1]    # [5, 4, 3, 2, 1, 0]  역순
```

|형태|의미|
|---|---|
|`[a:b]`|인덱스 a 이상, b 미만|
|`[a:b:c]`|a~b 범위에서 c칸씩 건너뜀|
|`[::-1]`|리스트 뒤집기|

---

## 4. 리스트 수정

```python
lst = [10, 20, 30]

# 값 변경
lst[0] = 99          # [99, 20, 30]

# 슬라이싱으로 범위 변경
lst[1:3] = [50, 60]  # [99, 50, 60]
```

---

## 5. 주요 메서드

### 추가

|메서드|기능|예시|
|---|---|---|
|`.append(x)`|맨 끝에 요소 1개 추가|`lst.append(4)`|
|`.insert(i, x)`|i번 위치에 x 삽입|`lst.insert(1, 99)`|
|`.extend(iter)`|다른 리스트 이어붙이기|`lst.extend([5, 6])`|

```python
lst = [1, 2, 3]
lst.append(4)        # [1, 2, 3, 4]
lst.insert(1, 99)    # [1, 99, 2, 3, 4]
lst.extend([5, 6])   # [1, 99, 2, 3, 4, 5, 6]
```

> [!warning] append vs extend 차이
> 
> ```python
> lst = [1, 2, 3]
> lst.append([4, 5])   # [1, 2, 3, [4, 5]]  ← 리스트 자체가 추가됨
> lst.extend([4, 5])   # [1, 2, 3, 4, 5]    ← 요소들이 각각 추가됨
> ```

### 삭제

|메서드|기능|예시|
|---|---|---|
|`.remove(x)`|값이 x인 첫 번째 요소 삭제|`lst.remove(3)`|
|`.pop()`|마지막 요소 삭제 후 반환|`val = lst.pop()`|
|`.pop(i)`|i번 인덱스 요소 삭제 후 반환|`val = lst.pop(1)`|
|`.clear()`|전체 삭제|`lst.clear()`|
|`del lst[i]`|i번 인덱스 요소 삭제|`del lst[0]`|

```python
lst = [1, 2, 3, 2, 4]
lst.remove(2)    # [1, 3, 2, 4]  ← 첫 번째 2만 삭제
lst.pop()        # [1, 3, 2]     ← 마지막 요소 삭제
lst.pop(0)       # [3, 2]        ← 0번 인덱스 삭제
```

### 탐색 / 정렬 / 기타

|메서드|기능|예시|
|---|---|---|
|`.index(x)`|x의 첫 번째 인덱스 반환|`lst.index(3)`|
|`.count(x)`|x의 개수 반환|`lst.count(2)`|
|`.sort()`|오름차순 정렬 (원본 변경)|`lst.sort()`|
|`.sort(reverse=True)`|내림차순 정렬|`lst.sort(reverse=True)`|
|`.reverse()`|순서 뒤집기 (원본 변경)|`lst.reverse()`|
|`.copy()`|얕은 복사|`new = lst.copy()`|
|`len(lst)`|길이 반환|`len(lst)`|
|`x in lst`|포함 여부 확인|`3 in lst`|

```python
lst = [3, 1, 4, 1, 5]
lst.sort()               # [1, 1, 3, 4, 5]
lst.sort(reverse=True)   # [5, 4, 3, 1, 1]

sorted_lst = sorted(lst) # 원본 유지, 정렬된 새 리스트 반환
```

> [!tip] `.sort()` vs `sorted()`
> 
> ||원본 변경|반환값|
> |---|---|---|
> |`.sort()`|O (변경됨)|`None`|
> |`sorted()`|X (유지됨)|정렬된 새 리스트|

---

## 6. 순회 (for문)

```python
fruits = ["사과", "바나나", "포도"]

# 기본 순회
for fruit in fruits:
    print(fruit)

# 인덱스 함께 사용: enumerate()
for i, fruit in enumerate(fruits):
    print(f"{i}: {fruit}")
# 0: 사과
# 1: 바나나
# 2: 포도

# 두 리스트 동시 순회: zip()
prices = [1000, 2000, 3000]
for fruit, price in zip(fruits, prices):
    print(f"{fruit}: {price}원")
```

---

## 7. 리스트 컴프리헨션 (List Comprehension)

> [!info] 개념 for문을 한 줄로 압축해서 리스트를 만드는 파이썬만의 간결한 문법

```python
# 기본 형태: [표현식 for 변수 in 반복가능객체]
squares = [x**2 for x in range(5)]
# [0, 1, 4, 9, 16]

# 조건 추가: [표현식 for 변수 in 반복가능객체 if 조건]
evens = [x for x in range(10) if x % 2 == 0]
# [0, 2, 4, 6, 8]

# 일반 for문과 비교
# ── 일반 방식
result = []
for x in range(5):
    result.append(x**2)

# ── 컴프리헨션 방식 (동일)
result = [x**2 for x in range(5)]
```

---

## 8. 자주 실수하는 포인트

> [!warning] 리스트 복사 함정 `=`로 복사하면 같은 리스트를 **가리키는 것**임. 원본도 같이 바뀜!
> 
> ```python
> a = [1, 2, 3]
> b = a         # ← 복사가 아니라 같은 리스트를 가리킴
> b[0] = 99
> print(a)      # [99, 2, 3]  ← a도 바뀜!
> 
> # 올바른 복사 방법
> b = a.copy()   # 또는 b = a[:]
> b = a[:]
> ```

> [!warning] 2차원 리스트 초기화 함정
> 
> ```python
> # ❌ 잘못된 방법 (같은 리스트 객체가 3개 복사됨)
> matrix = [[0] * 3] * 3
> matrix[0][0] = 9
> print(matrix)  # [[9,0,0],[9,0,0],[9,0,0]] ← 전부 바뀜!
> 
> # ✅ 올바른 방법
> matrix = [[0] * 3 for _ in range(3)]
> matrix[0][0] = 9
> print(matrix)  # [[9,0,0],[0,0,0],[0,0,0]] ← 정상
> ```

---

## 🔗 관련 노트

- [[파이썬_기초_입문]] — 변수, 자료형, 반복문
- [[파이썬_자료구조_튜플]] — 수정 불가능한 리스트
- [[파이썬_자료구조_딕셔너리]] — 키-값 쌍 저장
- [[파이썬_자료구조_세트]] — 중복 없는 집합