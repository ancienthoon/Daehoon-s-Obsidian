# 프로그래밍 스타일 (Programming Style)

---

## 1. 학습 배경

### 흔히 겪는 문제들

- 잘못된 알고리즘으로 코딩해서 엄청난 시간을 낭비
- 지나치게 복잡한 데이터 구조 사용
- 명백한 오류를 테스트에서 놓침
- 5분이면 잡을 버그를 하루 종일 찾음
- 다른 사람이 짠 코드를 도저히 이해 못해 처음부터 다시 작성

### 좋은 S/W 개발의 기본 원칙

|원칙|설명|
|---|---|
|**단순성 (Simplicity)**|프로그램을 간결하고 관리하기 쉽게 짜는 것|
|**명확성 (Clarity)**|기계뿐 아니라 사람도 이해하기 쉽게 만드는 것|
|**일반성 (Generality)**|다양한 상황에서 잘 동작하며 새 상황에도 잘 적응하는 것|
|**자동화 (Automation)**|하찮은 반복 작업을 기계가 대신하게 하는 것|

---

## 2. 스타일 (Style)

### 스타일이 필요한 이유

- 문제를 일으키는 결함을 **사전에 피하도록** 도와줌
- 작성자와 다른 사람 모두 **코드를 보기 편하게** 함
- **좋은 스타일** = 좋은 프로그래밍의 핵심 요소
- 스타일의 근본 원리는 **경험에서 나온 일반적 상식**에 기초

### 스타일의 근본 원리

1. **코드는 간단명료해야 함**
2. **일관성(Consistency)** 을 유지해야 함

---

## 3. 이름 (Naming)

### 이름의 역할

- 변수나 함수 등 객체들을 **구분** 짓고 **목적에 관한 정보를 전달**
- 정보면에서 **유익함**과 동시에 **간결하고 기억하기 쉬워야** 함

### 작명 원칙

|종류|원칙|예시|
|---|---|---|
|전역변수|서술적 이름 + 간단한 주석|`int Npending = 0; // 입력 큐 대기 원소 수`|
|지역변수|짧은 이름|`i`, `j` (루프), `p`, `q` (포인터), `s`, `t` (문자열)|
|상수|역할을 나타내는 이름|`INPUT_MODE`, `INPUT_BUFSIZE`|

#### ❌ 나쁜 코드 예시

```cpp
#define ONE   1
#define TWO   2
#define TWENTY 20
```

#### ✅ 좋은 코드 예시

```cpp
#define INPUT_MODE    1
#define INPUT_BUFSIZE 10
#define OUTPUT_BUFSIZE 20
```

### 명명 규칙 요약

|종류|관례|예시|
|---|---|---|
|포인터|`p`로 시작 또는 끝|`nodep`, `pNode`|
|전역변수|첫 글자 대문자 or 전체 대문자|`Globals`, `CONSTANTS`|
|헝가리안 표기|타입 접두사 사용|`pch`, `iNum`, `fRadius`, `dSquare`|

> [!tip] 핵심 원칙  
> 세부 규칙보다 **정해진 규칙을 일관되게 지키는 것**이 더 중요하다.

### 코드 비교: 간결함이 명료성을 높인다

#### ❌ 지나치게 긴 이름

```cpp
for (theElementIndex = 0; theElementIndex < numberOfElements; theElementIndex++) {
    elementArray[theElementIndex] = theElementIndex;
}
```

#### ✅ 간결한 이름

```cpp
for (int i = 0; i < nelems; i++) {
    elem[i] = i;
}
```

---

## 4. 표현식과 문장

### 들여쓰기 (Indentation)

> 프로그램 구조를 명확히 보여주는 **가장 쉬운 수단**

#### ❌ 나쁜 예 (한 줄에 여러 문장)

```cpp
for (n = 0; n < 100; field[n++] = '\0') ;
*i = '\0'; return('\n');
```

#### ✅ 좋은 예 (각 문장을 별도 줄에)

```cpp
for (n = 0; n < 100; n++)
    field[n] = '\0';

*i = '\0';
return '\n';
```

---

### 자연스러운 표현식

> **소리 내어 말하는 것처럼** 자연스럽게 코드를 쓸 것

#### ❌ 부자연스러운 부정문

```cpp
if (!(block_id < actblks) || !(block_id >= unblocks))
```

#### ✅ 자연스러운 긍정문

```cpp
if ((block_id >= actblks) || (block_id < unblocks))
```

---

### 괄호로 애매함 해소

- 꼭 필요하지 않아도 **의도를 명확히 하기 위해** 괄호를 사용하는 것이 좋음
- 언어마다 연산자 우선순위가 다르므로 괄호 사용 권장
- 논리 연산자와 대입 연산자를 섞어 쓸 때는 **반드시** 괄호 사용

```cpp
// 대입과 비교 혼용 시 반드시 괄호
while ((c = getchar()) != EOF) { ... }
```

#### 연산자 우선순위 혼동 예

```cpp
// ❌ 의도가 불분명 → if (x & (Mask == BITS)) 로 해석됨
if (x & Mask == BITS) { ... }

// ✅ 의도가 명확
if ((x & Mask) == BITS) { ... }
```

#### 윤년 계산: 괄호 있는 버전이 더 명확

```cpp
// 가능하지만 읽기 어려움
leap_year = y % 4 == 0 && y % 100 != 0 || y % 400 == 0;

// ✅ 의미가 더욱 명확
leap_year = ((y % 4 == 0) && (y % 100 != 0)) || (y % 400 == 0);
```

---

### 일관성과 관용 표현

#### 미결합 else (Dangling else) 문제

> 중괄호 없이 `if-else` 중첩 시 `else`가 어느 `if`에 속하는지 애매해짐

```cpp
// ❌ 위험한 코드 - else 귀속 불명확
if (iMonth == FEB) {
    if (iYear % 4 == 0)
        if (iDay == 29)
            cType = 'A';
    else                    // 이 else는 어디에 속하나?
        if (iDay == 28)
            cType = 'B';
}

// ✅ 중괄호로 명확히 구분
if (iMonth == FEB) {
    if (iYear % 4 == 0) {   // 4의 배수 (윤년)
        if (iDay == 29)
            cType = 'A';
    } else {                 // 4의 배수가 아닌 해
        if (iDay == 28)
            cType = 'B';
    }
}
```

> [!warning] 중괄호 사용 권장  
> 실행문이 하나여도 **항상 중괄호**를 쓰는 습관을 들이자.  
> 남의 코드를 수정할 때는 **원래 스타일을 유지**할 것. 일관성 > 개인 취향.

---

### 관용 표현: 루프 (Loop)

| 스타일           | 코드                                          | 평가    |
| ------------- | ------------------------------------------- | ----- |
| while 루프      | `i = 0; while (i <= n-1) array[i++] = 1.0;` | 가능    |
| for 루프 (비관용)  | `for (i = 0; i < n; ) array[i++] = 1.0;`    | ❌ 비권장 |
| for 루프 역방향    | `for (i = n-1; --i >= 0; ) array[i] = 1.0;` | ❌ 비권장 |
| **for 루프 표준** | `for (i = 0; i < n; i++) array[i] = 1.0;`   | ✅ 권장  |

```cpp
// 루프 변수를 루프 안에서 선언 (C++, Java 권장 방식)
for (int i = 0; i < n; i++)
    array[i] = 1.0;

// 연결 리스트 순회 관용 표현
for (Node* p = list; p != nullptr; p = p->next) { ... }

// 무한 루프 관용 표현
for (;;) { ... }       // C++ 스타일
while (true) { ... }   // 명확한 의도 표현
```

---

### 매크로 함수 vs inline 함수

#### 매크로 함수의 문제점

```cpp
#define SQUARE(x) x*x

int main() {
    int a = 5;
    std::cout << SQUARE(a) << "\n";     // 5*5 = 25 (OK)
    std::cout << SQUARE(a + 2) << "\n"; // a+2*a+2 = 5+10+2 = 17 ← 의도와 다름!
}

// ✅ 올바른 매크로 (인수를 괄호로 감쌈)
#define SQUARE(x) ((x) * (x))
```

#### inline 함수 (매크로보다 안전하고 권장됨)

```cpp
// ✅ inline 함수: 자료형 명시 → 안전하고 명확
inline int add2(int a, int b) {
    return a + b;
}

int main() {
    std::cout << 2 * add2(10, 20) << "\n"; // 60
}
```

> [!note] 매크로 vs inline
> 
> - 매크로: 빠르지만 자료형 없음, 부작용 위험
> - **inline 함수**: 자료형 명시, 디버깅 용이 → **현대 C++에서 권장**

---

### 매직 넘버 (Magic Number)

> 코드에 직접 쓰인 의미 불명의 숫자값 → **반드시 이름을 붙여야 함**

```cpp
// ❌ 매직 넘버 직접 사용
if (c >= 65 && c <= 90) { ... }

// ✅ 의미 있는 이름 사용
if (c >= 'A' && c <= 'Z') { ... }

// ✅ const 상수로 정의 (#define 보다 권장)
const int MAXCOL = 80;
const int MINCOL = 1;
const int MAXROW = 24, MINROW = 1;

// ✅ 객체 크기는 sizeof 사용
std::cout << sizeof(int) << "\n";
```

---

### 주석 (Comments)

|원칙|설명|
|---|---|
|명확한 코드에는 주석 ❌|코드 자체가 설명이 되어야 함|
|함수·전역 데이터에는 주석 ✅|역할, 인자, 반환값 설명|
|나쁜 코드를 설명하지 말 것|코드를 **새로 짜는 것**이 올바른 해법|
|주석과 코드가 모순되지 않게|코드 수정 시 주석도 반드시 함께 수정|

#### 함수 주석 예시

```cpp
/*
 * idct : Chen-Wang이 고안한 역 이차원 8x8 이산 코사인 변환(DCT)의 구현
 *        (IEEE ASSP-32, pp.803-816, 1984년 8월)
 *
 * 32비트 정수 연산 (8비트 계수)
 * 매 DCT마다 곱셈 11회, 덧셈 29회
 * IEEE 1180-1990 준수를 위해 계수가 12비트로 확장됨
 */
static void idct(int b[8 * 8]) {
    // ...
}
```

---

## 5. 자주 실수하는 비효율적인 프로그래밍 스타일

### ❌ 실수 #1: 초기화되지 않은 변수를 rvalue로 사용

```cpp
// ❌ sum이 초기화되지 않음 → 쓰레기 값 사용
int i, sum;
for (i = 0; i < nelems; i++) {
    sum = sum + i;
}

// ✅ 반드시 초기화 후 사용
int i, sum = 0;
for (i = 0; i < nelems; i++) {
    sum = sum + i;
}
```

---

### ❌ 실수 #2: 루프 내에서 관계없는 할당문 포함

```cpp
// ❌ sum = 0이 루프와 무관하나 반복 실행됨
for (int i = 0; i < nelems; i++) {
    sum = 0;         // 매 반복마다 0으로 초기화됨 → 버그!
    elem[i] = i;
}
sum = sum + result;

// ✅ 루프 외부로 이동
sum = 0;
for (int i = 0; i < nelems; i++) {
    elem[i] = i;
}
sum = sum + result;
```

---

### ❌ 실수 #3: 루프 내에서 변하지 않는 값을 매번 계산

#### 예시 1: 불필요한 sqrt 반복 계산

```cpp
// ❌ dSqr = sqrt(dInput)가 매 반복마다 계산됨
for (int i = 0; i < nelems; i++) {
    double dSqr = std::sqrt(dInput);
    dResult = dResult + dSqr * std::sqrt((double)i);
}

// ✅ 루프 밖으로 꺼내기
double dSqr = std::sqrt(dInput);
for (int i = 0; i < nelems; i++) {
    dResult = dResult + dSqr * std::sqrt((double)i);
}
```

#### 예시 2: 종료 조건에서 불필요한 반복 계산

```cpp
// ❌ sqrt(dInput)이 매 반복마다 계산됨
for (int i = 2; i <= (int)std::sqrt(dInput); i++) { ... }

// ✅ 미리 계산
int dLimit = (int)std::sqrt(dInput);
for (int i = 2; i <= dLimit; i++) { ... }

// ❌ strlen(str)이 매 반복마다 계산됨
for (int i = 0; i < strlen(str); i++) { ... }

// ✅ 미리 계산
int iLen = strlen(str);
for (int i = 0; i < iLen; i++) { ... }
```

> [!note] 단, C++의 `std::string::size()`는 O(1)이므로 반복 호출해도 성능 차이 없음

---

## 6. 다중 반복문에서의 변수 사용

> 변수의 **명명법**과 **동작 범위(scope)** 에 반드시 주의할 것

### 비교: 외부 선언 vs 루프 내부 선언

```cpp
// 케이스 1: 외부 선언, 다른 이름 사용 → 안전
int iSum = 0, i, k;
for (i = 1; i <= 10; i++) {
    iSum += i;
    for (k = 1; k <= 10; k++) {
        iSum += k;
    }
}
// 결과: 55 + 10*55 = 605
```

```cpp
// 케이스 2: 외부 선언, 같은 이름 사용 → ⚠️ 버그!
int iSum = 0, i;
for (i = 1; i <= 10; i++) {
    iSum += i;
    for (i = 1; i <= 10; i++) {  // 외부 루프의 i를 덮어씀!
        iSum += i;
    }
    // 내부 루프 종료 후 i=11 → 외부 루프도 즉시 종료
}
// 결과: 1 + 55 = 56  ← 의도와 전혀 다름
```

```cpp
// 케이스 3: 루프 내부 선언, 같은 이름 → 별개 변수 (C++)
int iSum = 0;
for (int i = 1; i <= 10; i++) {   // 외부 i
    iSum += i;
    for (int i = 1; i <= 10; i++) { // 내부 i (별개 변수, 외부 i에 영향 없음)
        iSum += i;
    }
}
// 결과: 케이스 1과 동일 = 605
```

```cpp
// 케이스 4: 루프 내부 선언, 다른 이름 → ✅ 가장 명확하고 권장
int iSum = 0;
for (int i = 1; i <= 10; i++) {
    iSum += i;
    for (int k = 1; k <= 10; k++) {
        iSum += k;
    }
}
// 결과: 605
```

> [!tip] 권장 방식
> 
> - 다중 루프에서는 **서로 다른 변수명** 사용
> - 루프 변수는 **루프 안에서 선언** (`for (int i = ...)`)
> - 가장 명확하고 안전한 방식 = **케이스 4**

---

## 7. 효율성과 무관한 코딩 스타일 선택

아래 두 코드는 **효율성 차이가 없음** → 가독성 기준으로 선택

```cpp
// 스타일 A: 입력과 계산을 같은 루프에서
for (int i = 0; i < iDataNum; i++) {
    std::cin >> man[i].name >> man[i].age >> man[i].po;
    avg += man[i].age;
}

// 스타일 B: 입력과 계산을 분리된 루프에서
for (int i = 0; i < iDataNum; i++) {
    std::cin >> man[i].name >> man[i].age >> man[i].po;
}
for (int i = 0; i < iDataNum; i++) {
    avg += man[i].age;
}
```

> [!note]  
> `std::string::size()`는 O(1) 최적화가 보장되므로,  
> `for (int i = 0; i < str.size(); i++)` 와 미리 계산하는 방식 모두 실질적 차이 없음.

---

## 8. 도전 문제

- **백준 2753번** - 윤년 (if 조건식 + 연산자 우선순위 연습)
- **백준 14681번** - 사분면 고르기 (다중 조건 분기 연습)
- **백준 1270번** - 지하철을 빠르게 빠르게 EASY (알고리즘 최적화: O(MN) → 개선)

---

## 📋 핵심 요약

```
좋은 코드의 5가지 원칙
1. 간결함 (Simplicity)   → 짧고 명확한 이름, 불필요한 코드 제거
2. 명확함 (Clarity)      → 자연스러운 표현식, 들여쓰기, 괄호 활용
3. 일관성 (Consistency)  → 관용 표현 준수, 스타일 통일
4. 안전함 (Safety)       → 초기화, 중괄호 사용, inline > 매크로
5. 효율성 (Efficiency)   → 루프 밖으로 불변 계산 이동
```