---
tags:
  - 2학기전공과목
  - "#파이썬"
---

# 3장. 조건문 (Conditional Statement)

> [!info] 강의 정보
> 
> - **과목**: 파이썬익스프레스
> - **단원**: 3장 — 조건문
> - **연관 노트**: [[4장 반복문]] · [[5장 함수]]

---

## 🎯 학습 목표

- [ ] 제어문에 대하여 이해한다
- [ ] `if-else` 문을 이해하고 사용할 수 있다
- [ ] 관계연산자와 논리연산자를 학습한다
- [ ] 블록의 개념을 학습한다
- [ ] 중첩 `if-else` 문을 학습한다
- [ ] 연속 `if-else` 문을 학습한다

---

## 📌 제어문 구조

```
제어문
├── 조건문 (if-else)  : 조건에 따라 다른 코드 실행
└── 반복문 (for/while): 조건이 참인 동안 반복 실행
```

---

## 1️⃣ if-else 문

### 기본 형식

```python
if 조건식 :        # 콜론(:) 필수 — 블록 시작을 의미
    문장1          # 조건이 참이면 실행
else :
    문장2          # 조건이 거짓이면 실행
```

> [!warning] 들여쓰기 (Indentation) — 가장 중요! 같은 레벨의 문장들은 **반드시 같은 위치(들여쓰기 위치)** 에 있어야 함  
> 파이썬에서 들여쓰기가 잘못되면 **오류 발생**

```python
# ✅ 올바른 들여쓰기
if number > 0 :
    print("양수")     # 들여쓰기 레벨 1
else :
    print("음수")     # 들여쓰기 레벨 1 (if와 else 같은 레벨)

# ❌ 잘못된 들여쓰기 → 오류
if number > 0 :
    print("양수")
  else :             # else가 if와 다른 위치 → 오류!
    print("음수")
```

### else 없이 사용 가능

```python
# 기본값 설정 후 조건에 따라 변경
shipping_cost = 3000          # 기본 배송비
if price > 20000 :
    shipping_cost = 0         # 2만원 초과이면 배송비 없음
```

### 예시 — 배송비 계산

```python
price = int(input("상품의 가격: "))

if price > 20000 :
    shipping_cost = 0
else :
    shipping_cost = 3000

print("배송비 = ", shipping_cost)

# 입력: 30000  → 배송비 = 0
# 입력: 15000  → 배송비 = 3000
```

---

## 2️⃣ 관계 연산자

| 연산       | 의미                  | 수학 표기 |
| -------- | ------------------- | ----- |
| `x == y` | x와 y가 **같은가**?      | =     |
| `x != y` | x와 y가 **다른가**?      | ≠     |
| `x > y`  | x가 y보다 **큰가**?      | >     |
| `x < y`  | x가 y보다 **작은가**?     | <     |
| `x >= y` | x가 y보다 **크거나 같은가**? | ≥     |
| `x <= y` | x가 y보다 **작거나 같은가**? | ≤     |

> [!note] `=` vs `==`
> 
> - `=` : **대입** 연산자 (변수에 값 저장)
> - `==` : **비교** 연산자 (같은지 비교, True/False 반환)

---

## 3️⃣ 부울 변수 (Boolean Variable)

> **True / False 값만 갖는 변수**

```python
radius = 100
flag = (radius > 32)    # flag는 부울 변수
print(flag)             # True

# 부울 변수를 if 조건에 직접 사용
expensive = price > 20000

if expensive :          # 관계 수식 대신 부울 변수 사용 가능
    shipping_cost = 0
else :
    shipping_cost = 3000
```

---

## 4️⃣ 문자열 비교

```python
# == 비교 (같은지)
s1 = "Audrey Hepburn"
s2 = "Audrey Hepburn"
print(s1 == s2)    # True

# < 비교 (사전순)
s1 = "Audrey Hepburn"
s2 = "Grace Kelly"
print(s1 < s2)     # True  → A가 G보다 사전에서 앞에 있음
```

---

## 5️⃣ 실수 비교 — 오차 주의!

> [!warning] 실수는 절대 `==` 로 비교하면 안 됨
> 
> - 실수는 이진수로 정확히 표현하기 어려움
> - 항상 **약간의 오차**가 존재
> - 실수 비교는 **오차를 허용한 방식**으로!

```python
from math import sqrt   # math 패키지에서 sqrt 함수만 가져옴

n = sqrt(3.0)

# ❌ 잘못된 방법
if n*n == 3.0 :
    print("같다")
# → "같지 않다" 출력 (오차 때문에)

# ✅ 올바른 방법 — 오차 허용
if abs(n*n - 3.0) < 0.00001 :
    print("같다")
# → "같다" 출력
```

---

## 6️⃣ 조건 연산자 (삼항 연산자)

> **한 줄로 if-else 표현**

```python
# 형식: (참일 때 값) if 조건 else (거짓일 때 값)
변수 = (값A if 조건 else 값B)
```

```python
# C++ 대응: shipping_cost = (price >= 20000) ? 0 : 3000;
shipping_cost = (0 if price >= 20000 else 3000)

absolute_value = (x if x > 0 else -x)   # 절대값
max_value      = (x if x > y else y)    # 최대값
min_value      = (x if x < y else y)    # 최소값
```

---

## 7️⃣ 논리 연산자

|연산|의미|
|---|---|
|`x and y`|AND — x와 y **모두** 참이면 참, 하나라도 거짓이면 거짓|
|`x or y`|OR — x나 y **하나라도** 참이면 참, 모두 거짓이면 거짓|
|`not x`|NOT — x가 참이면 거짓, 거짓이면 참|

```python
# and 예시 — 가격 2만원 초과 AND 파이썬 카드이면 배송비 없음
price = int(input("가격을 입력하시오: "))
card  = input("카드 종류를 입력하시오: ")

if price > 20000 and card == "python" :
    print("배송료가 없습니다.")
else :
    print("배송료는 3000원입니다.")

# 30000, python → 배송료가 없습니다.
# 30000, java   → 배송료는 3000원입니다.
```

---

## 8️⃣ 드모르간의 법칙

> **not 연산자가 포함된 복잡한 조건을 단순화**

$$\neg(P \vee Q) \Leftrightarrow (\neg P) \wedge (\neg Q)$$ $$\neg(P \wedge Q) \Leftrightarrow (\neg P) \vee (\neg Q)$$

```python
# ❌ 복잡한 표현 (not 안에 and 포함)
if not (country == "한국" and province != "제주") :
    shipping_cost = 8000

# ✅ 드모르간 법칙 적용 → 단순화
if country != "한국" or province == "제주" :
    shipping_cost = 8000
```

> 드모르간 법칙: `not (A and B)` = `(not A) or (not B)`

---

## 9️⃣ 중첩 if 문

> **if 문 안에 또 다른 if 문**

### 형식

```python
if 조건식1 :
    if 조건식2 :       # 조건식1이 참일 때 실행
        문장1
    else :
        문장2
else :
    if 조건식3 :       # 조건식1이 거짓일 때 실행
        문장3
    else :
        문장4
```

### 예시 — 국가별 배송비 계산

```python
country = input("배송지(korea/us): ")
price   = int(input("상품의 가격: "))

if country == "korea" :
    if price >= 20000 :      # 한국: 2만원 이상이면 무료
        shipping_cost = 0
    else :
        shipping_cost = 3000
else :                       # 미국
    if price >= 100000 :     # 미국: 10만원 이상이면 무료
        shipping_cost = 0
    else :
        shipping_cost = 8000

print("배송비 = ", shipping_cost)
```

---

## 🔟 연속 if 문 (elif)

> **여러 조건을 순서대로 검사** — `elif` 사용

```python
if 조건1 :
    문장1
elif 조건2 :      # else if 의 줄임말
    문장2
elif 조건3 :
    문장3
else :
    문장4
```

### 예시 — 학점 결정

```python
score = int(input("성적을 입력하시오: "))

if score >= 90 :
    print("학점 A")
elif score >= 80 :
    print("학점 B")
elif score >= 70 :
    print("학점 C")
elif score >= 60 :
    print("학점 D")
else :
    print("학점 F")

# 입력: 88 → 학점 B
```

```
점수 흐름도:
점수 ─→ 90 이상? ─Yes→ A
         No
         ↓
        80 이상? ─Yes→ B
         No
         ↓
        70 이상? ─Yes→ C
         No
         ↓
        60 이상? ─Yes→ D
         No
         ↓
        F
```

---

## 🧪 Lab 예제

### Lab 1 — 산술 퀴즈 프로그램

```python
import random

x = random.randint(1, 100)
y = random.randint(1, 100)

answer = int(input(f"{x} + {y} = "))

flag = (answer == (x + y))
print(flag)   # True or False

# 25 + 78 = 103 → True
# 25 + 78 = 100 → False
```

### Lab 2 — 거북이 제어하기

```python
import turtle

t = turtle.Turtle()
t.shape("turtle")
t.width(3)
t.shapesize(3, 3)    # 거북이 3배 확대

while True:
    command = input("명령을 입력하시오: ")

    if command == "l":      # 왼쪽으로 100픽셀
        t.left(90)
        t.forward(100)
    if command == "r":      # 오른쪽으로 100픽셀
        t.right(90)
        t.forward(100)
    if command == "q":      # 종료
        break

turtle.mainloop()
turtle.bye()
```

---

## 💡 핵심 요약

> [!summary] 3장 핵심 정리
> 
> **① if-else 기본**
> 
> - 콜론(`:`) 필수, 블록은 **들여쓰기**로 구분
> - `else` 는 생략 가능
> 
> **② 관계 연산자**
> 
> - `==` (같다) / `!=` (다르다) / `>` `<` `>=` `<=`
> - `=` (대입)과 `==` (비교) 혼동 주의!
> 
> **③ 논리 연산자**
> 
> - `and` (둘 다 참) / `or` (하나라도 참) / `not` (반전)
> - 복잡한 `not` 조건 → **드모르간 법칙**으로 단순화
> 
> **④ 실수 비교**
> 
> - 절대 `==` 사용 금지
> - `abs(a - b) < 0.00001` 패턴 사용
> 
> **⑤ 조건 종류**
> 
> - **조건 연산자**: `(A if 조건 else B)` — 한 줄 표현
> - **중첩 if**: if 안에 if — 복수 조건 계층화
> - **연속 if (elif)**: 여러 조건 순차 검사

---

## ✏️ 연산자 비교 한눈에 보기

|종류|연산자|반환|
|---|---|---|
|관계|`==` `!=` `>` `<` `>=` `<=`|`True` / `False`|
|논리|`and` `or` `not`|`True` / `False`|
|조건|`A if 조건 else B`|조건에 따른 값|

---

#파이썬 #조건문 #3장 #파이썬익스프레스 #if #elif #else #논리연산자 #관계연산자