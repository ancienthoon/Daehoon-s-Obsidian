# 📘 C++ 8장 강의 노트 — 복사 생성자 · 재정의 · 상속 유형 · 열거형

---

## 🔖 목차

1. [[#복사 생성자 Copy Constructor]]
2. [[#상속에서의 복사 생성자]]
3. [[#재정의 Overriding]]
4. [[#재정의 vs 중복 정의]]
5. [[#재정의된 멤버 함수 호출 순서]]
6. [[#자식 클래스에서 부모 함수 호출]]
7. [[#멤버 변수 재정의]]
8. [[#상속의 3가지 유형]]
9. [[#다중 상속]]
10. [[#열거형 enum]]
11. [[#핵심 정리]]

---

## 📌 복사 생성자 Copy Constructor

> **개념**: 이미 만들어진 객체를 "복사"해서 새 객체를 만들 때 호출되는 생성자

```cpp
Car c1(0, 1);
Car c2(c1);    // 복사 생성자 호출
Car c2 = c1;   // 위와 동일
```

- **디폴트 복사 생성자**: 사용자가 작성하지 않아도 컴파일러가 자동으로 만들어 줌
- 모든 멤버변수를 그대로 복사 (얕은 복사)
- **포인터 멤버변수**가 있는 경우에는 반드시 직접 작성해야 함

```cpp
// 디폴트 복사 생성자 형태
Car(const Car &obj) : speed(obj.speed), gear(obj.gear) { }
```

---

## 📌 상속에서의 복사 생성자

### 경우 1️⃣ 부모/자식 모두 복사 생성자 없는 경우

- 자식의 **디폴트 복사 생성자**가 자동 생성
- 부모의 **디폴트 복사 생성자** 자동 호출

### 경우 2️⃣ 부모/자식 모두 복사 생성자 있는 경우

```cpp
Child(const Child& tt) : Parent(tt) {  // ✅ 부모 복사 생성자 명시적 호출
    c = tt.c;
}
```

### ⚠️ 자식 복사 생성자에서 부모 복사 생성자 호출 생략 시

```cpp
Child(const Child& tt) : Parent() {  // ❌ 부모의 디폴트 생성자 호출됨
    c = tt.c;
}
```

| 상황               | 결과                                  |
| ---------------- | ----------------------------------- |
| `Parent(tt)` 명시  | 부모 복사 생성자 호출 → 부모 멤버도 복사됨           |
| `Parent()` 또는 생략 | 부모 디폴트 생성자 호출 → 부모 멤버가 기본값으로 초기화됨 ❌ |

> **결론**: 자식 복사 생성자는 반드시 `Parent(tt)`로 부모 복사 생성자를 호출해야 한다!

---

## 📌 재정의 Overriding

> **개념**: 부모 클래스에서 상속받은 함수를, 자식 클래스가 **같은 이름·반환형·매개변수**로 다시 정의하는 것

```cpp
class Car {
public:
    int getHP() { return 100; }
};

class SportsCar : public Car {
public:
    int getHP() { return 300; }  // ✅ 재정의
};
```

### 재정의 조건 (3가지 모두 동일해야 함)

| 조건            | 설명  |
| ------------- | --- |
| 함수 이름         | 동일  |
| 반환형           | 동일  |
| 매개변수 (개수, 타입) | 동일  |

### ⚠️ 반환형이 다르면 재정의가 아님!

```cpp
class Car {
public:
    int getHP() { return 100; }
};

class SportsCar : public Car {
public:
    float getHP() { return 300.0; }  // ❌ 재정의 아님 — 반환형 다름
};
```

> 결과값은 동일해 보여도, C++ 컴파일러 기준으로 재정의가 아님

### 함수 탐색 순서

```
자식 클래스에서 먼저 찾음 → 없으면 부모 클래스로 올라감
```

---

## 📌 재정의 vs 중복 정의

| 구분    | 재정의 (Overriding) | 중복 정의 (Overloading) |
| ----- | ---------------- | ------------------- |
| 위치    | 부모-자식 클래스 간      | 같은 클래스 안            |
| 함수 이름 | 동일               | 동일                  |
| 매개변수  | 동일               | 다름                  |
| 반환형   | 동일               | 무관                  |
| 목적    | 부모 기능을 자식이 교체    | 같은 이름으로 다양한 기능      |

---

## 📌 재정의된 멤버 함수 호출 순서

> **규칙**: 해당 객체의 클래스(자기 자신)부터 위로 올라가며 탐색

```
Animal : sleep(), eat(), move(), sound()
   └── Bird : sound()
         └── Eagle : move(), eat()
```

```cpp
Eagle e;
e.sleep();   // Animal의 sleep() 호출 (Eagle, Bird 모두 없음)
e.eat();     // Eagle의 eat() 호출 (자기 것 있음)
e.sound();   // Bird의 sound() 호출 (Eagle에 없어서 부모로)
```

---

## 📌 자식 클래스에서 부모 함수 호출

> 자식이 부모의 함수를 **보완**하여 사용하는 패턴 (일반적)

```cpp
class ChildClass : public ParentClass {
public:
    void print() {
        ParentClass::print();   // 부모 함수 먼저 호출
        cout << "자식 클래스의 print()" << endl;
    }
};
```

- 함수 이름이 같으면 `클래스명::함수명()` 형태로 명시
- 이름이 다르면 클래스명 없이 호출 가능

---

## 📌 멤버 변수 재정의

> 가능하지만 **혼란**을 일으키므로 권장하지 않음

```cpp
SportsCar sc;
cout << sc.speed;           // 자식의 speed
cout << sc.Car::speed;      // 부모의 speed
cout << sc.getSpeed();      // 부모 함수가 반환하는 speed (부모 것)
```

---

## 📌 상속의 3가지 유형

> 대부분 **public 상속**을 사용함

```cpp
class 자식클래스 : public 부모클래스 { };
```

### 접근 지정자별 상속 결과

|부모 멤버|public 상속|protected 상속|private 상속|
|---|---|---|---|
|public|→ public|→ protected|→ private|
|protected|→ protected|→ protected|→ private|
|private|접근 안됨|접근 안됨|접근 안됨|

### private 상속 예시

```cpp
class Child : private Parent {
public:
    void show() {
        cout << publicMember;    // ✅ (자식 내부에서는 private으로 변환됨)
        cout << protectedMember; // ✅
        // cout << privateMember; // ❌ 접근 불가
    }
};

Child c;
c.show();            // ✅
// c.publicMember;   // ❌ 외부에서 접근 불가
```

---

## 📌 다중 상속

> **잘 사용하지 않음** — 복잡성과 모호성 문제 때문

```cpp
class Sub : public Sup1, public Sup2 { };
```

### ⚠️ 다중 상속의 문제점 — 모호성 (Ambiguity)

```cpp
class SuperA { public: int x; };
class SuperB { public: int x; };
class Sub : public SuperA, public SuperB { };

Sub obj;
obj.x = 10;         // ❌ 오류: x가 SuperA의 것인지 SuperB의 것인지 불명확
obj.SuperA::x = 10; // ✅ 명시적으로 지정
```

---

## 📌 열거형 enum

> **개념**: 관련 있는 상수들을 묶어서 이름을 붙여주는 자료형 → 코드 가독성 향상

```cpp
enum COLOR { RED, GREEN, BLUE, WHITE, BLACK };
//            0     1      2     3      4   (자동으로 0부터 1씩 증가)
```

### 특징

- `enum`은 **새로운 자료형**
- `{ }` 안의 값들을 **열거 상수**라고 함
- 첫 번째 값은 자동으로 **0**, 이후 1씩 증가
- 열거형 변수는 정의된 값들만 가질 수 있음

```cpp
enum COLOR test;
test = BLUE;
cout << test;    // 2 출력 (BLUE는 2번째 = 정수값 2)
```

---

## 🗂️ 핵심 정리

> **"무엇이 언제 호출되는가"** 를 항상 추적하자!

| 개념          | 핵심 포인트                                     |
| ----------- | ------------------------------------------ |
| 복사 생성자      | `Car c2(c1)` 또는 `Car c2 = c1` 모두 복사 생성자 호출 |
| 상속 + 복사 생성자 | 자식 복사 생성자에서 반드시 `Parent(tt)` 명시 호출         |
| 재정의         | 이름·반환형·매개변수 **모두** 동일해야 성립                 |
| 함수 탐색       | 자기 자신 → 직접 부모 → 그 위 부모 순서                  |
| 부모 함수 호출    | `부모클래스명::함수명()` 형태 사용                      |
| 상속 유형       | 대부분 `public` 상속 사용                         |
| 다중 상속       | 가능하지만 모호성 문제 → 잘 쓰지 않음                     |
| 열거형         | `enum 이름 { 상수1, 상수2, ... }` / 0부터 자동 증가    |

---

> 📅 작성일: 2026-05-07  
> 🏫 과목: C++ / OOP  
> 📖 단원: 8_2장 — 복사 생성자, 재정의, 상속 유형, 열거형