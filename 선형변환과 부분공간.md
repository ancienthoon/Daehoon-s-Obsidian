# 🔀 Chapter 7: 선형변환과 부분공간

#선형대수 #선형변환 #부분공간 #계수 #rank #7장

---

## 📌 7.1 선형변환 (Linear Transformation)

### 정의 7-1: 선형변환

> 벡터공간 V → W로 가는 사상 L: V → W가 아래 두 조건을 만족하면 **선형변환(= 선형사상)**이라 한다.

|조건|수식|의미|
|---|---|---|
|가법성|L(u + v) = L(u) + L(v)|더하고 변환 = 변환하고 더함|
|동차성|L(cu) = cL(u)|스칼라배 후 변환 = 변환 후 스칼라배|

> 💡 **두 조건 합쳐서**: L(cu + dv) = cL(u) + dL(v) 로 한 번에 검증 가능

---

### 정리 7-2: 표준행렬 (Standard Matrix)

> R^n에서 R^m으로의 **모든 선형변환**은 행렬에 벡터를 곱하는 변환이다.

```
Lx = L(x1·e1 + x2·e2 + ... + xn·en)
   = x1·L(e1) + x2·L(e2) + ... + xn·L(en)
   = [L(e1)  L(e2)  ...  L(en)] · x
   = A · x
```

$$A = [L(e_1) \quad L(e_2) \quad \cdots \quad L(e_n)]$$

> 💡 **핵심**: 표준기저 벡터들에 L을 적용한 결과를 열로 나열 → 표준행렬 A

---

## 🎨 R² / R³ 공간의 선형변환 정리

|변환 이름|표준행렬 A|변환 효과|
|---|---|---|
|**확대/축소**|`[[α, 0], [0, β]]`|x축으로 α배, y축으로 β배|
|**회전** (θ)|`[[cosθ, -sinθ], [sinθ, cosθ]]`|원점 기준 반시계 θ 회전|
|**반사 (x축)**|`[[1, 0], [0, -1]]`|y좌표 부호 반전|
|**반사 (y축)**|`[[-1, 0], [0, 1]]`|x좌표 부호 반전|
|**층밀림 (x축 방향, k배)**|`[[1, k], [0, 1]]`|x2 = x1 + k·y1|
|**층밀림 (y축 방향, k배)**|`[[1, 0], [k, 1]]`|y2 = k·x1 + y1|

⚠️ **이동변환은 선형변환이 아님!** → L(0) = 0이 성립하지 않기 때문

---

### 동차 표현 (Homogeneous Coordinate)

> **이동변환**을 선형변환처럼 표현하기 위한 트릭

- R² 벡터 `[v1, v2]` → R³ 벡터 `[v1·h, v2·h, h]`로 표현 (동차좌표)
- h=1로 쓰면: `[v1, v2]` → `[v1, v2, 1]`

**이동변환 동차 표현 (R²):**

```
[x2]   [1  0  a] [x1]
[y2] = [0  1  b] [y1]
[1 ]   [0  0  1] [1 ]
```

→ 이렇게 하면 이동변환도 행렬 곱으로 표현 가능 (**아핀변환**)

---

## 🔗 선형변환의 합성과 역변환

### 정의 7-3: 합성

L2 ∘ L1: L1 먼저 적용 후 L2 적용

```
(L2 ∘ L1)(u) = L2(L1(u))
```

> 행렬로 표현: A2·A1 (순서 주의! 오른쪽부터 적용)

---

### 정의 7-6 / 7-7

|용어|정의|
|---|---|
|**선형연산자**|V = W 인 선형변환 (같은 공간 내 변환)|
|**직교연산자**|모든 x, y에 대해 L(x)·L(y) = x·y 를 만족하는 선형연산자|

---

## 🏗️ 7.2 선형변환의 부분공간과 계수

### 4가지 부분공간 한눈에 보기

```
         A (m×n 행렬)
정의역 R^n ──────────────→ 공역 R^m
  │                           │
  │  ┌────────────┐           │  ┌──────────────┐
  └──┤  영공간     │           └──┤  열공간(치역)  │
     │ Nul(A)     │              │  Col(A)       │
     │ Ax=0의 해  │              │  A의 열벡터들의│
     └────────────┘              │  선형결합      │
                                 └──────────────┘
행공간 Row(A) : A의 행벡터들의 선형결합
좌영공간 Nul(A^T) : A^T · x = 0 의 해
```

---

### 📦 1. 열공간 (Column Space) — Col(A)

**정의 7-8:**

> m×n 행렬 A = [a1 a2 ... an] 의 **열벡터들의 선형결합** 집합 → Col(A) = {Ax | x ∈ R^n}

```
y = Ax = x1·a1 + x2·a2 + ... + xn·an
```

**구하는 방법:** A를 행 축소 → **추축열(pivot column)** 위치 찾기 → 원래 A의 그 열들이 기저

⚠️ **주의**: 행 축소한 B의 열이 아니라 **원래 A의 열**이 기저!

**표기:** Col(A) 또는 im(L)

---

### 🕳️ 2. 영공간 (Null Space) — Nul(A)

**정의 7-10:**

> Ax = 0 을 만족하는 R^n의 벡터 집합

$$\text{Nul}(A) = {x \mid x \in \mathbb{R}^n, ; Ax = 0}$$

**구하는 방법:** 첨가행렬 [A|0]을 행 축소 → 자유변수로 해 표현 → span{...} 형태로 기저 작성

**정리 7-11:** 영공간은 R^n의 **벡터공간(부분공간)**이다.

**정리 7-17: 행렬방정식과 영공간**

> x'이 Ax=0의 해이고, y가 Ax=b의 해이면 → **x' + y도 Ax=b의 해** A(x' + y) = Ax' + Ay = 0 + b = b ✓

**표기:** Nul(A) 또는 Ker(A)

---

### 📏 3. 행공간 (Row Space) — Row(A)

**정의 7-12:**

> m×n 행렬 A의 **행벡터들의 선형결합** 집합

**구하는 방법:** A를 행 축소한 B의 영이 아닌 행들이 기저

⚠️ 열공간 기저는 원래 행렬에서, 행공간 기저는 행 축소 후 행렬에서!

**정리 7-18:** dim(Row(A)) = dim(Col(A))

**정리 7-19: 행공간과 영공간의 직교성**

> Row(A) ⊥ Nul(A) (행공간과 영공간은 직교)

**정리 7-20:** Col(A) = Row(A^T)

---

### ⬅️ 4. 좌영공간 (Left Null Space) — Nul(A^T)

**정의 7-14:**

> 전치행렬 A^T의 영공간 = Nul(A^T) 즉, A^T · x = 0을 만족하는 해의 집합

**구하는 방법:** A^T를 구한 뒤 영공간 계산과 동일

---

### 🔢 계수 (Rank)

**정의 7-15 / 7-16 (선형변환 관점):**

> - 상공간/치역: im(L) = {L(f) | f ∈ V}
> - 영공간/핵: ker(L) = {f | L(f) = 0, f ∈ V}

**행렬의 계수:**

```
rank(A) = dim(Col(A)) = dim(Row(A))
        = 기약행 사다리꼴의 추축열 수
        = 추축성분(pivot)의 수
```

---

### 📐 계수의 성질 (정리 모음)

|정리|내용|
|---|---|
|**정리 7-21**|rank(A) = rank(A^T)|
|**정리 7-22**|rank(AB) ≤ rank(A), rank(AB) ≤ rank(B)|
|**정리 7-23**|rank(AB) ≤ min{rank(A), rank(B)}|
|**정리 7-24**|B가 가역이면: rank(AB) = rank(A)|
|**정리 7-25**|rank(A^T A) = rank(AA^T) = rank(A)|
|**정리 7-26**|B, C가 가역이면: rank(BAC) = rank(A)|

> 💡 **핵심 암기**: 가역행렬을 곱해도 rank는 변하지 않는다!

---

### 🔗 Rank-Nullity 정리 (차원 정리)

> **m×n 행렬 A에 대해:**

$$\text{rank}(A) + \text{nullity}(A) = n$$

|용어|의미|
|---|---|
|rank(A)|Col(A)의 차원 = 추축열 수|
|nullity(A)|Nul(A)의 차원 = 자유변수 수|
|n|행렬의 열 수 (정의역 차원)|

**예시:** 5×3 행렬에서 rank=2이면 nullity = 3-2 = 1

---

## 📐 계수의 응용

### 연립선형방정식 해의 개수

|조건|결과|
|---|---|
|rank([A\|b]) > rank(A)|**불능** (해 없음)|
|rank([A\|b]) = rank(A)|**해 존재** (최소 1개)|
|rank([A\|b]) = rank(A) = n (미지수 수)|**유일한 해**|
|rank 같고 자유변수 존재|**부정** (무한히 많은 해)|

### 치역의 차원

> m×n 행렬 M이 R^n → R^m 으로 사상할 때, M의 계수가 r이면  
> **치역은 R^m의 r차원 부분공간**이 된다.

---

## 💡 핵심 정리

> ### 시험 포인트 요약

**4가지 부분공간 관계:**

```
열공간 Col(A) ⊂ R^m         (공역 쪽)
영공간 Nul(A) ⊂ R^n         (정의역 쪽)
행공간 Row(A) ⊂ R^n         (정의역 쪽)
좌영공간 Nul(A^T) ⊂ R^m    (공역 쪽)

Row(A) ⊥ Nul(A)             (직교!)
Col(A) ⊥ Nul(A^T)           (직교!)
```

**차원 관계:**

- dim(Col(A)) = dim(Row(A)) = rank(A)
- rank(A) + nullity(A) = n (열 수)

**기저 구하는 법:**

- 열공간: 행 축소 후 추축열 위치 → 원래 A의 해당 열
- 행공간: 행 축소 후 B의 영이 아닌 행
- 영공간: Ax=0 풀어서 자유변수 parametrize
- 좌영공간: A^T의 영공간 = A^T x = 0 풀기

> ### ⚠️ 실수 주의

- 열공간 기저: 행 축소한 B의 열 ❌ → **원래 A의 추축열** ✓
- 행공간 기저: 원래 A의 행 ❌ → **행 축소한 B의 비영 행** ✓
- rank(A) = 추축열 수 = 추축행 수 (같음)
- 이동변환은 **선형변환 아님** (L(0) ≠ 0)
- 회전행렬: cos θ는 대각, -sin θ는 오른쪽 위 (반시계 방향 기준)
- rank-nullity: n은 행 수가 아니라 **열 수** (정의역 차원)