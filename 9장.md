# 큐 (QUEUE)

---

## 📌 큐란?

> **먼저 들어온 데이터가 먼저 나가는 자료구조**

- **FIFO** (First-In First-Out): 선입선출
- 비유: 매표소 대기열, 일방통행 도로의 자동차

```
[입력 방향] →  | 3 | 7 | 5 |  → [출력 방향]
                후단(rear)       전단(front)
```

---

## 🔧 큐의 기본 연산

|연산|설명|
|---|---|
|`enqueue(x)`|큐의 **후단(rear)**에 데이터 삽입|
|`dequeue()`|큐의 **전단(front)**에서 데이터 삭제 및 반환|

### 연산 예시

```
enqueue(3) → [3]
enqueue(7) → [3, 7]
enqueue(5) → [3, 7, 5]
dequeue()  → [7, 5]   (3 반환)
dequeue()  → [5]      (7 반환)
```

---

## 📦 큐의 종류

### 1. 선형 큐 (Linear Queue)

- 배열을 **직선**으로 사용
- front / rear 포인터로 위치 추적
- ⚠️ **단점**: dequeue 후 앞쪽 공간이 낭비됨 → 삽입 계속하려면 요소를 앞으로 이동해야 함

```
초기: front=rear=-1
[-1][0][1][2][3][4][5]
 ↑↑
 front rear

enqueue(3) 후:
[0][1][2][3][4][5]
 3
 ↑  ↑
front  rear
```

### 2. 원형 큐 (Circular Queue)

- 배열의 끝과 시작을 **연결**하여 원형으로 사용
- 선형 큐의 공간 낭비 문제 해결
- 인덱스 계산: `(index + 1) % MAX_SIZE`

```
    [3]
  [2] [4]
[1]     [5]
  [0] [MAX-1]
     ↑
  (원형 구조)
```

---

## 📚 STL의 큐 (`#include <queue>`)

### 멤버 함수 표

| 멤버 함수         | 기능                         |
| ------------- | -------------------------- |
| `q.size()`    | 저장된 원소의 **개수** 반환          |
| `q.empty()`   | 큐가 비어있으면 `true` 반환         |
| `q.front()`   | **가장 먼저** 들어간 원소 반환 (삭제 X) |
| `q.back()`    | **가장 나중에** 들어간 원소 반환       |
| `q.push(val)` | 큐의 **뒤**에 val 삽입           |
| `q.pop()`     | **가장 먼저** 들어간 원소 삭제        |

### 사용 예시

```cpp
#include <iostream>
#include <queue>
using namespace std;

int main() {
    queue<int> q;

    q.push(4);  // [4]
    q.push(7);  // [4, 7]
    q.push(8);  // [4, 7, 8]

    while (q.empty() == false) {
        cout << q.front() << " ";  // 4 7 8 순서로 출력
        q.pop();
    }
    cout << endl;
    // 출력: 4 7 8
}
```

---

## 🔄 덱 (Deque)

> **double-ended queue**: 양쪽 끝에서 모두 삽입/삭제 가능

```
add_front  ←|           |← add_rear
delete_front←| 데이터들 |← delete_rear
get_front  ←|           |← get_rear
           전단(front)    후단(rear)
```

- 큐 + 스택의 기능을 모두 가짐
- STL: `#include <deque>`

---

## 🌐 큐의 응용

### 직접적인 응용

- 시뮬레이션 대기열 (공항 비행기, 은행 줄서기)
- 통신에서 **데이터 패킷** 모델링
- 프린터-컴퓨터 사이의 **버퍼링**

### 간접적인 응용

- 알고리즘 도구 (BFS 등에서 핵심 자료구조)

### 버퍼 역할

```
생산자 → [QUEUE 버퍼] → 소비자
컴퓨터 → [QUEUE 버퍼] → 프린터
```

> 속도가 다른 두 장치 사이에서 데이터를 임시 저장해주는 완충 역할

---

## 🧩 큐의 응용: 미로 문제

### 문제 정의

- 2차원 미로에서 `S`(출발점) → `E`(도착점) 최단거리 탐색
- 기호: `-` 빈 공간 / `#` 벽 / `S` 출발 / `E` 도착

### 미로 예시

```
S-###
-----
##---
E#---
---##
```

### 풀이 방법: BFS (너비 우선 탐색)

> **Queue를 이용한 Hill Climbing 알고리즘**

```
단계별 탐색 거리 기록:
S-###    S-###    S-###
1----    123--    123--
##---    ##---    ##4--
E#---    E#---    E#5--
---##    ---##    --6##
```

- 출발점에서 **가까운 곳부터** 차례로 탐색 → 최단거리 보장
- ⚠️ 이 방법은 **최단 거리만** 구할 수 있고, 이동 경로 자체를 추적하지는 못함

### BFS 핵심 원리

```
1. 출발점을 큐에 넣는다
2. 큐에서 꺼낸 위치의 상하좌우를 탐색
3. 방문하지 않은 빈 칸이면 큐에 추가 + 거리 기록
4. 도착점에 도달하면 종료
```

---

## 📝 관련 문제 (ascode.org)

|난이도|문제 번호|
|---|---|
|기본|1315|
|도전|1329, 1316|

---

## 💡 핵심 정리

> [!summary] 큐 핵심 요약
> 
> - **FIFO** 구조: 먼저 넣은 것이 먼저 나옴
> - **선형 큐**: 배열 직선 사용, 공간 낭비 문제 있음
> - **원형 큐**: 배열을 원형으로 연결, 공간 효율적
> - **덱**: 양쪽에서 삽입/삭제 가능한 확장형 큐
> - **STL**: `queue` 헤더, `push` / `pop` / `front` / `back` 사용
> - **대표 알고리즘**: BFS (너비 우선 탐색)에 핵심적으로 사용됨

---

#자료구조 #큐 #Queue #FIFO #BFS #STL #오픈소스프로젝트