# 🧩 오픈소스프로젝트 5강 — 효율적인 알고리즘

---

## 🎯 강의 목표

- 체계적이고 효율적인 프로그래밍 기법이 요구되는 문제들을 유형별로 학습
- **동일한 결과**라도 더 빠르고, 간결하고, 이해하기 쉬운 코드 작성을 목표로 함
- 다양한 방법을 비교하여 차이점과 장단점을 파악

> 💡 핵심 원칙: 입력/결과값의 범위를 먼저 파악 → 적절한 자료형 선택

---

## 📌 목차

|번호|문제명|핵심 키워드|
|---|---|---|
|1228/1229|숫자의 합 구하기|반복문 vs 수학 공식, 타입 선택|
|1432|피타고라스의 정리|3중 반복문, 중복 제거|
|1230|최대 삼각형 찾기|브루트포스 vs 정렬 후 탐색|
|—|제비 뽑기|O(n⁴) → O(n³logn) → O(n²logn)|
|1231|어떤 소수일까? (도전)|나머지 동일성, 유한/무한 소수 판별|

---

## ➕ 1228 / 1229 — 숫자의 합 구하기

### 📋 문제 요약

- `n`부터 `m`까지 모든 정수의 합 출력
- 범위: `-10^5 <= n <= m <= 10^5`
- 여러 테스트 케이스 (`T`개) 처리

### 💡 예시

```
입력: T=2 / 1 100 / -11 10
출력: 5050 / -11
```

### 방법 비교

|방법|원리|시간복잡도|특징|
|---|---|---|---|
|방법 1|반복문으로 직접 합산|O(m-n)|직관적, 단순|
|방법 2|가우스 합 공식 사용|O(1)|빠름, 경우 분기 필요|

> ⚠️ 결과값이 `int` 범위를 초과할 수 있으므로 `long long` 사용 권장

### 💻 방법 1 — 반복문 (C++)

```cpp
#include <iostream>
using namespace std;

int main() {
    int T;
    cin >> T;
    while (T--) {
        long long n, m;
        cin >> n >> m;
        long long sum = 0;
        for (long long k = n; k <= m; k++) {
            sum += k;
        }
        cout << sum << "\n";
    }
    return 0;
}
```

### 💻 방법 2 — 수학 공식 (C++)

```cpp
#include <iostream>
using namespace std;

// 1부터 x까지의 합 (x >= 0 일 때)
long long sumTo(long long x) {
    return x * (x + 1) / 2;
}

int main() {
    int T;
    cin >> T;
    while (T--) {
        long long m, n, fsum = 0;
        cin >> m >> n;

        if (m <= 0 && n >= 0) {
            // 음수 구간 합 + 양수 구간 합
            long long sum1 = -((-m) * (-m + 1) / 2);
            long long sum2 = sumTo(n);
            fsum = sum1 + sum2;
        } else if (m >= 0 && n >= 0) {
            // 전체 - m 이전 구간
            fsum = sumTo(n) - sumTo(m - 1);
        } else {
            // 둘 다 음수: 절댓값으로 계산 후 부호 처리
            long long sum1 = -((-m) * (-m + 1) / 2);
            long long sum2 = -((-(n + 1)) * (-(n + 1) + 1) / 2);
            fsum = sum1 - sum2;
        }
        cout << fsum << "\n";
    }
    return 0;
}
```

> ⚠️ 교안의 방법 2는 경우를 3가지로 분기 처리함. 더 간결한 방법은 아래처럼 공식 통일:

```cpp
// 가장 간결한 방법 (범용 가우스 공식)
long long sumRange(long long a, long long b) {
    return (a + b) * (b - a + 1) / 2;
}
```

---

## 📐 1432 — 피타고라스의 정리

### 📋 문제 요약

- 빗변의 최대 길이 `n`이 주어지면, `a² + b² = c²`을 만족하는 모든 (a, b, c) 출력
- 조건: `a <= b <= c <= n`

### 💡 예시

```
입력: 10
출력:
3, 4, 5
6, 8, 10
```

### 💡 중복 방지 전략

```
a <= b <= c 조건을 반복문 범위에 명시하면
(3,4,5)와 (4,3,5)가 동시에 출력되는 문제를 방지할 수 있음
```

### 💻 C++ 코드

```cpp
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    for (int a = 1; a <= n; a++) {
        for (int b = a; b <= n; b++) {      // b는 a 이상
            for (int c = b; c <= n; c++) {  // c는 b 이상
                if (a * a + b * b == c * c) {
                    cout << a << ", " << b << ", " << c << "\n";
                }
            }
        }
    }
    return 0;
}
```

> ⚠️ `b = a`부터, `c = b`부터 시작하는 것이 핵심 — 이 범위 설정으로 중복 자동 제거

---

## 🔺 1230 — 최대 삼각형 찾기

### 📋 문제 요약

- n개의 봉 중 3개를 골라 **둘레가 가장 긴 삼각형** 만들기
- 삼각형 조건: 가장 긴 변 < 나머지 두 변의 합
- 삼각형 불가 시 `0` 출력

### 💡 예시

```
n=5, a={2,3,10,5,4} → 출력: 12 (3+4+5)
n=4, a={4,20,10,5}  → 출력: 0 (삼각형 불가)
```

### 방법 비교

|방법|원리|시간복잡도|
|---|---|---|
|방법 1|3중 반복문으로 모든 조합 검사|O(n³)|
|방법 2|**정렬 후** 인접한 세 수만 검사|O(n log n)|

### 💡 방법 2 핵심 아이디어

```
정렬 후 인접한 세 수 a[i], a[i+1], a[i+2]만 비교하면 됨
→ 정렬되어 있으면 a[i] + a[i+1] > a[i+2] 만 확인하면 삼각형 조건 성립
→ 둘레가 가장 커지려면 뒤에서부터 검사
```

### 💻 방법 1 — 브루트포스 (C++)

```cpp
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];

    int maxPerimeter = 0;
    for (int i = 0; i < n; i++) {
        for (int j = i + 1; j < n; j++) {
            for (int k = j + 1; k < n; k++) {
                int s1 = a[i], s2 = a[j], s3 = a[k];
                // 삼각형 조건 (가장 긴 변 < 나머지 합)
                int mx = max({s1, s2, s3});
                int sum = s1 + s2 + s3;
                if (sum - mx > mx) {
                    maxPerimeter = max(maxPerimeter, sum);
                }
            }
        }
    }
    cout << maxPerimeter << "\n";
    return 0;
}
```

### 💻 방법 2 — 정렬 후 탐색 (C++) ⭐권장

```cpp
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];

    sort(a.begin(), a.end());  // 오름차순 정렬

    // 뒤에서부터 인접한 세 수 검사 → 첫 번째 성공이 최대 둘레
    for (int i = n - 1; i >= 2; i--) {
        if (a[i - 2] + a[i - 1] > a[i]) {
            cout << a[i - 2] + a[i - 1] + a[i] << "\n";
            return 0;
        }
    }
    cout << 0 << "\n";
    return 0;
}
```

---

## 🎰 제비 뽑기 — 알고리즘 효율성 비교

### 📋 문제 요약

- n개의 숫자 중 **복원 추출로 4번** 선택
- 4개의 합이 m이 되는 경우가 있으면 `Yes`, 없으면 `No`
- 조건: `1 <= n <= 50`, `1 <= m <= 10^8`, `1 <= ki <= 10^8`

### 💡 예시

```
n=3, m=10, k={1,3,5} → Yes  (5+1+1+3=10 등)
n=3, m=9,  k={1,3,5} → No
```

### 🔥 방법별 성능 비교

|방법|복잡도|핵심 아이디어|메모리|
|---|---|---|---|
|방법 1|**O(n⁴)**|4중 반복문 완전탐색|적음|
|방법 1 개선|**O(n⁴)**|함수화 + 조기 return|적음|
|방법 2|**O(n³ log n)**|3중 반복 + 이진탐색|적음|
|방법 3|**O(n² log n)**|2중 합 배열 생성 + 이진탐색|**n²** 추가 필요|

---

### 💻 방법 1 — 4중 반복문 O(n⁴) (C++)

```cpp
#include <iostream>
using namespace std;

const int MAX_N = 50;

int main() {
    int n, m, k[MAX_N];
    cin >> n >> m;
    for (int i = 0; i < n; i++) cin >> k[i];

    bool found = false;
    for (int a = 0; a < n && !found; a++)
        for (int b = 0; b < n && !found; b++)
            for (int c = 0; c < n && !found; c++)
                for (int d = 0; d < n; d++)
                    if (k[a] + k[b] + k[c] + k[d] == m) {
                        found = true;
                        break;
                    }

    cout << (found ? "Yes" : "No") << "\n";
    return 0;
}
```

---

### 💻 방법 1 개선 — 함수화 + 조기 종료 O(n⁴) (C++)

```cpp
#include <iostream>
using namespace std;

const int MAX_N = 50;
int n, m, k[MAX_N];

bool bFound() {
    for (int a = 0; a < n; a++)
        for (int b = 0; b < n; b++)
            for (int c = 0; c < n; c++)
                for (int d = 0; d < n; d++)
                    if (k[a] + k[b] + k[c] + k[d] == m)
                        return true;  // 발견 즉시 종료
    return false;
}

int main() {
    cin >> n >> m;
    for (int i = 0; i < n; i++) cin >> k[i];
    cout << (bFound() ? "Yes" : "No") << "\n";
    return 0;
}
```

> ⚠️ `return true` 조기 종료가 핵심 — 최악의 경우 O(n⁴)이지만 평균적으로 훨씬 빠름

---

### 💻 방법 2 — 이진탐색 활용 O(n³ log n) (C++)

```cpp
#include <iostream>
#include <algorithm>
using namespace std;

const int MAX_N = 50;
int n, m, k[MAX_N];

// 직접 구현한 이진탐색 (k 배열 대상)
bool binSearch(int x) {
    int left = 0, right = n;
    while (right - left >= 1) {
        int mid = (left + right) / 2;
        if (k[mid] == x) return true;
        else if (k[mid] < x) left = mid + 1;
        else right = mid;
    }
    return false;
}

bool found() {
    for (int a = 0; a < n; a++)
        for (int b = 0; b < n; b++)
            for (int c = 0; c < n; c++)
                if (binSearch(m - k[a] - k[b] - k[c]))
                    return true;
    return false;
}

int main() {
    cin >> n >> m;
    for (int i = 0; i < n; i++) cin >> k[i];
    sort(k, k + n);  // 이진탐색을 위해 반드시 정렬

    cout << (found() ? "Yes" : "No") << "\n";
    return 0;
}
```

> ⚠️ 이진탐색 전 **반드시 `sort()`** 호출해야 함 💡 STL의 `binary_search(k, k+n, x)`로 대체 가능

---

### 💻 방법 3 — 2합 배열 + 이진탐색 O(n² log n) (C++) ⭐최고 성능

```cpp
#include <iostream>
#include <algorithm>
using namespace std;

const int MAX_N = 50;
int n, m, k[MAX_N];
int kk[MAX_N * MAX_N];  // 2개 합의 모든 경우를 저장

// kk 배열 대상 이진탐색
bool binSearch2(int x) {
    int left = 0, right = n * n;
    while (right - left >= 1) {
        int mid = (left + right) / 2;
        if (kk[mid] == x) return true;
        else if (kk[mid] < x) left = mid + 1;
        else right = mid;
    }
    return false;
}

bool found() {
    for (int a = 0; a < n; a++)
        for (int b = 0; b < n; b++)
            if (binSearch2(m - k[a] - k[b]))
                return true;
    return false;
}

int main() {
    cin >> n >> m;
    for (int i = 0; i < n; i++) cin >> k[i];

    // 2개 합의 모든 경우 미리 계산
    for (int c = 0; c < n; c++)
        for (int d = 0; d < n; d++)
            kk[c * n + d] = k[c] + k[d];

    sort(kk, kk + n * n);  // 이진탐색을 위해 정렬

    cout << (found() ? "Yes" : "No") << "\n";
    return 0;
}
```

### 💡 방법 3 핵심 아이디어

```
k[a]+k[b]+k[c]+k[d] == m
→ (k[a]+k[b]) + (k[c]+k[d]) == m
→ 앞 두 수의 합(n²가지)을 미리 배열에 저장하고 정렬
→ 뒤 두 수의 합을 이진탐색으로 찾기
→ 반복 횟수: n² → n²log(n²) = O(n²log n)
```

---

## 🔢 1231 — 어떤 소수일까? (도전 문제)

### 📋 문제 요약

- 분수의 분자, 분모가 주어질 때 **유한 소수**인지 **무한 소수**인지 판별

### 💡 예시

```
1/2 = 0.5         → 유한 소수
5/8 = 0.625       → 유한 소수
2/3 = 0.666...    → 무한 소수
1/7 = 0.142857... → 무한 소수
```

### 💡 핵심 아이디어

```
나눗셈 과정에서 나머지가 반복되면 → 무한 소수
나머지가 0이 되면              → 유한 소수

판별 방법: 나머지를 배열에 저장 → 이미 나온 나머지가 다시 등장하면 무한 소수
          (나머지는 0 ~ 분모-1 범위이므로 최대 분모 번 이내에 판별 가능)
```

### 💻 C++ 코드 (개념 구현)

```cpp
#include <iostream>
#include <vector>
using namespace std;

int main() {
    long long numerator, denominator;
    cin >> numerator >> denominator;

    // 먼저 기약분수로 만들기 (GCD 사용)
    auto gcd = [](long long a, long long b) -> long long {
        while (b) { a %= b; swap(a, b); } return a;
    };
    long long g = gcd(numerator, denominator);
    denominator /= g;  // 분모만 약분 (분자의 값은 판별에 불필요)

    // 나머지 등장 여부 추적
    vector<bool> seen(denominator, false);
    long long remainder = numerator % denominator;

    while (remainder != 0) {
        if (seen[remainder]) {
            cout << "무한 소수\n";
            return 0;
        }
        seen[remainder] = true;
        remainder = (remainder * 10) % denominator;
    }
    cout << "유한 소수\n";
    return 0;
}
```

> 💡 수학적으로 더 간단한 방법: **분모를 기약분수로 만든 후 2와 5의 소인수만 남는지 확인**

```cpp
// 더 간결한 방법
long long g = __gcd(numerator, denominator);
denominator /= g;
while (denominator % 2 == 0) denominator /= 2;
while (denominator % 5 == 0) denominator /= 5;
cout << (denominator == 1 ? "유한 소수" : "무한 소수") << "\n";
```

---

## 🎯 핵심 정리

> **이 강의의 알고리즘 효율성 개선 패턴**

|기법|효과|사용 시점|
|---|---|---|
|수학 공식 적용|O(n) → O(1)|합/곱 등 수식으로 표현 가능할 때|
|정렬 후 탐색|O(n³) → O(n log n)|입력 데이터를 재사용할 때|
|이진탐색 적용|O(n) → O(log n)|정렬된 배열에서 존재 여부 확인|
|2합 배열 선처리|O(n⁴) → O(n²log n)|메모리를 희생하고 속도를 얻을 때|
|조기 종료 (early exit)|실질적 속도 향상|발견 즉시 탐색 중단 가능할 때|

### ⚠️ 자주 하는 실수

- `int` 사용 시 범위 초과 → 합산 결과에 **`long long`** 사용
- 이진탐색 전 **`sort()` 누락** → 잘못된 결과
- 피타고라스/삼각형에서 중복 방지 → 반복문 범위를 `j = i+1`, `k = j+1`로 설정
- 제비뽑기 방법 3에서 `kk` 배열 크기 → `MAX_N * MAX_N` 크기 필요
- 삼각형 불가 시 `0` 출력 누락 주의

---

#오픈소스프로젝트 #알고리즘 #효율성 #CPP #이진탐색 #정렬 #피타고라스 #제비뽑기