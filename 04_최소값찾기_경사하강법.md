---

title: "5부. 최소값 찾기 — Global Approach vs 경사하강법" course: 딥러닝기초 week: 1 tags: [딥러닝, 딥러닝기초, 경사하강법, 미분, 학습률, 선형회귀, SGD, 2학기전공과목] 관련교재:

- "처음 배우는 딥러닝 수학 (3~4장)"
- "모두의 딥러닝 (3장, 가장 훌륭한 예측선 긋기)"

---

# 5부. 최소값 찾기 — Global Approach vs Local Approach(경사하강법)

> [!info] 이 파트의 위치 [[03_비용함수와_최소제곱법|4부]]에서 "비용함수 $E(a)$를 최소로 만드는 a를 찾자"까지 정했다. 이제 **그 a를 실제로 어떻게 계산하는가**가 이 파트의 주제.

## 목차 (교수님 제시)

> [!question] How to find the minimum?
> 
> 1. **Global approach**
> 2. **Local approach**

---

## 1. Global Approach — 미분으로 "한 방에" 찾기

### 핵심 아이디어

비용함수 $E(a)$는 a에 대한 **2차 함수(포물선)** 모양이다. ($E(a) = \sum (y_i - ax_i)^2$을 전개하면 $a^2$ 항이 나오기 때문)

> [!tip] 비유 밥그릇처럼 생긴 곡선을 떠올려보자. 이 그릇의 가장 낮은 바닥 지점이 우리가 찾는 "최소가 되는 a"다.

### 바닥(최솟값)의 특징

포물선의 가장 낮은 지점에서는 **접선의 기울기가 0**이다. 오르막도 내리막도 아닌, 딱 평평해지는 순간이기 때문.

$$ \frac{dE(a)}{da} = 0 \text{을 만족하는 } a \text{를 찾는다} $$

즉 $E(a)$를 a로 **미분**해서 0이 되는 지점을 방정식으로 풀면, **한 번의 계산으로 정확한 정답 a**를 바로 얻는다. → "Global"이라는 이름의 이유: 그래프 전체를 수식으로 분석해서 정답을 통째로 찾아내기 때문.

📌 **교재 연결**: **처음 배우는 딥러닝 수학 3~4장**의 편미분·연립방정식으로 정식화되는 **최소제곱법의 해석적 해(analytical solution)**. 통계학의 "회귀직선 공식"이 바로 이 방법의 결과물.

### 왜 딥러닝에서는 이 방법을 잘 안 쓸까?

|변수가 1개일 때|실제 딥러닝 모델|
|---|---|
|미분해서 0으로 놓고 풀면 됨 (손으로도 가능)|변수(가중치)가 **수백만~수십억 개**|
|계산 간단|모든 변수에 대해 동시에 미분=0인 연립방정식을 정확히 푸는 것 = **계산적으로 불가능**|

→ 그래서 필요한 것이 **Local Approach = 경사하강법**.

---

## 2. Local Approach — 경사하강법 (Gradient Descent)

> [!success] 이 개념이 곧 [[01_강의소개와_문제제기#1. 첫 번째 질문 — 함수 그래프에서 한 점을 고른다면?|1부의 질문]]에 대한 최종 답변이다. "그래프를 볼 수 없다면 최솟값을 어떻게 찾을 것인가?" → 경사하강법으로 찾는다.

### 핵심 아이디어: "한 방에 계산" 대신 "조금씩 다가가기"

> [!tip] 비유 — 눈 가린 등산객 산속에서 눈을 가리고 있어 전체 지형은 못 보지만, **발밑의 경사(기울기)는 느낄 수 있다.**
> 
> 1. 지금 위치의 기울기를 확인한다.
> 2. 기울기가 아래로 향하는 방향으로 한 걸음 옮긴다.
> 3. 다시 기울기를 확인하고 또 한 걸음 옮긴다.
> 4. 기울기가 거의 0(평평)이 될 때까지 반복한다.

### 수식

$$ a_{\text{new}} = a_{\text{old}} - \eta \cdot \frac{dE(a)}{da} $$

|기호|의미|
|---|---|
|$\dfrac{dE(a)}{da}$|현재 위치에서의 기울기(미분값) — 어느 방향이 오르막인지 알려줌|
|$\eta$ (에타)|**학습률(learning rate)** — 한 걸음의 보폭 크기|
|$-$ (마이너스 부호)|기울기의 **반대 방향**(내리막 방향)으로 이동한다는 뜻|

### 왜 기울기의 "반대" 방향으로 갈까?

- 기울기가 **양수**(오르막) → a를 **줄여야** 아래로 내려감 → $-$ 부호가 자동 처리
- 기울기가 **음수**(반대쪽이 내리막) → a를 **늘려야** 아래로 내려감 → 역시 $-$ 부호가 처리

즉 부호 하나로 **"항상 낮은 쪽으로 가라"**는 규칙을 표현한 것.

### 학습률(η)의 중요성

|학습률이 너무 크면|학습률이 너무 작으면|
|---|---|
|최소 지점을 훌쩍 지나쳐 버림, 심하면 발산(진동)|바닥 도달까지 너무 오래 걸림 (비효율)|

> [!tip] 비유 계단을 한 칸씩 아주 조심조심 내려가면(학습률 작음) 시간이 오래 걸리고, 3칸씩 성큼성큼 뛰어내려가면(학습률 큼) 발을 헛디뎌 넘어질 수 있다.

---

## 3. 코드와의 연결 — `optimizer='sgd'`의 정체

[[01_강의소개와_문제제기#3. 실습 코드 뜯어보기|1부]]에서 봤던 코드:

```python
model.compile(optimizer='sgd', loss='mse')
model.fit(x, y, epochs=200)
```

|코드|대응 개념|
|---|---|
|`loss='mse'`|[[03_비용함수와_최소제곱법\|4부]]에서 배운 **비용함수(최소제곱법)**|
|`optimizer='sgd'`|이 노트에서 배운 **경사하강법**(Stochastic Gradient Descent, 확률적 경사하강법)|
|`epochs=200`|$a_{\text{new}} = a_{\text{old}} - \eta \cdot \dfrac{dE(a)}{da}$ 계산을 **200번 반복**|

> [!important] 정리 아무 생각 없이 실행했던 딱 두 줄의 코드가, 지금까지 배운 수학 전체(비용함수 + 미분 + 학습률 + 반복)를 압축해놓은 것이었다.

📌 **교재 연결**: 경사하강법은 **처음 배우는 딥러닝 수학 4장**에서 편미분과 함께 정식으로 다루고, **모두의 딥러닝 3장(가장 훌륭한 예측선 긋기)**에서 `sgd`라는 코드 옵션으로 바로 사용됨.

---

## 4. 선형회귀(Linear Regression) 정리 & 시뮬레이션

지금까지의 내용을 "선형회귀"라는 이름으로 정리.

- 🔗 시뮬레이션: https://metamath1.github.io/noviceml/linreg.html
- 학습률을 바꿔가며 직선이 점점 데이터에 맞춰지는 과정을 **직접 조작**해볼 수 있음.
- 개념을 몸으로 익히기 좋은 자료 — 꼭 한 번 실행해볼 것.

---

## 전체 비교 요약

|구분|Global Approach|Local Approach (경사하강법)|
|---|---|---|
|방법|미분 = 0을 풀어서 정답을 **한 번에** 계산|기울기 반대 방향으로 **조금씩** 이동|
|장점|정확한 정답을 얻음|변수가 아무리 많아도 계산 가능|
|단점|변수가 많아지면 계산 불가능|반복 계산 필요, 학습률 설정이 중요|
|딥러닝에서|잘 안 씀 (변수 too many)|**실제로 사용하는 방법**|

---

## Todo / 심화 학습

- [ ] 시뮬레이션(linreg.html)에서 학습률을 여러 값으로 바꿔보며 발산/수렴 관찰
- [ ] 편미분 기초 복습 (처음 배우는 딥러닝 수학 4장)
- [ ] 다음 강의: 신경망 구조(입력층-은닉층-출력층), 다중 퍼셉트론으로 확장 예정

## 관련 노트

- [[01_강의소개와_문제제기]]
- [[02_원의_모양_사고방식전환]]
- [[03_비용함수와_최소제곱법]]