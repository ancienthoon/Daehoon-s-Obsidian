# 7장. 파이썬 자료구조 II — 튜플 · 세트 · 딕셔너리

> [!info] 강의 정보
> 
> - **과목**: 파이썬익스프레스
> - **단원**: 7장 — 파이썬 자료구조 II
> - **연관 노트**: [[6장 리스트]] · [[5장 함수]]

---

## 🎯 학습 목표

- [ ] 튜플(tuple)을 이해하고 사용할 수 있다
- [ ] 세트(set)를 이해하고 활용할 수 있다
- [ ] 딕셔너리(dictionary)를 이해하고 활용할 수 있다
- [ ] 문자열의 각종 연산을 이해하고 활용할 수 있다

---

## 📌 자료구조 개요

> **자료들을 저장하는 여러 가지 구조 = 자료구조(Data Structure)**

### 파이썬 기초 자료구조 — 시퀀스(Sequence)

|특성|설명|
|---|---|
|요소(element)로 구성|순서가 있음|
|인덱스 지원|번호(인덱스)가 붙어 있음|
|공통 연산 지원|인덱싱, 슬라이싱, 덧셈, 곱셈|
|공통 내장함수|`len()`, `max()`, `min()`|

### 자료구조별 특성 비교

|자료구조|순서|변경|중복|표현|
|---|---|---|---|---|
|**리스트**|✅|✅|✅|`[ ]`|
|**튜플**|✅|❌|✅|`( )`|
|**세트**|❌|✅|❌|`{ }`|
|**딕셔너리**|❌|✅|❌(키)|`{키:값}`|

---

## 1️⃣ 튜플 (Tuple)

> **리스트와 유사하지만 변경이 불가능한 불변(immutable) 객체**

### 생성 방법

```python
# 기본 생성 — 쉼표로 항목 분리
t = (1, 'a', 3.14)

# 괄호 없이도 쉼표만 있으면 튜플
t = 1, 'a', 3.14      # 튜플

# 빈 튜플
t = ()
t = tuple()
```

> [!warning] 요소가 하나인 튜플 — 쉼표 필수!
> 
> ```python
> single_tuple = ("apple",)  # ✅ 튜플 → ("apple",)
> no_tuple     = ("apple")   # ❌ 튜플 아님 → 그냥 문자열 "apple"
> 
> single_tuple = (1,)        # ✅ 튜플 → (1,)
> no_tuple     = (1)         # ❌ 튜플 아님 → 정수 1
> ```

### 튜플의 불변성

```python
fruits = ("apple", "banana", "grape")

fruits[1]        # "banana"  → 읽기는 OK
fruits[1] = "pear"  # ❌ TypeError! 변경 불가
# TypeError: 'tuple' object does not support item assignment
```

### 튜플 ↔ 리스트 변환

```python
myList  = [1, 2, 3, 4]
myTuple = tuple(myList)    # [1,2,3,4] → (1,2,3,4)

myTuple = (1, 2, 3, 4)
myList  = list(myTuple)    # (1,2,3,4) → [1,2,3,4]
```

### 튜플 추가 연산

```python
fruits = ("apple", "banana", "grape")

# ✅ 튜플 + 튜플 → 새 객체 생성 (원본 변경 아님)
fruits += ("pear", "kiwi")
# → ("apple", "banana", "grape", "pear", "kiwi")

# ✅ 리스트 += 튜플 → 리스트에 추가 가능
numbers = [10, 20, 30]
numbers += (40, 50)        # → [10, 20, 30, 40, 50]

# ❌ 튜플 += 리스트 → 불가 (튜플에는 튜플만 연결 가능)
numbers = (10, 20, 30)
numbers += [40, 50]        # → TypeError
# 대신: numbers += tuple([40, 50])  으로 해야 함
```

> [!note] 튜플 안에 가변 객체가 있으면?
> 
> ```python
> students = ("kim", [3.1, 3.6, 4.0, 0.0])
> students[1][3] = 4.3     # ✅ 리스트는 변경 가능
> # → ("kim", [3.1, 3.6, 4.0, 4.3])
> # 튜플 자체가 아닌 내부 리스트의 요소를 변경하는 것이라 OK
> ```

### 튜플 패킹 & 언패킹

```python
# 패킹 — 여러 데이터를 하나의 튜플로
t = (1, 2, 3)

# 언패킹 — 튜플의 각 원소를 개별 변수에
s1, s2, s3 = t
s1, s2, s3 = (1, 2, 3)   # 동일

# 스왑 — 튜플로 간결하게
n1, n2 = 10, 90
n1, n2 = n2, n1           # n1=90, n2=10

# 함수의 다중 반환값 받기
n1, n2 = sub()            # sub()가 2개 반환하는 경우
(n1, n2) = sub()          # 동일
```

### enumerate()와 튜플

```python
fruits = ["apple", "banana", "grape"]

# enumerate는 (인덱스, 값) 튜플 반환
for ele in enumerate(fruits):
    print(ele)
# (0, 'apple') / (1, 'banana') / (2, 'grape')

# 언패킹으로 인덱스와 값을 따로 받기
for index, value in enumerate(fruits):
    print(index, value)
# 0 apple / 1 banana / 2 grape
```

### 튜플의 장점

- 반복문에서 리스트보다 **빠르게 작동**
- 함수에서 **여러 값 반환**에 사용
- **불변 데이터** 보호에 적합

> 리스트 함축 `[ ]` → 가능 / **튜플 함축 → 없음** / 세트 함축 `{ }` → 가능

---

## 2️⃣ 세트 (Set)

> **수학의 집합 — 고유한 값만 저장 (중복 불허), 순서 없음**

### 세트의 특성

- 원소 **중복 허용 안 됨** → 자동 제거
- **순서 없음** → 인덱스 사용 불가 (시퀀스 객체 아님)
- 변경 가능한 객체 (추가·삭제 가능)

### 세트 생성

```python
# 방법 1: { } 직접 할당
s = {1, 2, 3, 4, 5}

# 방법 2: set() 함수 — 리스트·문자열 등을 세트로 변환
s = set([1, 2, 3, 4, 5])        # 리스트 → 세트

# 중복 자동 제거
numbers = set([1, 2, 3, 1, 2, 3])
print(numbers)  # {1, 2, 3}

# 문자열 → 세트 (각 문자가 원소)
letters = set("abc")             # {'a', 'b', 'c'}
```

> [!warning] 공백 세트 주의
> 
> ```python
> v = {}     # ❌ 공백 딕셔너리 (세트 아님!)
> v = set()  # ✅ 공백 세트
> ```

### 세트 기본 연산

```python
fruits = {"apple", "banana", "grape"}

# 길이
len(fruits)           # 3

# 포함 여부
"apple" in fruits     # True

# 순회 (순서 보장 안 됨)
for x in fruits:
    print(x, end=" ")
# grape banana apple  ← 출력 순서가 다를 수 있음
```

### 세트 요소 추가·삭제

```python
fruits = {"apple", "banana", "grape"}

fruits.add("orange")      # 요소 1개 추가
fruits.update({"mango", "kiwi"})  # 여러 개 추가

fruits.remove("banana")   # 삭제 — 없으면 ❌ 오류
fruits.discard("banana")  # 삭제 — 없어도 ✅ 오류 없음

fruits.clear()            # 모든 요소 삭제 → set()
```

> [!tip] remove vs discard `remove`: 없는 요소 삭제 시 **KeyError 발생**  
> `discard`: 없는 요소여도 **오류 없이 통과** → 안전한 삭제

### 집합 연산 ⚠️

|연산|연산자|메소드|
|---|---|---|
|**합집합**|`A \| B`|`A.union(B)`|
|**교집합**|`A & B`|`A.intersection(B)`|
|**차집합**|`A - B`|`A.difference(B)`|
|**대칭차집합**|`A ^ B`|`A.symmetric_difference(B)`|

```python
A = {1, 2, 3, 4, 5}
B = {3, 4, 5, 6, 7}

A | B   # {1, 2, 3, 4, 5, 6, 7}  합집합
A & B   # {3, 4, 5}              교집합
A - B   # {1, 2}                 차집합
```

### 부분집합 연산

```python
A = {"apple", "banana"}
B = {"apple", "banana", "grape"}

A <= B   # True  — A가 B의 부분집합
A < B    # True  — A가 B의 진부분집합 (A != B)
A == B   # False — 동일한 집합인지
```

> [!note] 세트 크기 비교는 부분집합 검사 `A < B` = "A가 B의 진부분집합인가?" (숫자 비교 아님!)

### 실용 예시 — 리스트와 세트 결합

```python
# 중복 제거: 리스트에서 고유한 원소 개수
list1 = [1, 2, 3, 4, 5, 1, 2, 4]
len(set(list1))          # 5 (서로 다른 원소의 개수)

# 교집합: 두 리스트의 공통 원소
list1 = [1, 2, 3, 4, 5]
list2 = [3, 4, 5, 6, 7]
set(list1) & set(list2)  # {3, 4, 5}
```

### 세트 함축

```python
# 리스트 함축: [ ]
squares = [x**2 for x in range(5)]      # [0, 1, 4, 9, 16]

# 세트 함축: { }
squares = {x**2 for x in range(5)}      # {0, 1, 4, 9, 16}
```

### 세트 메소드 정리표

|메소드|기능|
|---|---|
|`add(x)`|원소 x 추가|
|`update(s)`|세트 s의 원소들 모두 추가|
|`remove(x)`|x 삭제 (없으면 오류)|
|`discard(x)`|x 삭제 (없어도 오류 없음)|
|`clear()`|모든 원소 삭제|
|`union(s)`|합집합 반환|
|`intersection(s)`|교집합 반환|
|`difference(s)`|차집합 반환|
|`issubset(s)`|부분집합 여부 반환|

---

## 🧪 Lab 예제

### Lab 1 — 두 문자열의 공통 문자 찾기

```python
s1 = input("첫 번째 문자열: ")   # "Hello World!"
s2 = input("두 번째 문자열: ")   # "How are you?"

common = set(s1) & set(s2)
print("공통적인 글자:", common)
# 공통적인 글자: {'o', 'H', 'r', 'e'}
```

### Lab 2 — 중복되지 않은 단어 개수 세기

```python
text = input("입력 텍스트: ")
words = text.split()        # 공백 기준으로 나눠 리스트 반환
unique = set(words)         # 중복 제거
print("사용된 단어의 개수=", len(unique))
print(unique)
```

```python
# split() 기본 사용법
s = "a b c d e f g"
r = s.split(" ")    # ['a', 'b', 'c', 'd', 'e', 'f', 'g']
# s.split() 도 동일 — 공백 기준 분리
```

### Lab 3 — 연락처 관리 프로그램 (딕셔너리 활용 예고)

```
1. 연락처 추가
2. 연락처 삭제
3. 연락처 검색
4. 연락처 출력
5. 종료
메뉴 항목을 선택하시오: 1
이름: KIM
전화번호: 123-4567
```

> 딕셔너리를 이용해 `{이름: 전화번호}` 형태로 저장 → 이번 장 마지막 실습

---

## 💡 핵심 요약

> [!summary] 7장 핵심 정리
> 
> **① 튜플**
> 
> - 리스트와 유사하지만 **변경 불가** (불변 객체)
> - 요소 1개 튜플은 **쉼표 필수**: `(1,)` ← 없으면 그냥 숫자
> - 패킹·언패킹으로 다중 변수 교환, 함수 다중 반환값 수신
> - 튜플끼리만 `+=` 연결 가능 (리스트와 연결 불가)
> 
> **② 세트**
> 
> - 수학의 집합 — **중복 없음**, **순서 없음** (인덱스 사용 불가)
> - 공백 세트: `set()` (`{}` 는 딕셔너리!)
> - 삭제 시: `remove` (오류 가능) vs `discard` (오류 없음)
> - 집합 연산: `|` 합집합 · `&` 교집합 · `-` 차집합
> - 실용: **리스트 중복 제거** → `set(list)` / **공통 원소** → `&`

---

## ✏️ 자주 나오는 함정 정리

|상황|틀리기 쉬운 것|정답|
|---|---|---|
|요소 1개 튜플|`(1)` → 정수|`(1,)` → 튜플|
|공백 세트 생성|`{}` → 딕셔너리|`set()` → 세트|
|세트 삭제|`remove` → 없으면 오류|`discard` → 안전|
|튜플에 리스트 연결|`t += [1,2]` → 오류|`t += tuple([1,2])`|
|세트 크기 비교|`A < B` = 숫자 비교?|`A < B` = 진부분집합 검사|

---

#파이썬 #자료구조 #7장 #파이썬익스프레스 #튜플 #세트 #딕셔너리 #tuple #set