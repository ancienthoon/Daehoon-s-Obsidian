---

title: "4부. 비용함수와 최소제곱법" course: 딥러닝기초 week: 1 tags: [딥러닝, 딥러닝기초, 비용함수, 손실함수, 최소제곱법, MSE, 선형회귀, 2학기전공과목] 관련교재:

- "처음 배우는 딥러닝 수학 (3장)"
- "모두의 딥러닝 (2장, mse)"

---

#딥러닝기초 

# 4부. 비용함수와 최소제곱법

> [!info] 이 파트의 위치 [[01_강의소개와_문제제기]]에서 코드로만 봤던 `loss='mse'`가 **정확히 무엇을 의미하는지, 왜 그렇게 정의하는지**를 수학적으로 다루는 파트.

---

## 1. 다시 "최소값 찾기" 문제로

> [!quote] 반복되는 핵심 문장 **Deep Learning is to minimize the Loss function.**

- 함수값의 의미 = **비용함수(Cost function)**
- 이 함수가 **닫힌 구간 [c, d]** 위에 있다면 → 최솟값은 반드시 존재 ([[01_강의소개와_문제제기#1. 첫 번째 질문 — 함수 그래프에서 한 점을 고른다면?|1부 질문]]에 대한 수학적 근거)
- 목표: 최소가 되는 지점 `(a, b)`를 어떻게 찾을 것인가?

### 전체 논리 흐름

```
"함수의 최솟값을 어떻게 찾지?"        ← 1부, 질문만 던짐
        ↓
"딥러닝 = 비용함수를 최소화하는 것"    ← 1부, 핵심 명제
        ↓
"비용함수는 어떻게 만들고,
 최솟값은 어떻게 찾지?"               ← 지금 이 노트에서 답변
```

---

## 2. 비유: 아이의 학습 (Child's Learning)

아이가 무언가를 배우는 과정 = 딥러닝의 학습 과정과 동일한 구조.

> [!tip] 비유 아이가 자전거를 처음 탈 때: 여러 번 넘어지고(오차 발생) → 넘어진 방향을 감안해 자세를 조금씩 고치고(오차 수정) → 결국 균형을 잡는다(최적화 완료).

---

## 3. 문제 단순화 — 딥러닝 관점에서의 구조

가장 단순한 예제 데이터:

|x (실제값)|y|
|---|---|
|-1|-1|
|0|0|
|1|1|
|2|2|

- 사람 눈에는 `y = x`라는 관계가 바로 보이지만, **딥러닝은 이걸 모른다고 가정**하고 데이터만으로 찾아야 한다.
- 예측 모델을 **y = ax** (절편은 0으로 고정)로 가정.

> [!question] 핵심 질문 a를 얼마로 정해야 이 4개의 점을 가장 잘 표현하는 직선이 될까?

이는 [[02_원의_모양_사고방식전환|2부]]에서 말한 "데이터로부터 규칙을 찾는다"의 가장 단순한 버전이다.

---

## 4. 비용함수 정하기 — 최소제곱법 (Least Square Method)

### "좋은 직선"을 숫자로 표현하려면?

a 값을 이것저것 대입해보면, 각 a마다 **실제값과 예측값의 차이(오차)**가 생긴다. 이 오차를 하나의 숫자로 요약해야 "a=1이 a=2보다 낫다"를 비교할 수 있다.

### 왜 오차를 그냥 더하면 안 될까?

오차 = (실제값 − 예측값)이라 하면, 어떤 점은 오차가 음수, 어떤 점은 양수가 되어 **더하면 서로 상쇄**된다.

> [!tip] 비유 다트를 5번 던져 과녁보다 3cm 왼쪽, 3cm 오른쪽에 번갈아 맞았다면, 평균은 "정중앙에 맞았다"가 되어버린다. 하지만 실제로는 계속 빗나가는 중. → 부호를 없애야 한다.

### 해결책: 제곱해서 더하기

$$ \text{비용함수 } E(a) = \sum_{i=1}^{n} (y_i - \hat{y}_i)^2 $$

- $y_i$ = 실제값, $\hat{y}_i = a \cdot x_i$ = 예측값

제곱의 효과:

- 음수든 양수든 항상 **양수**가 되어 상쇄되지 않음
- 오차가 클수록 **제곱 때문에 훨씬 크게 벌점** (오차 2 → 4점, 오차 10 → 100점)

> [!important] 정체 공개 이 $E(a)$가 바로 코드의 **`loss='mse'` (Mean Squared Error, 평균제곱오차)**의 정체. `mse`는 여기에 데이터 개수 $n$으로 나눠 평균을 낸 것뿐, 핵심 아이디어는 동일.

📌 **교재 연결**: 최소제곱법 = **처음 배우는 딥러닝 수학 3장**의 핵심 내용. **모두의 딥러닝**에서는 2장에서 `mse`라는 이름으로 곧바로 코드에 등장. → 두 책이 같은 개념을 "수학 vs 코드" 각도에서 설명하는 대표 지점.

---

## 5. 학습된 딥러닝 시스템

- 최적의 a로 그린 직선이 (-1,-1), (0,0), (1,1) 점들을 잘 통과하는 것을 확인.
- 즉, **비용함수 $E(a)$를 최소로 만드는 a를 찾음 = 딥러닝 모델의 "학습 완료"** 상태.

---

## 이 파트 요약

> [!success] 한 문장 요약 직선의 기울기 a 하나를 정하는 문제조차, **"오차를 어떻게 숫자 하나로 표현할 것인가(비용함수)"**와 **"그 숫자를 어떻게 최소로 만들 것인가(최적화)"** 라는 두 질문으로 쪼개진다.

## 남은 문제

지금까지 예제는 데이터가 정확히 직선 위에 있어 오차가 0이 되는 완벽한 a를 찾을 수 있었다. 하지만 실제 데이터([[01_강의소개와_문제제기|1부]]의 시험 성적 데이터)처럼 점들이 직선에서 살짝씩 벗어나 있으면, **최적의 a를 수학적으로 어떻게 계산할까?**

→ [[04_최소값찾기_경사하강법|5부. Global Approach vs 경사하강법]]에서 이어짐.

---

## 관련 노트

- [[01_강의소개와_문제제기]]
- [[02_원의_모양_사고방식전환]]
- [[04_최소값찾기_경사하강법]]